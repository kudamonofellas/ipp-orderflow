# Progress Tracker

Update this file after every meaningful implementation
change.

## Current Phase

- Stable and complete: token refresh retry flow, Customers and Products CRUD and directory pages, manual New Order entry (free-text per-line product matching via `AddItemModal`), the in-app WhatsApp-message intake flow (Channel Select → WhatsApp Intake → prefilled New Order, plus the "learned corrections" matching memory reviewable in Settings), and detailed dossiers (Orders, Customers, Products). **The in-app intake flow was removed 2026-08-11 and restored 2026-08-14 per explicit user request — only the Dashboard's automated draft-message triage panel (`IntakePanel`, reading the separate Evolution API → n8n → Directus pipeline) stays removed** — see the Completed entries below.

## Current Goal

- **Manual browser walkthrough still owed for the Customer Return rebuild against `context/designs/customer-return/*.png` (2026-08-26)** — see the Completed entry below; only verified by `tsc -b`/eslint/`npm run build` + live curl: open order #260821020 as Admin/Owner and step through all 5 `RETURN_DOC_OPTIONS` in the Settle dropdown, confirming against the design screenshots each time — button label, the ONE/TWO-document or signing/order-closes-after-receipt helper text below it, and (return-note only) that the "Input in Accurate & printed" checkbox genuinely blocks Confirm until checked; log in as Warehouse on the same order and confirm the per-line box shows the interactive weight input + camera + its own "Confirm received & weighed" button (not the Settle form) — then log in as Admin on it and confirm the reverse (Settle form, no weight input); open order #260821023 as Warehouse and confirm the Incoming Return card shows both pending lines (Ribeye, Salmon) independently — confirm just the Ribeye line and verify the Salmon line stays pending, unaffected, while Ribeye flips to the green "confirmed" state with a checkmark and its photos become non-deletable (no hover-trash); confirm the whole card disappears once the last line is confirmed and the quiet "Incoming return received · Undo" row appears in its place, and that clicking Undo re-opens both lines for re-weighing; confirm a Courier login can attach the signed-DO/SI photo on the Sign step (order #260821022) but cannot see the Settle dropdown or receive-weigh controls on any return; separately confirm the stepper reappears immediately for a replacement decision (stage flips to `cold`) but a return-note/revise-return close leaves the order permanently on the Customer Return card, no stepper, `stage` staying `returned`.
- **Manual browser walkthrough still owed for the return-documents/receive-photo fixes (2026-08-24)** — see the Completed entry below; only verified by `tsc -b`/eslint/`npm run build` + live curl of the ACL change: open order #260821022 and confirm the Return Settlement card shows 2 clickable weigh-back thumbnails (opens the full-size modal) plus a "Return documents" thumbnail; log in as Warehouse (or Admin) on a `returned`-stage order's "Awaiting Return" bucket, tap the camera icon, and confirm a file picker actually opens and the photo appears immediately as a new thumbnail (not silently failing) — repeat on order #260821023's Incoming Return card specifically, since that's where the broken button lived; upload 2+ photos to one line and confirm all show as separate thumbnails, then delete one via the hover-trash and confirm it disappears without affecting the others; reload the page and confirm previously-uploaded photos are still there (persisted, not just local state); pick "Sales Return Note (no replacement)" in the Settle form, attach an optional note photo, confirm, and confirm it shows up in the Return Settlement card afterward; log in as Admin specifically and confirm the `line_return_photos` ACL fix actually lets Admin upload/delete (not just Warehouse).
- **Manual browser walkthrough still owed for the Return Settlement + Incoming Return cards (2026-08-24)** — see the Completed entry below; only verified by `tsc -b`/eslint/`npm run build` + live curl checks of the new field/ACL: log in as Admin (or Warehouse), open a `returned`-stage order with at least one returned line, pick a replacement document type in the Settle step, submit **before** confirming "Awaiting Return" receipt — confirm the order lands back at `cold` with `is_replacement: true` and a new "Incoming return — receive & verify" card appears (log in as Warehouse specifically to confirm the fields ACL fix actually lets the write through, not just Admin/Owner via `fields:["*"]`); enter a verified weight + optional scale photo and tap "Confirm received & weighed", confirm the card disappears and a "Return settlement" card now shows the document label plus the returned line/photo; confirm "Undo" re-opens the Incoming Return card with the value seeded back in. Separately: settle a return into a replacement **after** already confirming receipt in the "Awaiting Return" step — confirm no Incoming Return card appears (not pending) and the Return Settlement card still shows correctly. Confirm the Return Settlement card is visible to every role (including Courier/Finance), not just Warehouse/Admin.
- **Manual browser walkthrough still owed for the full Finance Gate rebuild + cold-stage gating + Terms-invoice follow-up (2026-08-14)** — see the Completed entry below for the full file/logic list; only verified by `tsc -b`/build/lint + code review, plus live curl checks of the 6 new schema fields/ACL: (1) log in as a role with `approveFinance` (Finance, or Owner), open a `cold`-stage order with weighing not yet done — confirm the "Finance gate" card renders the full form (Method/Timing dropdowns, Amount/Bank-reference inputs, Account-exposure block only when Timing is Terms or the customer has a credit limit); (2) Upfront + Transfer: confirm "Clear — OK to proceed" stays disabled until "I verify it in our bank" is tapped, then clears and writes `payment_confirmed`/`payment_method`/`payment_timing`/`payment_amount`/`payment_bank_ref`; Upfront + Cash: same but the verify button reads "Cash received" and no bank-reference field shows; (3) Terms: confirm the exposure/credit-limit block shows real numbers (this customer's other non-terminal orders' value vs `customers.credit_limit`), confirm going over the limit disables Clear for a Finance user without `overrideCreditLimit` but not for Owner, confirm clearing writes `payment_due_date` (deliver_at + customer's term_days); (4) **the actual gating**: weigh an order to completion at `cold` *before* clearing payment — confirm the advance button reads "Release to Finance" (not "Done — Send to Production") and clicking it parks the order at a literal `finance` stage instead of `production`; confirm the Finance gate card still renders there and clearing it both writes the payment fields AND advances the order to `production` in one action; conversely, clear payment *before* weighing finishes — confirm the advance button now reads normally and going to Production skips the `finance` stage entirely; (5) confirm "Undo payment clearance" reaches back correctly: un-clearing while still at `cold` just un-clears in place (order stays at `cold`), but un-clearing after the order reached `production`/`packing`/`finalise` drops the stage back to `finance`; confirm Undo disappears once the order reaches `dispatch`; (6) deliver a Terms-timing order — confirm a "Terms invoice — payment not yet received" row appears in the Follow-ups pending card (with the due date if one was computed), tapping "Payment received" writes `payment_paid_at` and shows the green confirmed card with a working "Undo"; (7) confirm a Warehouse/Admin-helper user at `cold` still sees weighing controls (unaffected by the Finance-gate changes) and that a **Finance** user, even if granted `helpOtherStages` via Settings, never sees weighing controls at `cold` — only the Finance gate card; (8) confirm "Restore Order" on an `outstanding`-stage order now sends it to `dispatch` (not `intake`) with the hand-off chooser reappearing, and that restoring a `cancelled` order is unaffected (still goes to `cancelled_from ?? "intake"`).
- **Manual per-role walkthrough still owed for the floor-helper sweep (2026-08-14)** — see the Completed entry below: Production owner advances a production order; Warehouse owner advances a packing order; Admin advances both as floor helper (already covered by the pre-existing `canAdvance` — should already work, just unverified in a browser); Admin at a *cold* order now sees and can use the weight input + both photo cameras (weighing-photo and item-condition) when covering for Warehouse, not just the stage-advance button; Finance/Courier (no `helpOtherStages`, not stage owners) still cannot advance production or packing.
- **Manual browser walkthrough still owed for the QA feedback batch (2026-08-14)** — see the Completed entry below for the full list; nothing below has been clicked through in a real browser, only `tsc -b`/build/lint + code review, plus a live curl check of the new schema field/ACL: (1) log in as a role without `seePrices` (Warehouse/Production) and confirm a COD/outstanding-collection notification in the bell shows "Rp •••" instead of the real figure; (2) log in as Admin and confirm the Dashboard quick-action row no longer shows "My deliveries" (Pick list / Cash-up should still show per their own capabilities); (3) confirm the "Replacement in Transit" tile in Return Workflows now renders in red/danger for every role that owns it (Warehouse/Production/Courier/Admin), and that a replacement order's own `StatusPill` elsewhere is unaffected (still colours by real current stage); (4) **superseded 2026-08-14 by the full Finance Gate rebuild — see the newer Completed entry below**, this simple-button version no longer exists; (5) from Orders, pick a stage filter + type a search term, open an order, hit Back, confirm the same filter/search is still applied (not reset to "All stages"); (6) confirm a locked order (delivered, or dispatch-and-taken, or outstanding/cancelled/returned) hides the Edit button for a non-Owner role, and that Owner still sees it; (7) click through all 4 sortable tables (Dashboard Open Orders, Orders, Customers, Products) — confirm clicking a column header sorts ascending first then descending on a second click, the `circleArrowUp` icon rotates smoothly rather than jumping, only one column shows the icon at a time, and the Items column (client-side, page-local sort) behaves sensibly even though it doesn't refetch.
- **Manual browser walkthrough still owed for the drop-location distance + address-pin bootstrap (2026-08-13)**: only verified by `tsc -b`/build/lint. Owed: confirm the "Delivered & Closed" banner shows the plain "Delivery location captured · HH:MM" fallback for a customer with no `address_geo` yet; confirm a real own-courier delivery triggers the "Set this as {customer}'s delivery location?" prompt exactly once (not on pickup/3rd-party/denied-GPS deliveries, and never again once the pin exists) and that accepting it makes the very next delivery to that customer show "Dropped at delivery address · ~Nm · HH:MM" with a plausible distance; confirm Customer Edit's new "Delivery Location Pin" field round-trips a plain "lat, lng" paste and a real Google Maps URL paste, rejects garbage input with an inline error instead of saving silently, and that a Courier-role session can complete the bootstrap write (new field-scoped `customers.update` grant) without hitting a 403.
- **Important tooling correction, applies to all "tsc clean" claims in this file**: `npx tsc --noEmit` run against the *root* `tsconfig.json` type-checks **zero files** — root has `"files": []` and only references the app/node sub-projects, so a bare `tsc --noEmit` silently no-ops. Discovered 2026-08-10 when it reported clean while `npx tsc -b` (what `npm run build` actually runs) caught two real errors in `mockDashboard.ts`. Every "tsc clean" claim this session was still backed by a passing `npm run build` (which does run the real `tsc -b` check first), so nothing shipped broken — but going forward, verify with `npx tsc -b` directly, not `npx tsc --noEmit`, for faster real feedback without waiting on the full Vite build.
- Manual live QA pass still owed (dev server not exercised this session): walk one order through all 3 hand-off modes and confirm `order_history` gets an entry each time; confirm a real `courier_locations` row appears after granting browser location permission and that a second (Admin) session's `CourierLiveLocation` badge picks it up within ~20s; log in as Warehouse and confirm both the Orders page and the OOS toggle (list + edit page) now work; log in as Courier and confirm the Dashboard's Open Orders panel now expands with prices hidden; confirm Admin can now log a Document on `OrderDetail.tsx`; click Reorder end-to-end and confirm the new order lands at intake with the same lines/cuts and a fresh number; send an order back from dispatch after a courier already took it and confirm the hand-off chooser reappears next time it's back at dispatch; confirm a kg/loaf line shows no weight inputs/camera outside cold storage, and not for a Finance user at cold; visually confirm all 6 `StatusPill` role colours (+3 terminal states) are actually distinguishable in both light and dark themes — chosen by hex-value reasoning, not eyeballed in a browser; **specifically confirm the dispatch sub-label in a real browser** — the underlying fetch/derivation is now proven correct against live data (see 2026-08-11 entry below), but no one has actually looked at the rendered pill in a running app yet, and the previous bug report's example orders didn't match anything in the live DB, suggesting it may have been observed against a stale bundle or a different running instance.
- **Open question, needs the user to check**: a bug report described "Out for delivery" showing for every dispatch order (should differ by hand-off state) and named specific example orders/customers (Rifai, Ducking, Ivy) that do **not exist** in the live `dev-admin.kudafellas.cloud` database — those names only appear in the prototype's own mock data (`Dev-customers.js`). Investigated 2026-08-11: both described root causes (fields not fetched; inverted/hardcoded label) were already fixed as of the previous session's work, confirmed by re-reading every file in the chain (`useOpenOrders.ts`/`useOrders.ts`'s `fields` arrays, `dispatchSubStatus`/`dispatchSubLabel` in `pipeline.ts`, `StatusPill.tsx`'s render) and by live-testing against 3 temporary throwaway orders created specifically to exercise all 3 hand-off paths (`QA-TEST-UNASSIGNED`/`QA-TEST-PICKUP`/`QA-TEST-3RDPARTY`, created then deleted via curl) — all 5 dispatch orders (2 real + 3 temp) resolved to the correct label. If the user still sees the bug in their browser after a hard refresh / dev-server restart, it's a genuinely new symptom, not the one described in the report — worth a fresh look with the actual URL/account confirmed.
- **Open question, needs the user to check**: they reported the Documents "Failed to log document" error reproduced on *both* Admin and Owner test accounts. The Admin gap is confirmed and fixed (see below). Owner's role has `admin_access: true` in Directus, which bypasses every ACL check unconditionally — Owner getting a Directus permission error should be structurally impossible. If it still reproduces on Owner after this fix, the likely explanation is that their "Owner" test login isn't actually mapped to the Directus role with `admin_access: true` — worth confirming which account/role that session is really using.
- Deferred per user decision: a full row-by-row audit of `prototype-audit.md` §5 (per-field/action gates on `OrderDetail.tsx` and other screens) beyond what was already checked — only the data-layer (field-permission) sweep and the route-guard sweep were completed; individual field-level gates (e.g. Documents section visibility, credit-limit displays) were spot-checked but not exhaustively verified against every row.
- **Manual browser click-through still owed for the new `Modal`/`useDialog()` alert-replacement (2026-08-11)**: no Playwright/browser-automation tooling is installed in this environment, so the 72 converted `window.alert`/`window.confirm` call sites were verified by `tsc -b`/build/lint plus individual code review, not by actually clicking through the app. Worth spot-checking a handful of the higher-traffic ones (Settings' delete-member confirm, OrderDetail's cancel-order confirm and reorder confirm, a couple of the plain error alerts) in a real browser to confirm the modal renders/positions/dismisses correctly and that the `queueRef` chaining doesn't introduce a visible stall when two dialogs fire back-to-back.
- **Manual scroll-QA still owed for the Dashboard notifications batching (2026-08-11)**: confirm in a real browser that scrolling the notifications popover near the bottom actually triggers `loadMore()` (not just that the code compiles), that "Loading more…"/"No more activity." render correctly, and that the bell's unread-count badge behavior (only reflects the currently-loaded batch, not the full history — see the Completed entry below) reads as acceptable in practice, not just in theory.
- **Manual browser walkthrough still owed for the rebuilt Delivery Proof flow (2026-08-11)** — the whole capture → confirm → archive → read-only cycle, plus the new live schema (`attachments.proof_id`, Courier's new `attachments` create grant), has only been verified by `tsc -b`/build/lint, never actually clicked through: (1) own-courier delivery — take delivery, confirm the rest of the form stays hidden until a condition photo is uploaded, confirm "Change method"/action buttons appear once it is, upload 2+ photos to one field and confirm all thumbnails show with a working hover-trash, remove one, confirm delivery, confirm the read-only "Delivery proof" card appears with the right photos grouped and a working delete-with-confirm; (2) click "Change method" or "Delivery failed — retry" mid-capture with photos already staged, confirm a `delivery_proofs` row was created and archived (`archived: true`) with the staged photos linked via `proof_id`, then confirm the 2nd attempt starts clean (no leftover photos) and, once confirmed, only attempt 2's photos show in the read-only card (attempt 1's stay in the DB, just excluded from the default `readDeliveryProofs()` read — worth a direct DB/API spot-check, not just UI); (3) pickup and 3rd-party hand-off modes render their field-label variants correctly and the 3rd-party mode's simplified single-photo flow still works; (4) confirm Courier's new `attachments` `create` grant actually works end-to-end from a real Courier login (not just the admin-token curl check).
- **Real bug found and fixed via live data during that walkthrough prep (2026-08-13)**: `handleSendBack()`'s "Reopen" path (delivered → dispatch) never archived the confirmed `delivery_proofs` row or cleared `taken_by`/`pickup`/`third_party` — only the dispatch→prev "Send back" path did. Repeated reopen → redeliver → confirm cycles piled up multiple non-archived `delivery_proofs` rows for the same order instead of superseding the old one (found 6 on one test order via direct API query), which also explained a "two Delivery proof cards" report: the stale-`taken_by` capture card kept re-rendering (empty, since local photo state resets on refresh) alongside the read-only card. Fixed both the reopen path and added a defensive archive-before-create in `handleConfirmDelivery` itself. **Not yet re-walked in a browser** — verified only via the live data cleanup (archived the 6 stale rows) and code review; still owed: reopen → redeliver → confirm once more and confirm exactly one non-archived row + one visible card results.
- **Manual browser walkthrough still owed for the `useCashUp.ts` partial/outstanding fix (2026-08-14)** — see the Completed entry below; only verified by `tsc -b`/build/lint: confirm an `outstanding`-stage order (short/zero COD collection) now actually appears in the Cash-up queue under its courier, showing the collected amount (not the full order total) plus the new "Partial — balance outstanding" badge; confirm a `delivered` order with a full COD collection still shows the plain order total with no badge; confirm "Confirm" still reconciles correctly for both row types.
- **Manual browser walkthrough still owed for the COD-outcome rework (2026-08-13)**: full/partial/none segment selection, the amount-owed chip, the partial-amount input, the outstanding-reason chips, and the resulting stage (`delivered` vs `outstanding`) + `order_history` text have only been verified by `tsc -b`/build/lint and code review — never clicked through. Also confirm the COD row genuinely never blocks the "Confirm delivery" button (only the photo/name gate does), and that a non-COD order's proof card shows no COD row at all.
- **Manual browser walkthrough still owed for the delivered-order redesign (2026-08-13)** — see the Completed entry below for the full feature list; nothing below has been clicked through in a real browser, only `tsc -b`/build/lint + code review: (1) grant/deny browser location permission at both the condition-photo-upload step and at Confirm delivery, confirm `pickup_geo`/`deliver_geo` populate on grant and the flow proceeds silently (no error, no block) on deny; confirm the "Delivery location captured · HH:MM" line + working "Map" link render in the banner after a real delivered order; (2) confirm the quiet "Undo — back to dispatch" link appears immediately after confirming delivery, disappears after any other action on the order (docs-returned, COD reconcile, cancel, hold, or a second reopen), and that clicking it restores the exact pre-delivery `stage`/`delivered_at`/geo fields and archives the delivery-proof row; confirm a non-acting non-Owner user never sees the link; (3) confirm the Follow-ups pending card shows 0/1/2 rows correctly depending on COD-reconcile and docs-returned state, that "Confirm received" writes `cod_reconciled`/`cod_received_at` and matches what Cash-up shows, that "Mark returned" still works exactly as the old standalone card did, and that the card disappears entirely once both resolve; (4) confirm a `kg`/weighed line on a delivered order shows plain text weight + photo thumbnails only — no input, no camera button, no "Add weighing", no delete — and that clicking a thumbnail still opens the image modal read-only.
- **Deliberately deferred, flagged not built (2026-08-13)**: the Figma spec's "ghost Undo (reconcileCOD only)" — a reversal control on the COD-reconcile row itself, letting a Finance/Admin/Owner user un-reconcile a mistaken "Confirm received" click. Skipped because, unlike the main Undo feature (which mirrors the `delivery_proofs.archived` pattern already established in this codebase), there is no existing reversal-write precedent for `cod_reconciled` anywhere in the app (including `useCashUp.ts`, which only ever sets it forward) — building one would be a genuinely new feature, not a reuse of an existing shape. If a real "undo reconcile" need comes up, it should be scoped as its own small unit rather than bolted onto this one.
- **Manual browser walkthrough still owed for the situational Modal titles rollout (2026-08-18)** — see the Completed entry below for the full file list; only verified by `tsc -b`/eslint/`npm run build`: spot-check a handful of the ~90 converted `alert()`/`confirm()` call sites across the 7 files (a destructive confirm like Settings' "Remove team member", a validation alert like OrderDetail's "Photo required", and a plain error alert) and confirm each modal now renders a bold title above the message matching the action that triggered it, styled per the supplied `.title` CSS (`font: var(--text-h2)`).
- **Manual browser walkthrough still owed for the sort-persists-on-back-nav + descending-by-default fix (2026-08-18)** — see the Completed entry below; only verified by `tsc -b`/eslint/`npm run build`: on both Orders and the Dashboard's Open Orders panel, confirm a fresh load shows Order ID sorted descending (newest first) with the icon on that column; click into an order from a non-default sort/stage/search combo, hit Back, and confirm the table re-renders with that exact same sort (not reset to default) — including on the Dashboard, whose URL now carries `?sort=...` the same way Orders already carries `?stage=...&search=...`.
- **Manual browser walkthrough still owed for the Admin-as-floor-helper weighing ACL fix + Cold Storage weighing-readiness gate (2026-08-18)** — see the Completed entry below; only verified by `tsc -b`/eslint/`npm run build` + live curl checks of the ACL change: log in as Admin, open a `cold`-stage order, enter a weight on a Loaf/kg/gram line and confirm it saves without the "You don't have permission to access this" error (previously reproducible every time); confirm the item-condition-photo camera also uploads successfully as Admin; confirm the "Release to Finance" button stays disabled until every weighed-unit line has a weight entered, then enables — for both Warehouse and Admin-as-helper — and that an order with no weighed-unit lines at all still lets the button enable immediately (nothing to block on).
- **Manual browser walkthrough still owed for the Order Edit cutting-instruction save fix + weigh-tolerance hint (2026-08-18)** — see the Completed entry below: as Admin, edit an order's cutting instruction text (add/change/remove one) and confirm it actually persists after Save + reload (previously silently discarded); confirm a genuine save failure now surfaces the page's error banner instead of navigating away as if it succeeded. Separately, on a `cold`-stage order, weigh a Loaf/kg/gram line to noticeably under or over its ordered quantity (past the Settings-configured tolerance %, 10% by default) and confirm the "Total:" line turns warning-amber with a "· below order X kg?" / "· over order X kg?" hint; confirm it stays the normal color within tolerance, and that changing the tolerance %s in Settings → Cold Storage actually shifts where the hint kicks in.
- **Manual browser walkthrough still owed for the New Order duplicate-order-number Use/Replace banner (2026-08-18)** — see the Completed entry below: on New Order, manually type in an order number that already exists for that delivery date and confirm the amber banner shows the existing order's customer name plus a working "Use #{next free}" button (fills the lowest open slot) and "Replace it" button (shows a blue "Will replace the existing #X" banner with its own Undo); confirm Create stays disabled while a duplicate is unresolved, then submit with Replace chosen and confirm in Directus that the *old* order is now `cancelled` with its `no` renamed off the original value (not deleted) and the new order holds the original number; confirm typing a different number, or changing the delivery date, clears a pending Replace choice.
- **Manual browser walkthrough still owed for AddItemModal's learn-and-reuse-matches wiring (2026-08-18)** — see the Completed entry below: on New Order (or Order Edit), open Add Item, type a free-text line whose product doesn't auto-match (or auto-matches wrong), manually pick the correct product from the dropdown, and confirm the item; save/create the order; check Settings → Intake Learning and confirm the "N learned matches" count went up by one with the right product name. Then open Add Item again on a *different* order and type the same (or near-same, different qty/casing) free text — confirm it now auto-matches with the green "✓ learned match" badge showing before you even touch the dropdown. Confirm manually overriding a still-shown learned match's product clears the badge (shouldn't say "learned" once hand-corrected) and that the *new* correction is what gets saved, not the stale one. **Also re-test the WhatsApp-paste path specifically** (the case that surfaced the real blocker below): paste a message, manually correct 1-2 unrecognized/wrong lines' products, create the order, confirm the learned-matches count actually increments now (it was silently stuck at 0 before the `product_id` schema fix — see Completed); paste the same message again and confirm those lines now come back "recognized"/"learned" instead of "probable".
- **Manual browser walkthrough still owed for the duplicate-notification StrictMode race fix (2026-08-18)** — see the Completed entry below: create an order (or trigger any `order_history`-writing action) in the dev server and confirm exactly one entry appears in the bell popover, not two; scroll to trigger `loadMore()` a few times and confirm no entries repeat. Only reproduces in `npm run dev` (React StrictMode's dev-only double-effect-invoke) — not expected to reproduce in a production build, but worth confirming dev no longer shows the duplicate either.
- **Manual browser walkthrough still owed for the Loaf weighing-requirement flip-flop fix (2026-08-18)** — see the Completed entry below: create a new order with a Loaf-unit line via New Order, advance it to Cold Storage, and confirm the weighing input **now shows up immediately** (previously it silently didn't, for any order created via New Order, and the "Release to Finance" button was trivially enabled with nothing weighed). Confirm a Loaf line edited through Order Edit still behaves identically before and after touching its unit dropdown — no more "the requirement appears/disappears depending on which page last saved this line."
- **Manual browser walkthrough still owed for the past-delivery-date hint on New Order (2026-08-18)** — see the Completed entry below: pick a delivery date before today and confirm the amber "Delivery date {D} is in the past — is this a typo?" banner appears; confirm it's purely a hint (Create stays clickable, not gated like `dupOrder`/`badNo`); confirm it disappears once the date is corrected to today or later.
- **Open question, needs external-service access this repo doesn't have (2026-08-19)**: re-pasting the same WhatsApp message after manually correcting 2 lines' products (and after the `product_id` schema fix above) still shows "probable", not "learned", on those lines. The correction write itself is now confirmed working (verified live). But WhatsApp-paste matching happens entirely inside the external `/order-api/parse-order` service — not in this repo — so this app can only confirm the *write* succeeded, not whether that service's own lookup finds it. Suspect a key-format mismatch: this app currently saves the correction keyed by the raw pasted line text (`l.rawText`, e.g. `"1 loaf A5 Kamenoko shabu"`), while the service's docstring says it "mirrors the server-side port of recognize.js" — whose real tokenizer (`corrKey()` in the prototype) normalizes qty/price/instructions away, alias-maps, and sorts tokens before keying (e.g. `"a5 kamenoko"`). If the service computes that same normalized key for its own lookup, a raw-text key would never match. Confirming (or fixing) this needs either access to that service's actual source, or a deliberate decision to port a matching tokenizer into this repo's write path as a best-effort alignment — flagging rather than guessing further.
- **Manual browser walkthrough still owed for the Production "Start cutting" feature (2026-08-19)** — see the Completed entry below: as Production (or Admin/Warehouse helping), open an order at the `production` stage with cutting instructions on it and confirm the new card renders — "Start cutting" button, flipping to a green "Cutting in progress · time · name" line once tapped; confirm each cut renders as its own tap-to-tick checkbox and the final "Cutting done → to packing" button stays disabled until every cut is ticked; confirm it actually advances to `packing` and persists every cut's `done` flag (re-open the order and confirm the ticks survived); confirm an order with zero cutting instructions shows "No cutting needed." and the advance button is immediately enabled.
- **Manual browser walkthrough still owed for the new `prompt()` modal + Send Back reason capture + full cut-reset on production transitions (2026-08-19)** — see the Completed entries below: trigger "Send Back" on an order (as Admin, Owner, or the current stage's owning role) and confirm a proper in-app modal appears — "Send back to {stage} — why?" with a text input, not a native browser prompt — confirm Cancel aborts with no write at all, and typing a reason + Confirm both sends the order back *and* appends the reason to its history entry. Confirm sending back from `dispatch`/`finalise`/`cold` (transitions that don't touch `production` at all) still works with no permission error. Separately, confirm sending back from `production` (leaving it) *and* sending back from `packing` to `production` (arriving at it) both: reset "Start cutting" to unstarted, AND un-tick every cut checkbox (both a locally-ticked-but-never-saved cut and one already persisted `done: true` from a completed run before this send-back) — per explicit follow-up request, this port deliberately un-ticks persisted cuts too on these transitions, unlike the prototype which leaves them ticked.
- **Manual browser walkthrough still owed for the generic self-undo + Packing card (2026-08-19)** — see the Completed entry below: advance an order through several normal stage transitions (intake→cold, cold→finance/production, production→packing via "Cutting done", packing→finalise via "Packed & ready", finalise→dispatch) and confirm a "Pressed wrongly? Undo — back to {stage}" link appears after *each* one, immediately reversing just that move (stage + whatever else it changed) back to exactly where it was; confirm it disappears the moment *anything else* happens to the order (another note, another role's action) — even if you didn't undo yet; confirm a *different* role than the one who made the move never sees it (except Owner, who always can); confirm it doesn't appear on a cancelled order. Confirm the Finance gate's own "Undo payment clearance" link is unaffected and still persistent (not time-limited the same way). Separately: at the `packing` stage, confirm the new "Pack the order" card lists cut vs. non-cut lines correctly and "Packed & ready" advances to `finalise`; confirm a line's cutting-instruction icon shows a checkmark instead of a knife once that specific cut is marked done; confirm the "Cut · tick each cutting" heading (not just the checklist) is hidden until "Start cutting" is tapped on an order that actually has cuts.

## Completed

- **Five `OrderDetail.tsx` behaviours aligned with the prototype's exact gates — a mix of missing gates, wrong-set gates, and a third hardcoded stage-label map found while fixing the other four (2026-08-21), all reported directly with prototype line references:**
  - **Sending box** now correctly `stage === "cold"`-only, on top of the existing kg/gram exclusion from the earlier control-matrix fix — was previously showing at every stage past cold too (a deliberate port-specific extension, built earlier this session for "X to follow" visibility beyond Cold Storage). Reverted to the prototype's own scope per this direct instruction; **flagging the tradeoff**: any owed-quantity visibility for loaf/counted lines past `cold` is gone now, same as it never existed in the prototype — worth a decision if that visibility is still wanted, just through a different mechanism (e.g. the item summary row) rather than this badge.
  - **Copy WA button** was unconditionally visible to every role — now gated on `auth.can("createOrders")` (Admin + Owner by default), matching the prototype's own `can(role, 'createOrders')` rather than a hardcoded role check, so an Owner Settings grant to another role also enables it.
  - **Documents section** had no gate at all (visible to Warehouse/Production/Courier too) — now gated on a new `seeDocuments` capability (Admin + Finance + Owner by default), matching the prototype's hardcoded `['Admin','Finance','Owner'].includes(role)`. Added to the `Capability` union, both roles' `ALLOW` blocks, `CAPABILITIES`, and the Owner Settings `PERMISSION_GRID` (Orders section) — consistent with how every other gate in this app is modeled, not a one-off hardcoded check.
  - **Documents add-form** was gated on `printDocuments` (Admin/Owner only — the same capability that gates Finalise's "Print DO/SI" stage-advance action), which silently excluded Finance from adding documents even though nothing in the UI suggested it was Admin/Owner-only. Switched to the same new `seeDocuments` capability as the section itself, so see-and-add are now exactly the same role set — Finance can add documents, matching the prototype's add-form sitting inside the same Admin/Finance/Owner block with no extra gate of its own. `printDocuments` itself is untouched — Finalise's action stays Admin/Owner-only, a deliberately different, narrower set (Finance doesn't release orders to dispatch).
  - **Stage labels — found a THIRD hardcoded duplicate while fixing the others**: `pipeline.ts`'s `STAGE_LABELS`/`PIPELINE_STAGES` already had the exact prototype strings ("New Orders", "Cold Storage Picking", "Finance Review", "Processing", "Print DO/SI", etc.) — nothing needed changing at the root map itself. But two consumers had their own separate, wrong, hardcoded copies: `OrderDetail.tsx`'s local `PIPELINE_STAGES` const (driving its stepper — "New Order"/"Cold Storage"/"Finalise" instead of the correct strings) and `StatusPill.tsx`'s local `STATUS_LABELS` map (driving every stage pill across the whole app — same wrong values, plus its own slightly-different return-bucket label wording). Deleted both; both now import and read `STAGE_LABELS`/`PIPELINE_STAGES` from `lib/pipeline.ts` directly. Confirmed (grep, not assumed) `Orders.tsx` and `Dashboard.tsx` already correctly imported and used the shared `PIPELINE_STAGES` — no third or fourth duplicate found there.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. No live ACL change needed — `seeDocuments` is a client-side capability only; every role that needs to read/write `attachments` already has unrestricted Directus access to that collection. Not yet browser-tested: cold order shows Sending box on loaf/counted, absent on kg/gram and at every other stage; Copy WA visible only to Admin/Owner; Documents section+add-form visible to Admin/Finance/Owner, hidden from Warehouse/Production/Courier; every stage pill/stepper/filter/dashboard tile reads the corrected label text app-wide.

- **Re-seeded the QA DB against a revised `all-scenarios-db.md` that added an authoritative per-line data table with corrected customer assignments, real prices, and exact cuts (2026-08-21):** the doc changed since the first seed — a new "COMPLETE PER-LINE DATA" section (with a self-noted correction, "En Dining? no—Saffron") reassigned the customer on orders 1/2/3/4/6, swapped order 5's and 22's product from Wagyu Ribeye to "US Choice Ribeye IBP", and specified exact per-line prices/cuts that hadn't been given before (orders 2/3/4 explicitly stay unpriced — `NULL`, not `0`, per the doc's own warning about that exact mistake). Rather than patch individual fields and risk missing one of the many cross-cutting changes, wiped every order-dependent collection (`orders` → `order_lines` → `line_weighings`/`line_cuts` → `delivery_proofs`/`attachments`/`order_history`, `line_weighing_photos`/`line_photos`/`line_return_photos` also checked, already empty) and reseeded all 28 orders fresh from the corrected table. Customers were untouched — the roster didn't change between doc versions, only which order references which customer.
  - **Product substitution, flagged**: "US Choice Ribeye IBP" doesn't exist verbatim in the live catalog — the closest match is `us-choice-ribeye-ibp-cut-236` ("US Choice Ribeye Cut", its id literally contains "ibp") with no non-"Cut" sibling. Used it for orders 5 and 22; worth confirming with the user whether a plain (non-cut) IBP ribeye SKU should exist in the catalog.
  - **This run completed in one pass** with no FK errors, unlike the first seed — confirms the 9-field `special: ["uuid"]` fix from the previous entry actually holds.
  - **Scenario 27's undo-fix validation row** needed the same one-line correction as the first seed (`undo_snapshot.at` millisecond format re-aligned to match the history row's stored value) — same root cause as before (the seed script's own microsecond-stripped timestamps), not a recurrence of anything new.
  - Final counts: 19 customers (reused), 28 orders, 33 order_lines, 4 line_weighings, 9 line_cuts (up from 4 — the new table specifies far more cuts per order, including order 8's 2-cut split), 6 delivery_proofs, 2 attachments, 46 order_history rows.
  - Not yet compared against the prototype side-by-side — still the next manual step.

- **Seeded the live Directus DB with all 25 prototype-fixture scenarios + 3 app-specific edge cases (`context/qa/all-scenarios-db.md`), and found + fixed a real, previously-undiscovered live schema bug along the way (2026-08-21):** executed the QA doc's seed plan end to end — 19 customers, 28 orders (order numbers `260821001`–`260821028`) covering every stage/sub-state/return-bucket the doc lists, each with its `order_lines`/`line_weighings`/`line_cuts`/`delivery_proofs`/`attachments`/`order_history` rows.
  - **STEP 0**: confirmed the DB was already effectively empty (0 rows on every relevant collection except 5 leftover `messages` rows, which were deleted) before seeding — the file's claim "the user has manually cleared data" checked out live, not just taken on faith. **Not done, flagged rather than silently skipped**: the doc's sequence-restart step (`ALTER SEQUENCE ... RESTART WITH 1` for `order_history`/`attachments`/`messages`/`connection_events`) needs direct Postgres access — this repo only has the Directus REST API token (confirmed via `.env`), which has no raw-SQL or sequence-reset endpoint. The 4 integer-id collections will simply continue their existing auto-increment counters rather than restarting at 1 — cosmetic only, doesn't affect any FK or app behavior, but flagging since the doc explicitly asked for it and it's not achievable with what's available here.
  - **Real bug found while seeding, fixed live**: `POST /items/delivery_proofs` (and, once the pattern was suspected, 8 other FK fields across `attachments`, `draft_weighings`, `line_cuts`, `line_photos`, `line_return_photos`, `messages`) failed with `"Invalid foreign key for field ... INVALID_FOREIGN_KEY"` — even when that field was omitted entirely or explicitly set to `null`. Root cause: `cond_photo`/`recv_photo`/`signed_photo` (and the 8 others found by scanning every field in the schema for `foreign_key_table` + `special: ["uuid"]`) were misconfigured with the Directus "auto-generate a UUID" special flag — normally meant for primary-key-shaped fields — on what are actually plain nullable foreign keys to `directus_files`/`orders`/`order_lines`. Directus was generating a random UUID for these fields on every create (even overriding an explicit `null`) and then rejecting it as a nonexistent FK target. Fixed live via `PATCH /fields/:collection/:field` (`meta.special: null`) on all 9 affected fields; re-scanned the full schema afterward and confirmed zero remain.
    - **Confirmed real, live impact beyond the seed script**: `attachments.document_file` breaks the app's own "Add Document" form (`OrderDetail.tsx`'s Documents card) every time a document is logged with no file attached (the field is optional there) — this was silently broken in production, not just a seeding artifact. `messages.document_file` has the same shape for a plain-text WhatsApp message with no attachment. The other 7 fields (`order_id`/`order_uuid`/`line_id` FKs, always populated by the app in practice) were latent but not yet hit by real usage.
  - **Learned, not a bug**: seeding scenario 27 (the self-undo timestamp-fix validation row) initially showed a mismatched `undo_snapshot.at` vs. `order_history.at` — traced to the seed script itself stripping microseconds from its own generated timestamps (`datetime.now().replace(microsecond=0)`), while Postgres's `timestamp` column always reformats a read-back value to include `.000` milliseconds and the raw JSON `undo_snapshot` field preserves whatever string was written verbatim. The real app never hits this — `new Date().toISOString()` in JS always includes millisecond precision, so both writes in a single request are byte-identical from the start. Fixed by overwriting scenario 27's `undo_snapshot.at` to match the history row's stored (millisecond-included) format exactly, so it now correctly validates the fix.
  - Not yet compared against the prototype side-by-side (that's the next manual step the QA doc describes) — this entry covers the seeding + the schema-bug fix, not the actual scenario-by-scenario diff.

- **"Isn't weighed yet" safety net for a line added after Cold Storage — a real, previously-missing feature, requested directly with the prototype's exact logic spelled out (2026-08-21):** the port's edit lock doesn't engage until dispatch (`isOrderLocked`), so an order sitting at finance/production/packing/finalise is still editable — someone could add a kg/gram/loaf line that then ships and gets invoiced with no recorded weight, unbillable for a catch-weight meat business. The prototype has always had a banner + fix action for exactly this (`Dev-OrderDetail.jsx:137-146,314,1314-1321`); this port never ported it.
  - **New `unweighedAdded`/`needsWeighing`/`canWeighFix`** (`OrderDetail.tsx`, next to `canEdit`): a kg/gram/loaf line with no recorded weight (checked against `weighingsMap`, the actual `line_weighings` totals — `order_lines.weight` itself is never written anywhere in this port, confirmed by grep, so checking it directly the way the prototype checks `l.weight` would have been checking a field that's always empty) and, for kg/gram, not flagged `short` (a short line is legitimately weightless). Gated to `pastCold && !isOrderLocked(order)` — reused the existing `isOrderLocked` instead of porting a separate `hasLeftWarehouse` helper, since within `pastCold`'s own stage set (finance/production/packing/finalise/dispatch) the two are equivalent — `isOrderLocked`'s extra terminal-stage cases (outstanding/cancelled/returned/delivered) never overlap with `pastCold` anyway.
  - **New warning-toned banner** (new `.needsWeighingBanner`/`.needsWeighingRow`/`.needsWeighingText` CSS, ported layout — not copy — from the prototype's own warning card), naming the unweighed line(s) and, for `canWeighFix` (Admin/Owner/Warehouse, or anyone else granted `sendBackStage` — the prototype's own hardcoded set, not the Owner-configurable grant alone), a "Send to Cold Storage to weigh" button. Both the banner text and button label were already sitting translated, unused, in `translations.ts`.
  - **New `orders.reweigh_from` field** (live, nullable text — genuinely new this time, not dormant scaffolding, confirmed via `/fields/orders` there was nothing "reweigh"-shaped already there) — remembers which stage to return to once the new item is weighed. New `handleSendToColdToWeigh()`: sends the order to `cold`, sets `reweigh_from` (preserving an existing value if already mid-loop), and resets `cutting_started`/`_at`/`_by` so a later re-entry to Production isn't stuck showing stale "in progress" state — ported from the prototype's `sendToColdToWeigh()`/`CUT_RESET`.
  - **Built the full re-weigh detour, not just the banner** (the request allowed shipping the banner alone as a fallback, but the pieces were all available, so did the complete version): `handleAdvance`'s cold-release target now checks `order.reweigh_from` — routes straight back to it and clears the flag, UNLESS something in the order still needs cutting and the origin was past Production (`packing`/`finalise`/`dispatch`), in which case it routes through `production` first so the new item's own cut isn't skipped. `handleCuttingDoneAdvance` mirrors this on the Production side: if `reweigh_from` is still set when cutting finishes, it returns straight there (skipping Packing) instead of the normal forward path, and clears the flag. Both ported directly from the prototype's own release-target logic (`Dev-OrderDetail.jsx:712-719,760-764`).
  - **Live ACL**: Warehouse's and Production's `orders.read`/`orders.update` grants (both field-restricted) extended to include `reweigh_from` — needed for both roles to actually read/clear it during the detour, not just Admin's already-unrestricted access. `context/schema/snapshot.json` re-exported to capture the new field; `roles-and-permissions` updated for both roles.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live schema/ACL changes verified via curl. Not yet browser-tested: edit a production/packing/finalise order, add a kg line, confirm the banner names it and the button works; add a counted line instead and confirm no banner; flag an added kg line short and confirm no banner; confirm a dispatch-taken or delivered order shows no banner regardless; click through the full loop (send to cold → weigh → release) and confirm it returns to origin without re-running stages already passed, and that a still-needed cut routes through Production first.

- **The generic "Pressed wrongly?" self-undo never actually appeared for ANY role at ANY stage — a real, previously-undiscovered bug in the visibility gate itself, reported directly with the exact root cause already diagnosed (2026-08-20):** `canUndo`'s gate requires `lastHistoryEntry.at === order.undo_snapshot.at` — but the two `at` values were always generated independently: `snapshotFor()` stamped its own `new Date().toISOString()` when building the snapshot, while the paired `appendOrderHistory()` call passed no `at` at all, letting Directus default the history row's timestamp to its own separate `now()` moment. Two different clocks, two different instants — the strings could never match, so `canUndo` was always `false`, for every role including Owner (the failing branch is the timestamp-equality check, which no role bypasses). This had been silently broken since the generic self-undo mechanism first shipped (2026-08-19) — every "Not yet browser-tested" walkthrough note left on that work and its several follow-ups was, in hindsight, never going to find a working button no matter who tested it or which stage they tried.
  - **Fixed by sharing one `at` between the two writes, matching the prototype's own `advance()`** (`Dev-OrderDetail.jsx:194-216`, which writes the snapshot and the history entry as one object with one timestamp): `snapshotFor(patch, at)` now takes the timestamp as a parameter instead of generating its own. Every one of its 9 call sites (`handleAdvance`, `handleCuttingDoneAdvance`, `handlePackAdvance`, `handleFinaliseAdvance`, `handleSendBack`, `handleHold`, `handleRestore`, the return-refusal handler, `handleConfirmSettle`'s replacement branch) now generates a single `const actionAt = new Date().toISOString()` up front and passes it to both `snapshotFor(patch, actionAt)` and the paired `appendOrderHistory({ ..., at: actionAt })`.
  - **Found a 10th, structurally-identical instance while auditing**: `handleConfirmDelivery` builds its own `UndoSnapshot` object by hand (it carries an extra `proofId` field `snapshotFor` doesn't produce) rather than calling the shared helper — it had the exact same independent-timestamp bug, just not caught by grepping for `snapshotFor(`. Fixed the same way (one shared `actionAt`).
  - **`CreateOrderHistoryInput`/`appendOrderHistory`** (`directus.ts`) gained an optional `at` field — `order_history.at` was already confirmed live-writable (a plain timestamp column with a `now()` DB default, not a Directus-managed `date-created` special field, `readonly: false`) and every role's `order_history.create` grant is already `fields: ["*"]`, so this needed no live ACL change, purely a type/client addition.
  - **Confirmed `handleUndo` itself needs no equivalent fix** — it writes `undo_snapshot: null` (consuming the snapshot, not creating a new one), so its own `appendOrderHistory` call correctly has nothing to pair against.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested — but this time with actual confidence the mechanism is reachable at all: advance an order through any stage transition (as the actor, and separately as Owner) and confirm "Pressed wrongly? Undo — back to {stage}" now renders; confirm it disappears after any other action; confirm every stage transition (intake→cold, cold→finance/production, production→packing, packing→finalise, finalise→dispatch, dispatch→delivered, send-back, hold, restore, return-refusal, replacement-settle) gets its own working undo.

- **Price-presence detection fixed to treat `0` as "no price," and empty per-line summary rows removed — a same-day follow-up to the order-level-summary fix below, reported directly with exact line references (2026-08-20):** the port's price-presence checks all tested `price != null && price !== ""`, which reads a `0` price as "present" — producing `Rp 0` chips and a falsely-"priced" order the prototype never shows (it uses a truthy/`> 0` test throughout, `Dev-OrderDetail.jsx`/`Dev-domain.js`). Also found a duplicate of the flag added in the previous fix: that one (`orderPriced`, new) and a separate pre-existing one (`orderIsPriced`, already driving the Finance-gate's suggested payment amount 3 call sites away) had drifted apart — the pre-existing one still used `.every()` with the `!= null` bug, silently mis-classifying any mixed order (some lines priced, one not) as "unpriced" even when most lines had real prices.
  - **New shared helpers** `linePriceValue`/`lineHasPrice` (module-level, next to `isWeightOnlyUnit`) — `lineHasPrice` is the one true presence test (`> 0`) used everywhere now; existing `parseFloat(...) || 0`-style helpers that compute totals (not presence) were left untouched, per explicit instruction.
  - **Consolidated to one `orderIsPriced`** (removed the just-added duplicate `orderPriced`, fixed the pre-existing one to `lines.some(lineHasPrice)` instead of `.every()` + `!= null`) — this one flag now correctly drives both the Finance-gate's suggested-amount fallback (`handleApproveFinance`, the gate card's amount hint) and the item list's order-level price summary, so a mixed order's Finance suggestion and item-list total now agree with each other instead of disagreeing.
  - **Per-line price chip** (`hasPrice`) now uses `lineHasPrice` — a `0` or missing price renders no chip at all, never "Rp 0".
  - **New `showLineDetail`** gate on the whole per-line "Item Summary" row (adapted from the prototype's `Dev-OrderDetail.jsx:1425` line-detail gate to this port's actual per-line attributes — weight total, short flag, price; this port has no per-line "delivered" chip to include, unlike the prototype) — a plain counted line past `intake` with no price and nothing short previously rendered a bare "Total:" with nothing after it; now renders nothing at all when there's genuinely nothing to show.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested: (a) a `price: 0` or unpriced line shows no chip, not "Rp 0"; (b) a bare unpriced/unweighed line shows no detail row at all; (c) all-priced order → one total; (d) all-unpriced/all-zero order → one "No price" line, no "Rp 0" total; (e) **mixed order (the case `.every()` broke)** → shows the real total, not "No price"; (f) Courier (no `seePrices`) → no chips, no summary row at all.

- **"No price on the order — invoiced in Accurate" moved from a per-line message to a single order-level summary row, matching the prototype exactly (2026-08-20), reported directly with exact line references:** the port computed `hasPrice` per line (`line.price != null`) and rendered the "No price" text inside the item loop itself — on a mixed order (some lines priced, some not) this repeated once per unpriced line, and a fully-unpriced order showed both the repeated "No price" text AND a separate "Order value · from PO Rp 0" total underneath, which is actively misleading (a real zero-value order and an unpriced-in-Accurate order look identical). The prototype (`Dev-OrderDetail.jsx:1451`) never had this — it computes one order-level `orderPriced` flag and renders a single summary row after the whole item list.
  - New `orderPriced = lines.some(l => Number(l.price) > 0)`, next to the existing `orderTotal` reduce.
  - Removed the per-line "No price" branch from inside the item loop — a line's own price chip (`hasPrice ? <priceCalc> : nothing`) now renders nothing at all when that line has no price, instead of a repeated explanatory sentence. The per-line priced chip itself is untouched.
  - The order-level total row (previously just `canSeePrices && <total>`) is now a 3-way per the exact spec: `canSeePrices && orderPriced` → the total row; `canSeePrices && !orderPriced` → the single "No price on the order" row instead; `!canSeePrices` → nothing, same as before.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested: (a) an all-priced order shows one total; (b) an all-unpriced order shows one "No price on the order" line and no "Rp 0" total; (c) a mixed order shows the real total, not repeated "No price" lines; (d) a Courier session (no `seePrices`) sees neither message nor any price row.

- **Courier's "My deliveries" 403'd outright ("Failed to load deliveries: You don't have permission to access this") — same field-restricted-ACL-request bug class as the 2026-08-11 Orders-page fix, just never applied to this hook (2026-08-20), reported directly:** `useDeliveries.ts`'s `readOrderLines()` call names `price` in its explicit `fields` array (needed for each COD stop's collection amount, `qty × price`) — Courier's live `order_lines.read` grant is field-restricted and didn't include `price`, and Directus rejects the *whole* request when a field-restricted role's query names a disallowed field, not just that one field. The 2026-08-11 fix for this exact bug class in `useOrders.ts`/`useOpenOrders.ts` made `price` conditional on `auth.can('seePrices')` — **not applicable here**: unlike those two pages (where price is a pure business-sensitivity display choice), a courier genuinely needs the COD amount to know how much cash to collect, and `Deliveries.tsx` has always rendered it unconditionally for COD stops, so hiding the field client-side would have broken the feature rather than just a number. Fixed by granting `order_lines.price` read live to Courier instead of gating it away — the app's `seePrices` **capability** stays `false` for Courier, so every other price display in the app (`OrderDetail.tsx`, Orders/Products pages) is unaffected; only `Deliveries.tsx`'s aggregate COD total (and the code as-is needed no changes at all — the client was always correctly requesting the field it needed, only the live ACL was missing it).
  - **Found the identical latent bug while in this code, not yet fixed**: `useTodayDigest.ts`'s "COD collected today" Dashboard metric has the exact same unconditional `price` request, feeding a `readOrderLines` call whose errors are silently swallowed (no `error` branch) — for Courier this is now fixed as a side effect of the same live grant, but Warehouse and Production also lack `order_lines.price`/`seePrices` and would still silently under-report this one digest number (stuck at 0) rather than error visibly. Not fixed — unlike Courier's clear-cut "needs to know how much cash to collect" justification, there's no equally obvious functional case for giving Warehouse/Production line-item price visibility just for one dashboard metric; flagged in `roles-and-permissions` for a decision rather than assumed.
  - `context/schema/roles-and-permissions` updated (Courier's field-level notes) with the full reasoning above. `context/schema/snapshot.json` untouched — it only tracks collection/field schema, not ACL rules. No app code changes — this was purely a live permissions fix; the client was already correct.
  - No `tsc -b`/`eslint`/`build` needed (no code changed). Not yet browser-tested: as Courier, open "My deliveries" and confirm it loads without error, and that a COD stop shows the correct amount to collect.

- **Finalise stage got its own "Print DO/SI" card, replacing the bare generic "Ready — Send to Dispatch" button — content ported from the prototype's `finalise` case, not its layout (2026-08-20), requested directly with a screenshot of the prototype's Admin view:** ported from `Dev-OrderDetail.jsx:784-798` — one optional "DO / SI number" input plus a single primary button (`Printer` icon, "Delivery Order (Surat Jalan) or Sales Invoice (Faktur Penjualan) Printed") that both logs the document (only if a number was typed) and releases to dispatch in one action, matching the prototype's combined confirm-and-release semantics exactly. Both the button label and the input's placeholder were already sitting translated, unused, in `translations.ts` — same dormant-scaffolding pattern as several other features this session.
  - New `handleFinaliseAdvance()`: reuses the existing `createAttachment` call the general Documents card already uses (`doc_type: "DO/SI"`, `note` set to `"replacement delivery"`/`"original delivery"` depending on `order.is_replacement`, matching the prototype's own note text) so a printed DO/SI shows up in the Documents list like any other logged document, then writes `{ stage: "dispatch" }` + a fresh `undo_snapshot` (this port's generic self-undo — the prototype doesn't have an equivalent per-action undo here, but every other stage transition in this port already gets one, so finalise's own advance keeps that consistency) and an `order_history` entry (`"DO/SI printed (N) — released to dispatch"` or, with no number, `"Document printed — released to dispatch"`).
  - New card rendered for `stage === "finalise" && canAdvance` (now Admin/Owner-only per the fix earlier the same day), replacing the generic advance button for this one stage the same way Cold/Production/Packing/Dispatch already replace it for theirs. Styled with this port's own `Card`/`Button` components and `.editInput`/`.cardActions` classes (already used elsewhere in this file) — not the prototype's raw `<input>`/`<button>`, per explicit "content not layout" instruction.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested: as Admin, open a `finalise`-stage order, type a DO/SI number, tap the print button, and confirm it both appears in the Documents list and advances the order to `dispatch`; confirm leaving the number blank still advances with a plain "Document printed" history entry and no document logged.

- **"Print DO/SI → Send to Dispatch" (the `finalise` stage) was actionable by Warehouse and Courier, not just Admin/Owner (2026-08-20), reported directly with a screenshot of the prototype's Admin view for reference (content, not layout):** root cause was `STAGE_FLOW.finalise`'s capability, `advanceStage`, being granted by default to Warehouse, Production, *and* Courier in `domain.ts` — traced every other stage first and found `advanceStage` is actually only ever consumed by two `STAGE_FLOW` entries, `intake` and `finalise` (every other stage has its own dedicated capability: `weighColdStorage`/`cutProduction`/`packWarehouse`/`dispatch`/`approveFinance`), and both of those are `Admin`-only in the prototype's `ACTOR` map with no floor-helper carve-out at all (`Dev-OrderDetail.jsx:123`'s `helpOtherStages` list is `['cold','production','packing','dispatch']` — `finalise`/`intake` are never in it, same treatment as `finance`, fixed the day before). So `advanceStage: true` on Warehouse/Production/Courier was live, granted, and wrong from the start — not a recent regression.
  - Removed `advanceStage: true` from Warehouse's, Production's, and Courier's `ALLOW` blocks in `domain.ts` — kept only on Admin (Owner is unaffected either way, always bypasses via `can()`'s own short-circuit).
  - **`canAdvance`'s `helpOtherStages` fallback** (`OrderDetail.tsx`) tightened from a single `stage !== "finance"` exclusion to a proper allowlist (`['cold','production','packing','dispatch']`), matching the prototype's `canAct` exactly — `finalise` and `intake` now also excluded from floor-helper coverage, not just `finance`. Doesn't change today's default behavior beyond the `advanceStage` fix above (Admin is the only role with `helpOtherStages` by default, and Admin already passes via its own `advanceStage`/`printDocuments` grants regardless) — this is defense-in-depth for if the Owner ever grants `helpOtherStages` to another role via Settings.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested: as Warehouse or Courier, open an order at `finalise` and confirm the "Ready — Send to Dispatch" button is gone, replaced by the "This order is currently with Admin" actor notice; confirm Admin/Owner still see and can use the button normally.

- **Dashboard's "Replacement in Transit" return-workflow tile was flat danger-red for every role — reverted to a per-viewer role colour instead (2026-08-20), reported directly with a screenshot:** a previous pass (documented in this same file, "QA feedback batch") had special-cased this one tile to `var(--state-error)` whenever highlighted, specifically because `replacement_transit` has no single fixed owning role (`RETURN_BUCKET_ACTOR` deliberately excludes it — a replacement order could be sitting with Warehouse, Production, Finance, or Courier) so `statusColor()` alone can't colour it. In practice this made a routine in-transit replacement read as an urgent/error state for whichever role's dashboard it showed up on. Per direct follow-up ("make it neutral... just like awaiting return", then corrected to "respective role's colour instead" once the neutral version was seen) — the tile now colours itself by **whoever's currently viewing it** (the signed-in role), not one fixed bucket-owner: `ReturnWorkflowsPanel` gained a `currentRole` prop (wired from `Dashboard.tsx`'s existing `useRole()`), and when the highlighted tile is `replacement_transit`, it looks up `ROLE_COLOR[currentRole]` directly instead of going through `statusColor()`. Every other bucket tile is untouched, still coloured via `statusColor()` by its one fixed owning role exactly as before.
  - Confirmed this can't leak into a replacement order's own `StatusPill` elsewhere — same isolation already verified when the danger-red version was built: this per-viewer colouring is local to this one dashboard tile's `color` variable, never routed through the shared `statusColor()` function's own return value for this key.
  - `tsc -b` ✓, `eslint` ✓, `npm run build` ✓. Not yet browser-tested: with a replacement order in transit, confirm the tile now shows in each role's own accent colour (e.g. Warehouse's colour when logged in as Warehouse, Production's when logged in as Production) rather than red for everyone.

- **Item photos were deletable at every stage past Cold Storage (production/packing/finalise/dispatch/outstanding, not just delivered) — gated on the wrong condition (2026-08-20), reported directly:** the delete affordance (both the thumbnail's hover-trash icon and the enlarged `ImageDetailsModal`'s own delete button, which is driven by whether `photoId`/`lineId` got attached to `activeImageModal` on click) was gated on `stage !== "delivered"` — the ONLY stage it was actually blocked at. Every other stage let anyone viewing the order (any role, not just Warehouse) delete an item photo, long after Cold Storage finished. The upload button right next to it was already correctly gated on `canWeighHere` (Cold Storage + a weighing-capable role) — delete just never got the matching treatment. Fixed by switching both the hover-trash icon's render condition and the `activeImageModal` click handler's ternary from `stage !== "delivered"` to `canWeighHere`, so upload and delete now share the exact same window — closes it for every role, not only Warehouse. Viewing/enlarging a photo (click-to-zoom) is unaffected at any stage; only the delete option disappears once the order leaves `cold`.
  - **Weighing-line photos confirmed already correctly scoped, no change needed**: that whole section (weight inputs, "Add weighing", and its own photo thumbnails + delete) is nested inside `{isWeighedItem && canWeighHere && (...)}` — it was never reachable outside Cold Storage in the first place, so there was nothing to fix there.
  - **Not addressed, flagged rather than silently assumed out of scope**: this is a client-side (UI) gate only. Warehouse's live Directus ACL on `line_photos`/`line_weighing_photos` (full CRUD, `fields: ["*"]`) has no awareness of the related order's `stage` — a request made directly against the API (bypassing the UI) could still delete a photo at any stage. Every authorization check in this app to date has been this same shape (a client-side capability/stage gate backed by a broad, non-stage-conditioned Directus role ACL — `canAdvance`, `canEdit`, etc. all work the same way), so this isn't a new gap relative to the rest of the app, but it's worth naming explicitly: closing it for real would mean a stage-conditioned Directus permission filter (e.g. `line_id.line_weighings_id.line_id.order_id.stage._eq: cold`), which is a different, riskier kind of change (relational permission filters, not a field allow-list) than anything done elsewhere this session — not attempted without being asked.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested: as Warehouse, upload an item photo at Cold Storage, advance the order to Production, and confirm the delete (hover-trash icon and the modal's delete button) is gone — only the plain thumbnail remains, still clickable to view.

- **Directus admin UI showed "The relationship hasn't been configured correctly" on `orders.cutting_started_by` (2026-08-20), reported directly with a screenshot:** confirmed via the Directus `/fields` and `/relations` APIs — the field's meta already declared it as an M2O relation (`special: ["m2o"]`, `interface: select-dropdown-m2o`, added alongside the 2026-08-19 Start Cutting feature), but no actual `directus_relations` row or database-level foreign key was ever created for it — `schema.foreign_key_table` was `null`. `orders.taken_by` (the closest equivalent — also "who did the action", also → `directus_users`) has a real FK constraint (`orders_taken_by_foreign`); `cutting_started_by` never got the same treatment. Verified the 3 existing non-null values (`Disty`, `Disty`, `Nando`) are all valid `directus_users` rows before adding the constraint, so it wouldn't fail. Fixed live: `POST /relations` to create `orders.cutting_started_by → directus_users.id` (`ON DELETE SET NULL`, matching `taken_by`'s own delete behavior) — this both adds the real FK and satisfies the meta's relation declaration, resolving the admin-UI error. Frontend app code was never affected by this (it reads/writes the raw UUID directly, doesn't rely on Directus's relation resolution) — this was purely an admin-panel/schema-integrity fix.
  - `context/schema/snapshot.json` re-exported live from `/schema/snapshot` to capture this relation plus everything else changed live this session (the `line_weighing_photos` permission rows, `order_lines.sent`/`short`, etc.) — per this repo's own convention ("schema changes happen in Directus first, then the snapshot is re-exported").

- **Cold-stage per-unit control matrix fixed to match the prototype exactly, plus the missing `line_weighing_photos` ACL that blocked every weighing-photo upload (2026-08-20), both reported directly:**
  - **"Failed to save photo: You don't have permission to access this" on every weighing-photo upload**: root-caused via curl — `line_weighing_photos` (the junction collection linking a `line_weighings` row to an uploaded photo) had **no permission rows at all** for Warehouse, Production, or Finance; only Admin had any grant. `directus_files` (the underlying upload) was already fully open for Warehouse, so the upload itself always succeeded — only the link-the-photo-to-the-weighing step 403'd. Its sibling collections `line_weighings`/`line_photos` both already had the correct role grants (Warehouse full CRUD, Production read) — `line_weighing_photos` was simply never given the same treatment when it was added. Fixed live: Warehouse full CRUD, Production read, Finance read (matching `line_photos`'s own Finance-read grant from the 2026-08-19 QA batch). While in there, also found and corrected a **stale doc entry**: `roles-and-permissions` still listed Finance as "No access" to `line_photos`, even though that grant was actually added the day before — the doc was never updated to match. Both `line_photos` and the new `line_weighing_photos` rows are now correct in the doc for every role.
  - **Sending box was showing on kg/gram lines** — the prototype never renders a Sending box for weight-unit (kg/gram) lines at all (`counted = !isWeightUnit`, `Dev-OrderDetail.jsx:1364`); this port's `sendingBadge` had no unit gate, so it rendered on every line regardless of unit. Fixed: wrapped the badge in `!isWeightOnlyUnit(line.unit)` — kg/gram lines now show only their weight inputs + Short button (no Sending box); Loaf and counted lines keep the Sending box as before. Loaf is the deliberate hybrid here (weighed for catch-weight, but "held" via Sending = 0 like a counted unit, not via `short`) — confirmed still correct, matching the prototype's own `isWeighed` (weight inputs) vs. `isWeightUnit` (Sending/short split) predicates exactly.
  - **`handleToggleShort` simplified**: no longer writes `order_lines.sent` alongside `short` — since kg/gram lines have no Sending box to reflect it in anymore, forcing `sent` on a short kg line was writing an inert value nothing displays. Now writes only `{ short: next }`, matching the prototype's own `newLines` mapping exactly (`nl.short` for weight-unit lines, `nl.sent` only ever set for non-weight-unit lines — never both on the same line). The persistent "Short — ran out of stock" chip (Item Summary row, unaffected by this change) remains the "customer still owed this" signal for kg/gram lines.
  - Confirmed already correct, no change needed: the Short toggle button and its persistent chip were already `isWeightOnlyUnit`-gated (kg/gram only); the weight-input/"Add weighing" section was already `isWeighedUnit`-gated (kg/gram/loaf, excluding counted units); `coldWeighingReady`'s two `isLineHeld` branches (short flag for kg/gram, Sending = 0 for everything else) were already both in place from the previous fix that same day.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live ACL change verified via curl. Not yet browser-tested: as Warehouse, upload a weighing photo and confirm it saves; load a cold order with one line of each unit type (kg, Loaf, counted) and confirm the control matrix — kg gets weight inputs + Short button only, Loaf gets weight inputs + Sending box only, counted gets Sending box only.

- **Order Edit silently showed "— Custom Product —" for a real, existing product ("Aus Wagyu Cube Roll 4-5") — a Directus default-`limit`-of-100 truncation, not a matching-logic bug (2026-08-20):** reported directly with the exact order (`9c5cc497-f54f-4c34-9da9-529c01ba250f`) and a screenshot. Confirmed via curl: the line's `product_id` (`wagyu-cube-roll-4-5-ruby-38`) is valid and exists live — the product just wasn't in the 239-row `products` table's first 100 rows in default order, which is all `OrderEdit.tsx`'s `readProducts()` call (line ~147) was fetching, since it passed no query at all — Directus's SDK silently caps an unspecified `limit` at 100. The `<select>`'s `value` then didn't match any rendered `<option>`, so it fell back to the first one ("— Custom Product —"). `OrderNew.tsx`'s own `readProducts()` call already correctly passes `limit: -1` — `OrderEdit.tsx` was the outlier that never got that treatment.
  - Fixed `OrderEdit.tsx`'s `readProducts()` → `readProducts({ limit: -1, sort: ["name"] })`.
  - **Proactively swept for the same bug class** rather than waiting for it to resurface elsewhere (same root cause: `readCustomers`/`readProducts` pass their query straight through to Directus with no baked-in limit, unlike `readAllUsers`/`readTeamMembers`/`readCorrections`, which already hardcode `limit: -1` inside their own `directus.ts` wrappers) — grepped every bare `readProducts()`/`readCustomers()` call site in `src/`. Found and fixed two more: `OrderEdit.tsx`'s own `readCustomers()` (same call, same fix) and `OrderDetail.tsx`'s `readCustomers()` (~line 530, used to resolve `order.customer_id` for display). Neither had visibly misbehaved yet — the live `customers` table only has 17 rows, well under the 100-row cliff — but both were the same latent landmine, waiting for the row count to cross 100. Every other `readProducts`/`readCustomers` call site in the app was confirmed already safe: either a bounded `_eq`/`_in` filter (1 or a handful of rows), explicit pagination (`limit: PAGE_SIZE`), or already carrying its own `limit: -1`.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested: open Order Edit on order `9c5cc497-f54f-4c34-9da9-529c01ba250f` and confirm the Loaf line now shows "Aus Wagyu Cube Roll 4-5" selected, not "— Custom Product —".

- **Cold Storage "Short — ran out of stock" flag, ported from the prototype's `held()`/`shortFlag` (2026-08-19), plus a "Pull & weigh" Card rebuild of the cold-stage action panel:** requested with 5 explicit sub-steps; found the schema/ACL half already fully live and dormant (same pattern as several other features this week) — `order_lines.short` (boolean) already existed and was already in Warehouse's `orders_lines.update` field grant, just never referenced anywhere in the app. Scope narrowed to match the prototype's own exact boundaries (confirmed by reading `Dev-domain.js`/`Dev-OrderDetail.jsx` directly, not assumed): `short` only ever applies to kg/gram lines (`isWeightOnlyUnit`), never Loaf — a Loaf line has no exemption path, matching the prototype's own `isWeightUnit(l.unit)`-gated `held()` branch exactly.
  - **New `handleToggleShort(lineId, currentShort, qty)`**: writes `{ short: next, sent: next ? 0 : qty }` immediately (this port persists per-field on interaction, unlike the prototype's fully-local-until-advance model) and updates local `lines`/`sendingQtyMap` state. Forcing `sent` to 0 (and restoring it to the full qty on un-mark) reuses this port's existing "X to follow" mechanism (`order_lines.sent`, shipped 2026-08-18) as the owed-quantity tracker — this port has no separate fulfillment-tracking field the way the prototype's `lineLeft()`/delivery-time `owes` check does, so routing through `sent` was the most direct way to make "customer still owed this" visible to every role/stage without inventing new plumbing.
  - **New toggle button** (`Icon name="cancelled"`, the existing package-remove icon — no new icon needed) rendered next to "Add weighing" for `canWeighHere && isWeightOnlyUnit(line.unit)` lines; a persistent warning-colored chip ("Short — ran out of stock", reusing the `.toFollowHint` style) shows in the Item Summary row for every role/stage once a line is flagged, matching the prototype's own always-visible `l.short` chip (not just at Cold Storage).
  - **`coldWeighingReady`** now treats a short-flagged kg/gram line as satisfied without a recorded weight — a genuine stock-out no longer deadlocks the "Release to Finance" button.
  - **Follow-up fix, same day, reported directly**: `coldWeighingReady` only had the kg/gram short-flag branch — it was missing the prototype's OTHER `held()` branch (`Dev-OrderDetail.jsx:671`: a non-weight-unit line, i.e. Loaf or counted, held via its Sending qty being 0), so a Loaf line with nothing to send still blocked the release button, unlike the prototype. Fixed by hoisting the `isLineHeld(l)` helper (already built for the photo-requirement exemption below) above `coldWeighingReady` and having both gates share it, instead of `coldWeighingReady` re-implementing only half of `held()` inline. Confirmed the Short toggle button and its persistent chip were already correctly kg/gram-only (`isWeightOnlyUnit`, never Loaf) — no change needed there; a held Loaf line is exempted via its existing Sending-qty-0 box, not via Short, matching the prototype's exact per-unit split.
  - **Cold-storage photo requirement** (`handleAdvance`): new `isLineHeld(l)` helper (short for kg/gram, `sending qty === 0` for everything else — Loaf included, matching the prototype's `held()` exactly) — the "attach at least one item photo" gate is now skipped entirely when every non-removed line is held, ported from the prototype's `photosOk`. This port's photo rule was already looser than the prototype's (order-wide "at least one photo anywhere" vs. the prototype's true per-line requirement) — that pre-existing simplification wasn't touched, only the held-line exemption was added on top of it.
  - **Counted-item "held" equivalent** (a sending box set to 0 exempting a line) was verified to already exist structurally via `order_lines.sent`/the "to follow" hint (shipped 2026-08-18) — no separate implementation was needed there, just reused by `isLineHeld` above.
  - **"Pull & weigh" Card**: the cold-stage advance button was previously a bare `<Button>` with no heading, no Finance-parallel-queue status line, and no explainer text — reported directly, with a screenshot of the prototype's equivalent card. Rebuilt as its own `Card` (matching the pattern already established for Production/Packing/Dispatch's own cards), gated on `canWeighHere` rather than `canAdvance` (matches the prototype's own gate — keeps a Finance user who's been granted `helpOtherStages` from seeing this card instead of their own Finance-gate form). Copy — "Pull & weigh" heading, "✓ Payment already cleared by Finance" / "⏳ Finance is clearing payment in parallel" status line, the full weighing/short-flag explainer paragraph, and the "Nothing to weigh — fixed packs only." fallback — was already sitting translated, unused, in `translations.ts`; wired in verbatim.
  - **Audited every other stage's action panel against the prototype's `renderPanel()`** (per "check all the section for all roles and stages"): Production/Packing/Dispatch already have their own dedicated Cards with matching headings/copy from earlier sessions — no gaps found. `intake` and `finalise` have no heading/explainer in the prototype either (just a bare button in a plain card) — left as-is, nothing to port. The prototype's `outstanding`/`awaiting` cases (full nyusul/backorder/reminder-date flow) were **not** touched — those are missing *features* this port doesn't have yet at all (explicitly flagged out of scope in the self-undo audit entry above), not a copywriting gap on an existing feature; forcing that copy onto this port's actually-different COD-outcome-driven `outstanding` design would have been misleading.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓ (393 modules). Not yet browser-tested: at `cold`, confirm a kg line can be marked Short without a weight and the release button enables; confirm a Loaf line has no Short button; confirm the "X to follow" hint appears for a short-flagged line at every later stage; confirm an order with only short/held lines and zero photos can still release when `require_photo` is on; confirm the new "Pull & weigh" card's payment-status line flips between the two states correctly.

- **New dedicated `CustomerNew`/`ProductNew` create pages, replacing the awkward "empty detail page → click Edit" create flow (2026-08-19):** Customers'/Products' "+ New" buttons already navigated to `/customers/new`/`/products/new`, but no dedicated route existed for either — both matched the `:id` detail route with `id="new"`, landing on `CustomerDetail`/`ProductDetail` rendered mostly empty with a "New Customer"/"New Product" title and an Edit button that then routed to `.../new/edit` (`CustomerEdit`/`ProductEdit`, which already had working `isNew` create-mode logic) — two clicks and a confusing blank dossier page in between, reported directly.
  - **New pages**: `src/pages/CustomerNew/CustomerNew.tsx` and `src/pages/ProductNew/ProductNew.tsx` (+ their own `.module.css`, layout/CSS ported verbatim from `CustomerEdit`/`ProductEdit`) — create-only, no load-existing/change-detection machinery a fresh record doesn't need. Routed at `customers/new`/`products/new` in `App.tsx`, declared before the `:id` routes for readability (React Router ranks static segments over `:id` automatically regardless of declaration order, so this was already unambiguous, but matches how a reader would expect to find it).
  - **Extracted `parseLatLng`/`formatLatLng`** (the "lat,lng" / Google-Maps-URL parser for the Delivery Location Pin field) out of `CustomerEdit.tsx` into a new shared `src/lib/latlng.ts` — both `CustomerEdit` and the new `CustomerNew` need it now.
  - **Cleaned up the now-dead `isNew`/`id === 'new'` branches** in `CustomerEdit.tsx`, `ProductEdit.tsx`, `CustomerDetail.tsx`, and `ProductDetail.tsx` — creation only ever goes through the new dedicated pages now, so the old create-mode paths in Edit/Detail were unreachable dead code (the `.../new/edit` URL still technically resolves to `CustomerEdit`/`ProductEdit` with `id="new"`, but nothing in the app links there anymore). `ProductEdit`'s OOS-only-user access path (`canToggleOOS`) is untouched — that's existing-record-only and unaffected by the create-mode removal.
  - **Real bug found and fixed while building `CustomerNew`**: the removed `CustomerEdit.tsx` create branch (and my first draft of `CustomerNew.tsx`) generated a custom id (`"c-" + Date.now().toString(36)`) for the new customer. Confirmed live via curl: `customers.id` is a genuine Postgres `uuid` column (`default_value: gen_random_uuid()`) — a non-UUID string in that field fails the column's type check, and Directus reports it as a misleading `403 "You don't have permission to access this"`, not a validation error (same confusing-403 pattern as several other bugs fixed this session, just from a completely different cause this time — a data-type mismatch, not an ACL gap). Fixed by omitting `id` entirely and letting Postgres generate it, matching `OrderNew.tsx`'s own inline "create a new customer" call site, which was already doing this correctly. `products.id` has no equivalent issue — confirmed it's plain `varchar(255)`, so `ProductNew`'s slug-based custom id is safe as-is.
  - **Investigated a related live report** ("why can't Admin save an edited customer") while in this code: confirmed Admin's live `customers.update` Directus grant is fully unrestricted (`fields: ["*"]`, empty `permissions` filter) and queried all 17 real customers in the live DB — none has a malformed (non-UUID) id, so the bug above, while real and now fixed, isn't what the user hit (no existing customer could have been created through the buggy path — `OrderNew.tsx`'s customer-creation call, the only other create site, was already correct). Root cause not yet found; asked the user for the exact error text / which customer to pin it down further.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓ (393 modules). Not yet browser-tested: click "+ New Customer"/"+ New Product", confirm the new page renders directly (no intermediate blank dossier), fill the form and create, confirm it lands on the new record's detail page; confirm Cancel/Back both return to the list; confirm a non-`manage_customers`/`manage_products` role is redirected away if it somehow reaches the URL directly.

- **Admin viewing a `finance`-stage order saw neither the Finance-gate form nor the "This order is with Finance now" notice — a silent screen with no explanation (2026-08-19), reported directly against the prototype's `renderPanel()` guard (`Dev-OrderDetail.jsx:656`: `"This order is with {ACTOR[order.stage]} now. You can view it, but the action is theirs."`):** the `.actorNotice` banner itself already existed in this port (built 2026-08-06, `OrderDetail.tsx`'s `showActorNotice`) — the bug was in one of its inputs. `canAdvance` (`flow ? auth.can(flow.capability) || auth.can("helpOtherStages") : false`) applied the `helpOtherStages` floor-helper fallback to **every** stage's capability, including `finance`'s (`approveFinance`). The prototype's own equivalent (`canAct`, `Dev-OrderDetail.jsx:122-127`) deliberately scopes `helpOtherStages` to only `['cold', 'production', 'packing', 'dispatch']` — `finance` is covered by a *separate* clause gated on `actFinanceGate`/the `Finance` role specifically, never the generic floor-helper grant. Since Admin is the only role with `helpOtherStages: true` by default in this port, and Admin lacks `approveFinance`, this meant `canAdvance` read `true` for Admin at `finance` (enough to suppress `showActorNotice`, since it's gated on `!canAdvance`) while the actual Finance-gate form stayed correctly hidden (separately gated on `auth.can("approveFinance")` alone) and the generic advance button never rendered either (`finance`'s `flow.next` is `null`) — net result, nothing on screen at all for Admin.
  - **Fixed**: `canAdvance` now excludes `stage === "finance"` from the `helpOtherStages` fallback, matching the prototype's explicit stage list exactly.
  - **Confirmed this is an isolated, safe fix**: every other `canAdvance` call site (the generic advance button, the Production/Packing/Dispatch cards) already only renders for `cold`/`production`/`packing`/`dispatch`/`finalise`/`intake` stages — none of them is `finance`, so the added exclusion changes behavior at `finance` only.
  - **Re-audited the rest of the actor-notice mechanism against the prototype while in this code** (the user asked for a full review, not just this one case, echoing the same audit style as the self-undo pass below): `ACTOR` map (`pipeline.ts:299`) matches the prototype's 9 entries exactly; the notice's 3 terminal exclusions (`delivered`/`cancelled`/`returned`) match the prototype's own `!['delivered','cancelled','returned'].includes(order.stage)` guard exactly (deliberately does *not* exclude `outstanding`/`awaiting` — matches); `hasParallelFinanceAction`'s `['cold','finance']` + Finance-role carve-out matches the prototype's third `canAct` clause. No other gaps found.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains). Not yet browser-tested: log in as Admin, open an order parked at the `finance` stage, and confirm the "This order is currently with **Finance**. You can view it, but the action is theirs." notice now renders; confirm a genuine Finance-role (or Owner) session still sees the full Finance-gate form there, unaffected.

- **Full audit of the prototype's self-undo against every stage-changing handler in this file — 4 more real gaps found and fixed (2026-08-19), per explicit follow-up request to check *all* stages/roles, not just the ones already reported:** grepped every `advance(...)` call site in the prototype (`Dev-OrderDetail.jsx`, ~16 sites across the main pipeline and the returns sub-flow) and cross-referenced against every `updateOrder(id, {...})` call in `OrderDetail.tsx` (20 sites) to find which ones actually change `stage` and were still missing a snapshot:
  - **Fixed**: `handleHold` (→ `outstanding`) — was explicitly nulling `undo_snapshot`; the prototype's own `canSelfUndo` has no exclusion for the outstanding/hold stage (only `cancelled` is excluded), so this was a real, incorrectly-suppressed case. `handleRestore` (cancelled/outstanding → prev stage) — ported from the prototype's `restoreOrder()`/`reopenOrder()`, both of which go through `advance()` unconditionally. `handleConfirmRefusal` (delivery-time refusal → `returned`, Courier's action) — matches the prototype's own refusal handler (`Dev-OrderDetail.jsx:1002`), which calls `advance()` with no `noUndo` flag. `handleConfirmSettle`'s replacement branch (→ `cold`) — matches the prototype's return+replacement `advance()` call (`Dev-OrderDetail.jsx:415`); the other two `handleConfirmSettle` branches (revise-return, return-note) don't touch `stage` at all and correctly stay untouched.
  - **Confirmed already correct, no change needed**: `handleCancel` (→ `cancelled`) still explicitly nulls `undo_snapshot` rather than writing one — functionally identical outcome to the prototype either way, since `canUndo`'s `stage !== "cancelled"` exclusion would hide the button regardless of whether a snapshot exists; `handleConfirmDocsReturned`/`handleTermsPaymentReceived`/`handleUndoTermsPayment`/`handleReconcileCOD`/`handleConfirmReceive`/`handleMarkSignedAndClose` don't change `stage` at all (several already have their own dedicated, narrower undo — e.g. `handleUndoTermsPayment` — which is correct and unrelated to this generic mechanism).
  - **Explicitly out of scope, flagged rather than silently built**: a handful of prototype `advance()` call sites have no equivalent handler in this port at all yet — "send the rest now" (nyusul/partial-backorder re-delivery), the reweigh-detour (`reweighFrom`, an added/changed weighed item routes the whole order back through Cold Storage), "close short"/"drop the remainder" (backorder closing), and "Awaiting stock → Activate". These are missing *features*, not missing *undo coverage* on an existing feature — porting them is new scope, not part of this audit.
  - Every role that can reach any of the newly-covered handlers already has `undo_snapshot` in its live Directus grant (Admin/Owner via `fields: ["*"]`/`admin_access`; Warehouse, Production, Finance, Courier all fixed in the entry directly below) — verified no new ACL gap was introduced by this pass.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **Warehouse couldn't advance "Release to Finance" ("You don't have permission to access this") — the generic self-undo write hadn't been added to the field-restricted roles' live ACLs (2026-08-19):** same bug class as Finance's and Production's `stage`-missing errors from earlier the same day, but for the brand-new `undo_snapshot` field the generic self-undo mechanism (above) started writing on every stage-changing action. Warehouse's, Production's, and Finance's live `orders.update` grants are all field-restricted lists (not `fields: ["*"]`) and none of them included `undo_snapshot` — so the moment `handleAdvance` started sending it alongside `stage`, the whole write 403'd for every one of those roles, not just the one that got reported. Audited and fixed all three at once rather than waiting for each to surface separately:
  - Added `undo_snapshot` to Warehouse's, Production's, and Finance's `orders.update` grants (Courier already had it, from the pre-existing delivery-confirm Undo; Admin/Owner unaffected, `fields: ["*"]`/`admin_access`).
  - Also added it to Warehouse's and Production's `orders.read` grant (Finance already reads `fields: ["*"]`) — without read access the self-undo link would have silently never appeared for them even after a successful write, since the app couldn't read `order.undo_snapshot` back.
  - While in there, also added the still-missing `cutting_started_at`/`cutting_started_by` to Production's read/update grants (had `cutting_started` but not its two companion fields from the 2026-08-19 Start Cutting feature).
  - Docs updated. No app code changes — purely a live ACL gap.

- **Generalized the one-off "Pressed wrongly? Undo" into the prototype's actual generic self-undo mechanism, closing a real gap: it only worked on 2 of the prototype's ~7 stage transitions before (2026-08-19):** the packing-stage screenshot showed "Undo — back to Processing" as an *ordinary* per-stage-transition affordance — going and checking the prototype's actual source (not just the screenshot) found it isn't stage-specific at all: `canSelfUndo`/`undoMyStep()` (`Dev-OrderDetail.jsx:345-356`) is ONE generic mechanism, offered after *any* `advance()` call, to whoever just made the move (or Owner), for as long as it's still the last thing that happened to the order. This port had instead grown three separate, hand-rolled, stage-specific Undo implementations (delivered→dispatch, Finance-clear, and the previous session's own cold→finance "release" undo) — each duplicating the same idea in a different shape, with everything *else* (production→packing, packing→finalise, every plain advance, every send-back) having no undo at all.
  - **Reused the existing `undo_snapshot`/`UndoSnapshot` infrastructure** (`{prevStage, changedFields, who, at}`, originally built only for the delivered-order case) — it was already structurally generic, just never used anywhere else. New `snapshotFor(patch)` helper captures the pre-write value of every field a given patch is about to touch, from the current `order`; any stage-changing handler can now call it with its own patch object and get back exactly what's needed to reverse itself.
  - **Wired into every stage-changing handler that lacked it**: `handleAdvance` (intake→cold, cold→finance/production, finalise→dispatch), `handleSendBack` (every send-back, superseding its old one-off `undo_snapshot: null` clearing), `handleCuttingDoneAdvance` (production→packing), and the new `handlePackAdvance` (packing→finalise, see below). Deliberately **not** wired into `handleApproveFinance`/`handleUndoFinanceClear` — those keep their own separate, deliberately non-time-limited Undo (`showFinanceUndoRow`), matching the prototype's own separate `canUndoClear`, which is real and distinct from `canSelfUndo` there too.
  - **Generalized `canUndo`/`handleUndo`** (previously delivery-specific, gated on `stage === "delivered"`): added the "last action" check (the snapshot's timestamp must match the newest `order_history` entry — the instant anything else happens, the offer silently disappears, no explicit clearing needed anywhere) and the `stage !== "cancelled"` exclusion (a cancelled order uses Restore instead), both ported directly from `canSelfUndo`'s own condition. The confirm/history copy is now dynamic (`STAGE_LABELS[snapshot.prevStage]`) instead of hardcoded "back to dispatch". Render moved from inside the delivered-only proof card to one stage-agnostic location (replacing the retired cold-release-specific block), so it now surfaces after *any* eligible move, not just a delivery confirmation.
  - **Retired `showColdReleaseUndoRow`/`handleUndoColdRelease`** (added the previous session) — fully superseded now that `handleAdvance`'s cold→finance write carries its own generic snapshot.
  - **New "Pack the order" card** at the `packing` stage (`Dev-OrderDetail.jsx:769-782`), replacing the generic advance button for that stage the same way Production's card already does: lists cut lines vs. other lines by name, "Packed & ready" advances to `finalise`. Every string this needed (`"Pack the order"`, the explainer sentence, `"cut item(s)"`, `"other item(s)"`, `"Packed & ready"`) was already sitting unused in `translations.ts` — same dormant-scaffolding pattern as several other features this week.
  - **Two small companion fixes caught while in this code**: the read-only cutting-instruction icon (shown on every line, every stage — e.g. Warehouse reviewing at `packing`) now shows a checkmark instead of a knife once that specific cut is marked done, matching the prototype's own visual cue; the "Cut · tick each cutting" section *heading* (not just the checklist under it) is now hidden until "Start cutting" is tapped, not just visually empty.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **New `prompt()` modal (replacing the need for `window.prompt`) + Send Back now asks why, resetting "Start cutting" on arrival at `production` too (2026-08-19):** the Production "Start cutting" feature shipped the day before only reset `cutting_started` when *leaving* `production` — but the prototype's `sendBackStage()` (`Dev-OrderDetail.jsx:360`) resets it on *every* send-back, so an order routed back to `production` from a later stage (e.g. `packing`) arrived with stale "cutting in progress" state and no visible "Start cutting" button, exactly as reported.
  - **New shared `prompt()` dialog**, extending the existing `alert()`/`confirm()` Modal-based system (`dialog-context.ts`/`DialogProvider.tsx`) rather than falling back to `window.prompt` — a text input inside the same shared Modal, resolving the typed string on Confirm or `null` on Cancel (matching `window.prompt`'s own contract, so the ported `if (reason === null) return` guard reads identically to the prototype's). New `DialogProvider.module.css` for the input's styling.
  - **`handleSendBack`** now opens `prompt("Send back to {stage} — why?")` first — cancelling aborts with no write, same as before any button existed; a typed reason gets appended to the `order_history` entry (`Stage returned: X → Y — reason`). Available to Admin (`sendBackStage`), Owner (always, via the existing bypass), or whoever owns the *current* stage (per the 2026-08-18 `canSendBack` fix) — confirmed by reading the prototype's own capability default directly, not just assumed.
  - **Cutting-reset scope corrected**: rather than porting the prototype's *unconditional* `CUT_RESET` verbatim (safe there — one denormalized document, no field-level ACL), scoped it to exactly the two transitions where it's meaningful (`stage === "production"` leaving, or `flow.prev === "production"` arriving) — sending the reset fields on an unrelated transition (e.g. Courier's dispatch→finalise) would have 403'd the whole write, since Courier's live `orders.update` grant doesn't include `cutting_started*` at all. This is *narrower* than the prototype on purpose, not a partial port.
  - **Follow-up, same day, per explicit request**: the cut-tick checkboxes weren't resetting alongside "Start cutting" — deliberately diverges from the prototype here (which leaves `line_cuts.done` alone on send-back, "the meat doesn't become un-cut") on explicit instruction: this port now un-ticks *every* cut on the same two transitions, both the local not-yet-saved override map and any cuts already persisted `done: true` from a completed run before the send-back (written back via `updateLineCut`, batched, then `lineCutsByLine` refreshed) — the reasoning being a send-back means something needs re-doing, so Production should re-verify each cut rather than trust a stale tick.
  - **Cut tick-off UI swapped from full-width buttons to the shared `Checkbox` component**, per direct follow-up request — same `checked`/`onChange`/`label` pattern already used in `ProductEdit.tsx` (`<label><Checkbox/><span>{text}</span></label>`).
  - Registered a new `steak` icon (`hugeicons:steak`, confirmed present in the offline `@iconify-json/hugeicons` set) — added alongside the cut checkbox.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **Settings' "N learned matches" count differed by role — Warehouse/Production/Finance/Courier had zero live access to the `corrections` collection at all (2026-08-19):** only Admin/Owner had ever been granted `read` on `corrections`; every other role's `aggregateCorrections()` call had nothing to count, so the same order's real match count showed correctly for Admin and as 0 (or silently wrong) for everyone else. Per explicit request — the *count* should be consistent for every role, but the detailed browsing page and delete action should stay Admin/Owner-only:
  - Granted `read` (`fields: ["*"]`) on `corrections` to Warehouse, Production, Finance, and Courier live — matches Admin's own read grant shape. Docs updated.
  - The new "View all matches" button on Settings' Intake Learning card is now gated behind `canManageSettings` (Admin/Owner only) — everyone else still sees the summary count, just not the button.
  - `/settings/learned-matches` itself is now route-guarded (`<Guarded cap="manageSettings">`, same pattern as every other capability-gated route in `App.tsx`) — direct-URL access for a non-manager now redirects home, not just the button being hidden.
  - `tsc -b` ✓, `eslint` ✓, `npm run build` ✓. Live ACL change verified via curl. Not yet browser-tested — log in as Warehouse/Production/Finance/Courier and confirm the count now matches Admin's, and that `/settings/learned-matches` redirects away for all of them.

- **Production couldn't advance production→packing at all ("You don't have permission to access this") + the prototype's whole "Start cutting" workflow was never built (2026-08-19):**
  - **Root cause, same bug class as Finance's the day before**: Production's live `orders.update` Directus grant was restricted to just `cutting_started` — missing `stage` entirely, so `handleAdvance`'s `{ stage: target }` write was rejected outright for every genuine Production session, regardless of the `cutProduction` capability being correctly granted client-side. Added `stage` to Production's live field grant. Docs updated.
  - **"Start cutting" — a real, previously-undiscovered gap**: `orders.cutting_started` already existed as a live field (confirmed dormant — grepped the whole `src/` tree, zero references anywhere) and every i18n string the feature needs (`"Start cutting"`, `"Cutting in progress"`, `"Cut · tick each cutting"`, `"No cutting needed."`, `"Cutting done → to packing"`, the locking-explainer sentence) already existed in `translations.ts`, unused — same "scaffolded but never wired up" pattern as `order_lines.sent` and the Loaf-casing bug earlier this week. Ported the prototype's Production card faithfully (`Dev-OrderDetail.jsx:744-766`, `startCutting()`/`CUT_RESET` at lines 303-306):
    - New live fields `orders.cutting_started_at` (timestamp) and `orders.cutting_started_by` (uuid → `directus_users`) — `cutting_started` alone existed but not its accompanying who/when, which the prototype's card displays inline. Production's (and Warehouse's, for floor-helper coverage) live read/update grants extended to include both.
    - New Production card replaces the generic advance button *only* for the `production` stage (`stage === "production"` now excluded from the generic button's render condition): "Start cutting" (writes the 3 fields + a "Started cutting" history entry) flips to a green "Cutting in progress · HH:MM · name" line once tapped; below it, one tap-to-tick button per cutting instruction across every line (`cutTasks`, flattened); the final "Cutting done → to packing" button — the *actual* stage-advance action for this stage — stays disabled until every cut is ticked, then persists each `line_cuts.done` flag and advances to `packing` in one action, matching the prototype's `done = cutTasks.every(...)` gate exactly.
    - Tick state is local-only until the final click (mirrors the prototype's own local `cut` state) — no per-tap network write, one batch write at advance time.
    - `handleSendBack`'s production→cold path now resets `cutting_started`/`_at`/`_by` to null (ported from `CUT_RESET`) — a re-entry to production later shouldn't arrive pre-flagged as "in progress". Already-ticked `line_cuts.done` flags are deliberately left alone (physically cut meat doesn't become un-cut) — matches the prototype's own CUT_RESET scope exactly (it never touches `cut.done`).
    - **Deliberately not ported in this pass**: the prototype's `lineFrozen()` (locks a cut line from further edits in Order Edit once cutting starts or a cut is marked done). This request was scoped to "the button" and "the logic for production" — i.e. the production-stage workflow itself, which is now complete — not Order Edit's separate edit-lock behavior. Flagging as a known, deliberately-deferred follow-up rather than silently skipping it.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live schema + ACL changes verified via curl. Not yet browser-tested — see the Current Goal note above.

- **Finance couldn't clear payment at the `finance` stage ("You don't have permission to access this") — same root cause as the earlier unresolved Admin report, now confirmed and fixed (2026-08-19):** `handleApproveFinance` bundles `stage: "production"` into the same `orders.update` write whenever clearing payment while the order is parked at the virtual `finance` stage (Cold Storage finished weighing first) — by design, per the documented parallel-gate feature. Finance's live Directus `orders.update` grant was field-restricted to just the `payment_*`/`cod_*` fields and did **not** include `stage` — Directus rejects a field-restricted write wholesale if *any* field in the payload isn't on the allow-list, so this single combined write always 403'd for a genuine Finance session the moment it also needed to touch `stage`. (The earlier report attributed to "Admin" was almost certainly the same bug hit from a Finance-role test account — Admin's own grant is unrestricted `fields: ["*"]`, confirmed clean.) Fixed live: added `stage` to Finance's `orders.update` field list via curl. `context/schema/roles-and-permissions` updated to match.
  - Not yet browser-tested — confirm as Finance: clear payment on an order sitting at `finance` and confirm it both writes the payment fields *and* advances to `production` in one action, matching the documented design.

- **QA feedback batch #2 — 9 fixes across Add Item, weighing, sending qty, photo access, and a new Learned Matches page (2026-08-19):**
  - **AddItemModal custom-name input was broken twice over**: the main items-list "Item name" input for a custom (no product match) line had no `value` prop at all (always rendered blank, regardless of what was actually stored) *and* its `onChange` compared a shadowed inner `l` to itself (`l.id === l.id`, always true) inside `setLines`, so every keystroke overwrote **every line's** `name` field — a field `LineDraft` doesn't even declare, silently tolerated by TS's structural typing. Fixed in `OrderNew.tsx`: now `value={l.freeText}` with `onChange` routed through the existing `updateLine(l.id, { freeText: ... })` helper, matching how the rest of the form already updates lines.
  - **Weigh-tolerance hint no longer applies to Loaf**: re-checked the prototype directly (`Dev-domain.js:32,35`) — `isWeightUnit` (kg/gram only) and `isWeighed` (`isWeightUnit || loaf`) are two different predicates there; the tolerance hint always used the narrower one, since a Loaf's ordered "qty" is a piece count, not a weight, so comparing it against a kg total never made sense. Added `isWeightOnlyUnit()` alongside the existing `isWeighedUnit()` in `OrderDetail.tsx` and switched the hint to use it — Loaf keeps its weighing requirement (unaffected), just not the below/above-order warning.
  - **"Sending" quantity was pure local `useState`, never read from or written to the database** — a real, previously-undiscovered gap: every page load reset it to the full ordered qty regardless of what any previous session had entered, which is exactly why Warehouse saving "2 of 3" still showed "3 of 3" to Finance in a different session. Added a live `order_lines.sent` field (`numeric(12,3)`, nullable — matches `qty`'s own precision) via curl, added it to `OrderLinesCollectionSchema`, and extended the *live* field-restricted ACLs that were missing it entirely (Warehouse's create/read/update/delete, Production's and Courier's read — Admin/Finance already had `fields: ["*"]`). `OrderDetail.tsx` now initializes the sending map from the persisted `sent` value (falling back to full qty only when never set) and saves on blur via a new `handleSendingBlur`, mirroring the weighing inputs' own save pattern.
  - **Sending qty now capped at the line's ordered qty**, and a "· N to follow" hint (amber, reusing the `to follow`/"menyusul" i18n key already in the file) shows whenever sending is less than the full qty — both ported from the prototype's `sendingOf()`/`remaining()`/"to follow" text (`Dev-OrderDetail.jsx:223,1367-1370`).
  - **Sending qty is now read-only for every role except whoever owns Cold Storage weighing** (`canWeighHere` — Warehouse, Owner, or a floor helper): everyone else sees the saved figure as plain text instead of an editable input, closing the "unauthorized role can edit sending" gap flagged live.
  - **Finance couldn't see item-condition photos at all** — root cause: Finance's live Directus ACL had *zero* rows for `line_photos` (not even read), confirmed directly; the app itself never gated the photo *fetch* or *display* by role, so this was purely a missing grant. Added a `read`-only `line_photos` permission for Finance so they can review photos before clearing payment, matching the prototype (single denormalized document, no ACL boundary to hit there).
  - **Warehouse (and other stage owners) had no self-service way to correct a mistaken stage advance**: `canSendBack` was gated on the generic `sendBackStage` capability alone (Admin-only by default) — extended it to also allow whoever owns the *current* stage (`flow.capability`) or is helping as floor cover (`helpOtherStages`), mirroring `canAdvance`'s own pattern, for every normal linear stage transition (production→cold, packing→production, etc.).
  - **New, separate fix for the `cold → finance` parallel-gate park specifically**: that transition can't be reversed through the generic `canSendBack` mechanism at all — the `finance` entry in `STAGE_FLOW` has no `prev` (it's a virtual parking stage, not a normal link in the chain) and its own `capability` is `approveFinance` (Finance's job, not Warehouse's). Found a matching *orphaned* i18n key (`"Undo — back to"`, never referenced anywhere in the code) as a strong signal this was intended but never finished. Built a dedicated quiet "Pressed wrongly? Undo — back to Cold Storage Picking" link (same visual pattern as the existing delivered-order and finance-clearance Undo rows), shown only at the `finance` stage to whoever can weigh at Cold Storage, writing `stage: "cold"` with no other field changes.
  - **New standalone "Learned Matches" page** (`/settings/learned-matches`), reached via a new "View all matches" tertiary button on Settings' Intake Learning card (right-aligned, in line with the "N learned matches" count text, per explicit request). Table layout matching Customers/Products (search box, `SortableTh` columns: Token/Product/Added by/Date/Uses, delete action gated on `manageSettings`) — reuses the existing `useCorrections()` hook as-is. Settings' own inline corrections list was removed in favor of just the summary card + link, since the full browsable/searchable table now has its own page with real room.
  - **Sort persistence extended to Customers and Products** (previously only Orders/Dashboard): both pages' `sortBy` now lives in `?sort=...`, same pattern, so it survives Back navigation from a customer/product detail page the same way Orders already does.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` `set-state-in-effect` error remains), `npm run build` ✓ (387 modules). Live schema (`order_lines.sent`) and ACL changes (Warehouse/Production/Courier/Finance) verified via curl. Not yet browser-tested — see the Current Goal notes above, including the two open questions (payment-permission mismatch, WhatsApp-paste key-format mismatch) that need more information to close.

- **Past-delivery-date hint added to New Order (2026-08-18):** ported the prototype's `deliverPast` (`Dev-Intake.jsx:154,219`) — picking a delivery date earlier than today now shows an amber "Delivery date {D} is in the past — is this a typo?" banner (same `.bannerWarning` treatment as the duplicate-order-number banner added earlier today), right above the Order No./Delivery Date row. Purely a hint, same as the prototype — doesn't gate Create the way `dupOrder`/`badNo` do, since a genuinely late order (a real backdated entry) is a legitimate thing to create, just worth a second look.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **`order_lines.product_id` / `corrections.product_id` were both `uuid`-typed columns referencing a non-UUID primary key — a severe, previously-undiscovered foundational bug, root-caused live (2026-08-18):** investigating why "learned matches" stayed at 0 despite repeated corrections led here. `products.id` is a slugified string (`${slugify(name)}-${Date.now().toString(36)}`, e.g. `"90-cl-friboi-123"`) — it has never been a UUID, in any product, ever. But `order_lines.product_id` and `corrections.product_id` were both created as Postgres `uuid` columns. `directus.ts`'s `sanitizeUuidFields()` helper — a defensive guard that silently nulls any non-UUID value before a write, added to stop bad data from crashing a request — has therefore silently discarded **every single `product_id` write in this app's history**: confirmed via direct query that 0 of the live database's 27 `order_lines` rows have ever had `product_id` populated, including rows with `status: "recognized"` (a real product match was found client-side, and thrown away before the write). `corrections` writes failed even harder — no sanitizer runs there, so every `upsertCorrection()` call threw a raw Postgres `22P02 invalid input syntax for uuid` error, caught internally and returned as `{error: ...}`, which every calling site (`OrderNew.tsx`'s submit sweep, the newly-added AddItemModal wiring above) never checked — so the failure was completely silent, explaining the "still 0" symptom exactly.
  - **Live schema fix** (curl + admin token): `PATCH /fields/order_lines/product_id` and `PATCH /fields/corrections/product_id`, both `uuid` → `string` (`character varying(255)`, matching `products.id`'s own `max_length`). Verified via a live test write both before (`22P02`) and after (succeeds) the change. `context/schema/snapshot.json` updated to match.
  - **Code fix**: removed `"product_id"` from the 3 `sanitizeUuidFields(...)` call sites that guarded it (`createOrderLines`, `updateOrderLine`, `createOrderLine` in `directus.ts`) — it's no longer a UUID field, so the guard was actively wrong, not just unnecessary.
  - **Real-data fix, not just a schema change**: the order line the user was live-testing this exact bug against (`"Jpn A5 Kamenoko (outside Knuckle) Cut"`, order `260809003`) had its `product_id` set correctly via the same curl path, matching the real catalog product — confirming the full path now works end to end.
  - **Deliberately not backfilled**: the other 26 pre-existing `order_lines` rows with a null `product_id` were left alone — matching them back to a real product by `name` text carries real mismatch risk (free-text/unrecognized lines have no reliable product to backfill to), and doing it silently as part of an unrelated bug fix isn't the right call. Flagged here in case a deliberate backfill pass is wanted later.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live schema + data changes verified via curl before and after.

- **Duplicate entries in the notifications popover — a React 18 StrictMode dev-only race in `useNotifications.ts`, not a duplicate database write (2026-08-18):** confirmed first that `order_history` itself only ever had one "Order created" row for the order in question — ruling out a double `appendOrderHistory()` call. The actual cause: the hook's mount `useEffect` used a plain boolean `cancelledRef` to guard against applying a stale fetch's results, but StrictMode's dev-only mount→cleanup→mount double-invoke resets that boolean back to `false` (by the *second* mount) before the *first* call's async continuation resumes — so the first (stale, duplicate) fetch's rows still get pushed into the shared `byDayRef` map alongside the second (correct) fetch's identical rows, with no dedup on the push. Replaced the boolean with an incrementing `epochRef` counter: each call captures its own epoch at invocation and only applies results if the epoch is still current when it resumes, correctly invalidating a superseded call regardless of how many times the effect re-fires. Production builds don't double-invoke effects, so this was dev-only, but the underlying unprotected-shared-ref-mutation pattern was a real latent bug either way.
  - `tsc -b` ✓, `eslint` ✓, `npm run build` ✓.

- **Loaf-unit lines silently skipped Cold Storage weighing when created via New Order, then the requirement flipped on once the line was later saved through Order Edit — a real, previously-undiscovered casing bug (2026-08-18):** `OrderNew.tsx`'s unit dropdown (`UNITS`) writes lowercase `"loaf"`; `OrderEdit.tsx`'s (`UNIT_OPTIONS`) writes capitalized `"Loaf"`. `OrderDetail.tsx`'s `isWeighedItem` check (and the `coldWeighingReady` gate added earlier today) both compared case-sensitively against `"Loaf"` only — so a line created fresh via New Order (lowercase `"loaf"`) never matched, silently never showing the weighing input and never counting toward the "Release to Finance" readiness gate, while the *identical* line, if later re-saved through Order Edit's differently-cased dropdown, would suddenly flip into "needs weighing." User caught this live by watching a single line's behavior change across an edit round-trip. Fixed with a shared case-insensitive `isWeighedUnit()` helper in `OrderDetail.tsx`, replacing both call sites — fixes every existing lowercase-`"loaf"` line immediately, no data migration needed, and stops the requirement from depending on which page last touched the line's unit.
  - `tsc -b` ✓, `eslint` ✓, `npm run build` ✓.

- **AddItemModal's manual "Add Item" flow now actually learns and reuses corrections — a real, previously-undiscovered gap (2026-08-18):** Settings' "Intake Learning" section (count card + reviewable list, `useCorrections.ts`) and the write path (`upsertCorrection`) were already fully built and working for the WhatsApp-paste flow — but that's a separate external service (`/order-api/parse-order`, outside this repo) that presumably consults `corrections` server-side. The in-app manual "Add Item" modal (`AddItemModal.tsx`, shared by `OrderNew.tsx` and `OrderEdit.tsx`) had its own completely separate, local free-text matcher (`parseFreeTextLine` — exact name → substring → word-overlap, against `products` only) that never touched `corrections` at all: it neither consulted learned matches to improve its own matching, nor saved anything when a user manually fixed a wrong/missing match. The "10 learned matches" count in Settings was real, but only ever grew from pasted-WhatsApp orders — every manually-added item was a dead end, matching from scratch every time no matter how many times the same fix was made.
  - **New `src/lib/itemToken.ts`** — `normalizeItemToken()`: lowercases, trims, collapses whitespace, and strips a leading qty+unit ("2 kg ", "1 Box ") so "2 kg short rib" and "3  Kg  Short Rib" resolve to the same key. Deliberately simpler than the external parse-order service's own tokenizer (aliases, stopwords, price-stripping) — this key only needs to be self-consistent between AddItemModal's own save and lookup, not compatible with that other service's keys, which this repo doesn't control anyway.
  - **`AddItemModal.tsx`**: `parseFreeTextLine` now checks `corrections` (passed in as a new required prop) for a `token_key` match *before* falling through to the substring/word-overlap heuristic — "a learned correction wins outright," mirroring the WhatsApp-parser's own stated rule. `AddItemResult` gained `rawText` (the normalized key, only set when text was actually typed — never for the plain manual qty/unit/product row) and `learned` (true when the result came from a correction, not the heuristic) for callers to persist. Added a small green "✓ learned match" badge next to "Matched result" when `learned` is true; manually overriding the product dropdown afterward clears `learned` back to false so the badge can't misrepresent a hand-correction as a learned one — and, since `rawText` stays put across that override, the *new* correction is what actually gets saved, not the stale suggestion.
  - **`OrderNew.tsx`**: fetches `corrections` once alongside customers/products, passes to `AddItemModal`, and threads `result.rawText`/`result.learned` onto the new `LineDraft` (also setting `parseStatus: "recognized"` when learned, so the existing WhatsApp-prefill status-chip UI shows "learned" for these lines too, for free). No change needed to the submit-time `upsertCorrection` sweep — it already picks up any line with `rawText` + `productId`, regardless of source.
  - **`OrderEdit.tsx`**: same fetch/thread wiring, but this page had **no correction-learning sweep at all** before now (only `OrderNew.tsx` did) — added one to `handleSaveAllEdits`, same best-effort `Promise.allSettled` shape, so items added while editing an existing order also teach the matcher.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` `set-state-in-effect` error remains, untouched by this change), `npm run build` ✓ (385 modules, up from 384 for the new `itemToken.ts`). Not yet browser-tested — see the Current Goal note above.

- **New Order's duplicate-order-number banner now has the prototype's "Use #next" / "Replace it" actions, not just a static warning (2026-08-18):** ported from `Dev-Intake.jsx:161-162,220-225` — `dupOrder` was already computed (`existingOrderNos.includes(orderNoValue)`) but only rendered a plain sentence and unconditionally blocked submit; no way to resolve it except manually retyping a number.
  - **`existingOrders` now carries `{id, no, customer_name, stage}`** (was just a bare `no` string array) — needed to show which customer the duplicate belongs to and, for Replace, which order to act on. `dupOrder` is now the matched order object (or `null`), not a boolean.
  - **"Use #{expectedNo}"**: fills the lowest open slot for that delivery date (the same `expectedNo` the existing `seqGap` banner already computed) — one click, no typing.
  - **"Replace it"** — real design decision, confirmed with the user first: the prototype hard-deletes the old duplicate (it's an in-memory array there); this app has no order-delete anywhere, and every other termination (Cancel, Undo) is soft/reversible with a full `order_history` trail. So "Replace it" here sets a `replaceId`, and on submit — *before* creating the new order — the old one is renamed off the number (`orders.no` is a live unique constraint, confirmed via schema snapshot, so both couldn't hold it even briefly) to `` `${no}-REPL${Date.now().toString(36)}` `` and cancelled (`stage: "cancelled"`, `cancelled_from` its prior stage, same shape as `OrderDetail.tsx`'s own `handleCancel`), with a history entry noting it was replaced. The new order is then created normally with the original number. Reversible via the existing Restore flow, unlike a hard delete.
  - **New "Will replace the existing #X" confirmation banner** (blue/info tone) with its own Undo, mirroring the prototype's `form.replaceId` state exactly. `replaceId` clears automatically when the order-number input is hand-edited or the delivery date changes (a new date means a different duplicate set — a stale `replaceId` from before means nothing).
  - **Create button** now also disabled while `dupOrder && !replaceId` (previously only blocked at submit-time via an error message, not via the disabled state).
  - Visual polish: added `.bannerWarning`/`.bannerInfo` modifier classes (`OrderNew.module.css`, `color-mix()` amber/blue treatment matching the token convention already used in `OrderDetail.module.css`) plus the `alert` icon, so the duplicate/replace banners actually read as warning/info rather than plain unstyled text like the neighboring `badNo`/`seqGap` banners (left those two as-is — out of scope for this fix).
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **Order Edit silently discarded cutting-instruction changes for Admin ("You don't have permission" swallowed with no error) + weigh-tolerance hint wired up (2026-08-18):**
  - **Real bug, two layers**: (1) `OrderEdit.tsx`'s `handleSaveAllEdits` awaited `createLineCut`/`updateLineCut`/`deleteLineCut` without ever checking `.error` — unlike every other write in that same function (`updateOrder`/`createOrderLine`/`updateOrderLine`/`deleteOrderLine`, all of which `throw` on error into the surrounding try/catch that sets the visible error banner). Any cut write failure was silently swallowed: the page still navigated back to the order as if the save succeeded, with the cutting-instruction edit quietly lost. (2) The underlying reason it *was* failing for Admin: live Directus ACL had Admin as read-only on `line_cuts` (confirmed via curl — only a bare `read` permission row existed), even though `editOrderLines`/Order Edit is Admin's own capability and the prototype's equivalent (`editOrders`) always let the same role that edits an order also edit its cuts (single denormalized document there; this port normalizes `line_cuts` into its own ACL-scoped collection, which had never been extended to match). Fixed both: the 3 write calls now check `.error` and throw like their siblings, and Admin's role gained `create`/`update`/`delete` on `line_cuts` live (curl + admin token, copied field-for-field from Warehouse's/Admin's own existing `line_weighings`/`line_photos` grants added earlier today — `permissions: {}`, `validation: {}`, `fields: ["*"]`). `context/schema/roles-and-permissions` updated (per-role table + summary matrix).
  - **Weigh-tolerance hint — found the groundwork already fully built, only the consuming check missing**: `Settings.tsx` already has a complete "Weigh tolerance vs the order" UI (`tol_below_pct`/`tol_above_pct` inputs, defaulting to 10%), the `settings` collection already has both fields live, and both i18n keys ("below order"/"over order") already existed — but nothing in `OrderDetail.tsx` ever read them. Ported the prototype's `belowHint`/`aboveHint` (`Dev-OrderDetail.jsx:1350-1351`) directly: for each Loaf/kg/gram line with a non-zero weighed total, flags below/above when the total falls outside `qty × (1 ± tolPct/100)`. The item's "Total: X kg" line now turns `--state-warning` amber with a "· below order {qty} kg?" / "· over order {qty} kg?" suffix when out of range — a hint only, same as the prototype ("gentle, does not block"), no change to `handleAdvance`'s own gating logic (`coldWeighingReady`, added earlier today) which only cares about weight being entered at all, not whether it's within tolerance.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live ACL change verified via curl before and after. Not yet browser-tested — see the Current Goal note above.

- **Admin-as-floor-helper couldn't actually save a weighing ("You don't have permission to access this") + Cold Storage's "Release to Finance" button had no readiness gate (2026-08-18):** the 2026-08-14 floor-helper sweep fixed the *client-side* capability (`canWeighHere` gained `|| auth.can("helpOtherStages")`) but never checked the *live Directus ACL* — `line_weighings` and `line_photos` were still Read-only for the Admin role (confirmed via curl: only a bare `read` permission row existed for each, no `create`/`update`/`delete`), so every weight/photo save Admin attempted while covering Cold Storage 403'd. Fixed live via curl + admin token: added `create`/`update`/`delete` permission rows for Admin on both collections, copied field-for-field from Warehouse's existing unrestricted grants (`permissions: {}`, `validation: {}`, `fields: ["*"]`) — Admin now has the same full CRUD on `line_weighings`/`line_photos` that Warehouse has. `context/schema/roles-and-permissions` updated (both the per-role table and the summary matrix) to match; `snapshot.json` untouched, since it only tracks collection/field schema, not ACL rules.
  - **Real, previously-undiscovered second gap, found while comparing against the prototype**: the "Release to Finance" button (`Dev-OrderDetail.jsx:693`, `disabled={!ready || uploading > 0}`) was never actually ported — this port's equivalent button was only ever disabled by `advancing` (the in-flight-save flag), so it was clickable at any time, including with zero weight entered on every catch-weight line — silently skipping Cold Storage's whole job. New `coldWeighingReady` (`OrderDetail.tsx`, next to `canWeighHere`): every Loaf/kg/gram line must have a summed weighing total `> 0` before the button enables; an order with no weighed-unit lines is vacuously ready (nothing to block on, matches the prototype's `catchLines.every(...)` on an empty array). Applies uniformly to whichever role reaches the button (Warehouse, Owner, or Admin-as-helper) — same as the prototype, which never special-cased this by role. This port has no "short"/held-line concept the prototype's `ready` also accounts for, so the rule is simpler: weight-unit lines always need a real weight, no exemption. The existing required-item-photo check (`requirePhoto` setting) was left as its current click-time alert rather than folded into the same `disabled` state, since it already explains itself and a bare disabled button wouldn't.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live ACL change verified via curl before and after. Not yet browser-tested — see the Current Goal note above.

- **Sort now persists across Back navigation on Orders + Dashboard, and defaults to Order ID descending (2026-08-18):** `sortBy` on both `Orders.tsx` and `Dashboard.tsx` was plain `useState`, unlike `stage`/`search` (already URL-driven) — so even though `OrderRows.tsx`'s Back-nav state already carried `location.pathname + location.search`, the sort itself was never in that string and silently reset to the default on remount. Moved `sortBy` into the URL on both pages (`?sort=...`, mirroring `Orders.tsx`'s existing `stage`/`search` pattern exactly — param omitted when it equals the default, to keep the URL clean); `Dashboard.tsx` gained its first `useSearchParams()` usage for this. Also reverted the default sort from ascending (`"no"`) back to descending (`"-no"`) per explicit request, in all 4 places that had been flipped the other way during the 2026-08-14 sortable-header rework: `Orders.tsx`'s new URL-default, `Dashboard.tsx`'s new URL-default, `OpenOrdersPanel.tsx`'s own `sortBy` prop default, and `useOpenOrders.ts`'s `sort` param default — `useOrders.ts` (Orders' own data hook) already defaulted to `"-no"` and was untouched by that rework, so this brings the other three back in line with it.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **`payment_amount` numeric field out of range, blocking Finance Gate "Clear" (2026-08-18):** live error "Numeric value in collection 'orders' is out of range" when clearing payment on any order with a real Rupiah total. Root cause: `payment_amount` was created (as part of the Finance Gate rebuild above) with Directus's default numeric precision, `numeric(10, 5)` — max value 99999.99999, far too small for a typical order total like 840000. Every other Rupiah-money field in this schema (`order_lines.price`, `customers.credit_limit`, `delivery_proofs.cash_collected`) uses `numeric(15, 2)`; `payment_amount` was the one field added without matching that established convention. Fixed live via curl + admin token (`PATCH /fields/orders/payment_amount`, `numeric_precision: 15, numeric_scale: 2`), `context/schema/snapshot.json` updated to match. No frontend code change needed — `schemas.ts`'s `payment_amount: numeric` (the same shared union helper, already zod-loose on precision) was never the problem.

- **Situational titles added to every `alert()`/`confirm()` dialog app-wide (2026-08-18):** `Modal.tsx`'s `title` prop and the `AlertOptions`/`ConfirmOptions.title` plumbing (`dialog-context.ts` → `DialogProvider.tsx` → `Modal`) already existed end-to-end from the original alert-replacement work (2026-08-11) but no call site ever passed one, so the title block never rendered. Purely additive — no component/CSS change needed; the user-supplied `.title` CSS already matched `Modal.module.css` exactly. Added a `title: t("...")` (or a plain string for untranslated cases) to all ~90 call sites across the 7 files that use `useDialog()`: `Products.tsx` (1), `ProductEdit.tsx`/`ProductDetail.tsx` (3 each, identical delete-flow pattern), `CashUp.tsx` (2), `OrderNew.tsx` (2), `Settings.tsx` (13), and `OrderDetail.tsx` (63 — the largest, covering weighing/photo, documents, hand-off, terms/COD/finance-gate, delivery-proof/undo, cancel/hold/restore/reorder, and the full returns sub-flow).
  - **Naming convention established and applied consistently**: destructive confirms name the action ("Delete product", "Remove team member", "Cancel order", "Undo delivery" — with `danger: true`); error alerts get a short "Couldn't X" or "X failed" derived directly from the original message; validation alerts get imperative "X required" titles ("Photo required", "Receiver name required"); not-yet-built stub buttons get "X unavailable"; a couple of non-error confirmations got plain descriptive titles ("Copied", "Save delivery location?").
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains, untouched by this change), `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **Full Finance Gate rebuild + real cold-stage payment gating + Terms-invoice follow-up + Restore-Order fix for outstanding orders (2026-08-14):** closes QA `admin.md` §4's remaining unchecked items ("Help other stages" at cold, "Cannot act as finance gate unless covering", "Reopen a closed order... resets fulfilment for redelivery") and replaces yesterday's placeholder "Approve Payment" button with the prototype's actual form + gating logic (`Dev-OrderDetail.jsx:577-742`), per the Figma layout at `context/designs/finance_gate.png`. **Scope decision confirmed with the user first**: build Upfront + Terms (with the credit-limit/exposure check ported faithfully) — deliberately leave "COD" out of the Finance Gate entirely, since the port's existing delivery-time COD-outcome capture (`delivery_proofs.cash_collected`, full/partial/none) is COD's one correct source of truth; a second COD concept inside the Finance Gate would just create two mechanisms that could disagree.
  - **New live fields on `orders`** (curl + admin token, same workflow as every prior addition): `payment_method` (transfer|cash), `payment_timing` (upfront|terms — no cod), `payment_amount`, `payment_bank_ref`, `payment_due_date`, `payment_paid_at`. Finance's `orders.update` field grant extended to include all 6 (was `[payment_confirmed, payment_confirmed_at, cod_reconciled, cod_received_at]`). **Fixed a real TDZ bug introduced while adding these**: `schemas.ts`'s shared `numeric` union helper was declared *after* `OrdersCollectionSchema` in the file — referencing it from the new `payment_amount: numeric` field would have thrown `ReferenceError` on every module load (the whole app crashing on start). Moved the `const numeric = ...` declaration to the top of the file, above every schema that uses it (grepped all 13 usages to confirm none were before the new position). Caught and fixed before it ever reached `npm run build`, but worth flagging as a lesson: a same-file forward reference inside a schema object literal doesn't get caught by `tsc`, only by actually running the code.
  - **Real gating, not just a card**: `handleAdvance`'s cold branch now checks `payment_confirmed` — if weighing finishes before Finance clears, the order goes to a literal `finance` stage (parks there) instead of skipping straight to `production`, matching the prototype's `normalTarget = cleared ? 'production' : 'finance'` exactly; the advance button's label flips to "Release to Finance" while unpaid. Previously (yesterday's version) `payment_confirmed` was purely informational — Cold Storage could always advance straight to Production regardless of Finance's state, which turned out to contradict the port's own "Finance is clearing payment in parallel" copy once Cold actually finished first. The Finance gate card now renders at both `cold` and `finance` stages (`showFinanceGateForm`); clearing while parked at `finance` advances the order to `production` in the same write, clearing while still at `cold` doesn't move the stage (weighing's own advance button decides that later) — same split as the prototype's `case 'cold'` vs `case 'finance'`.
  - **Undo reaches back across stages**: mirrors the prototype's `undoClearance()` (`Dev-OrderDetail.jsx:325-330`) exactly — un-clearing while still at `cold` just resets the flags in place; un-clearing after the order already advanced (production/packing/finalise) drops the stage back to `finance` to be re-checked. Available to anyone with `approveFinance`, not just whoever originally cleared it (matches the prototype's `canUndoClear`), and only while the order is still in `[cold, finance, production, packing, finalise]` — port has no separate `order.hold` boolean like the prototype (`handleHold` here just sets `stage: 'outstanding'`), so the stage whitelist alone already excludes on-hold orders.
  - **`canWeighHere` tightened to explicitly exclude Finance** even if a Finance user is ever granted `helpOtherStages` via Settings — matches the prototype's `role !== 'Finance' && can(role, 'helpOtherStages')` guard (`Dev-OrderDetail.jsx:171`): at cold, Finance's only job is ever the Finance gate card, never the weigh controls, so the two can't overlap for one role even under a broad grant.
  - **Terms-invoice follow-up, delivered orders**: a Terms-timing order that reaches `delivered` with `payment_paid_at` still null gets a third row in the existing Follow-ups pending card ("Terms invoice — payment not yet received" + due date), "Payment received" writes `payment_paid_at`; once paid, a green confirmed card (same visual treatment as "Signed DO & SI returned") appears with a working "Undo" reverting just that one field. Gated on `approveFinance`, matching the prototype's Finance/Owner/`actFinanceGate` gate.
  - **Restore Order fix for `outstanding` orders — a real, previously-undiscovered bug**: `handleRestore` always used `order.cancelled_from ?? "intake"` as the target stage. `cancelled_from` is only ever set by `handleCancel` — an `outstanding` order (COD shortfall, goods already delivered) was never cancelled, so restoring one silently fell back to `"intake"`, wiping an already-fulfilled order back to square one. Now: `isOutstanding` orders restore to `dispatch` instead (a genuine redelivery/re-collection attempt), with the same hand-off-field reset (`taken_by`/`pickup`/`third_party`/`courier_service`, delivery-proof archiving) `handleSendBack`'s delivered→dispatch reopen path already established — otherwise the hand-off chooser would never reappear. `cancelled`-order restore is unaffected.
  - **Follow-up fix, same day**: the Finance gate rebuild above didn't account for `showActorNotice` ("This order is currently with Warehouse.") — that notice gates on `!canAdvance` (stage-advance capability only), so a Finance user viewing a `cold` order saw it sitting right above their own Finance gate card, implying they had nothing to do when they actually did. The prototype's equivalent `canAct` explicitly includes Finance at `['cold', 'finance']` (`Dev-OrderDetail.jsx:126`) precisely so this doesn't happen. Added the missing carve-out (`hasParallelFinanceAction`) so the notice is suppressed whenever the viewer has `approveFinance` and the order is at `cold`/`finance`, matching the prototype. Caught by the user spotting the stray copy live, not by code review.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live schema/ACL changes verified via curl before and after, same as every prior addition. Not yet browser-tested — see the Current Goal note above.

- **Floor-helper (`helpOtherStages`) gap swept across cold/production/packing (2026-08-14):** the prototype gates all 4 floor stages uniformly under one `canAct` that includes `helpOtherStages` (`Dev-OrderDetail.jsx:122`) — so Admin, as the floor helper, can cover any of them. This port had already split that into `canAdvance` (correctly `helpOtherStages`-inclusive, `OrderDetail.tsx:627`) plus a separate cold-only extra-control gate that wasn't.
  - **Real bug, fixed**: `canWeighHere` (`stage === "cold" && auth.can("weighColdStorage")`) gates the weight-input row, the per-line weighing-photo camera, and the item-condition-photo camera — all three excluded Admin-as-helper, since `weighColdStorage` is a Warehouse-only capability with no `helpOtherStages` fallback (unlike `canAdvance`). Added `|| auth.can("helpOtherStages")`, matching `canAdvance`'s own pattern — Admin covering someone else's cold-storage queue can now actually weigh and attach photos, not just click the stage-advance button.
  - **Swept production and packing for the same shape of gap — found none**: `cutProduction`/`packWarehouse` are referenced *only* inside `STAGE_FLOW` (i.e. only via `canAdvance`, already helper-inclusive) — grepped the whole file to confirm neither capability gates any second, separate control the way `weighColdStorage` gated weighing. The item-condition/weighing-photo buttons are the *only* photo controls in the shared item-list loop and are cold-only by design (photos are a cold-storage weighing artifact in the prototype, not a production/packing action) — so production/packing correctly show no photo button for anyone, at any capability. Packing has no controls beyond the shared advance button. Confirmed, not changed.
  - **Cut-completion — a real but separate feature gap, flagged not built**: `line_cuts.done` exists in the live schema (`schemas.ts:231`) mirroring the prototype's `cut.done`, but `OrderDetail.tsx`'s cutting-instructions block (`~2838`) only ever renders `c.text` — no read of `.done`, no toggle, no write path anywhere in the file. This is a missing interaction, not a permission-gating bug (there's nothing to gate), so per the sweep's own instruction it's reported here rather than silently added. If a per-cut "mark done" control is wanted at production, it's new scope: a toggle UI plus an `updateLineCut()` write, not a `canAdvance`/`helpOtherStages` fix.
  - `tsc -b` ✓, `eslint` ✓, `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **QA feedback batch — 4 Dashboard bugs, Orders back-nav filter loss, order edit lock gate, table-wide sortable-header redesign (2026-08-14):** every item traced to an exact file:line cause in both this port and the reference prototype (`IPP-OrderFlow-COMPLETE-2026-07-05/IPP-OrderFlow/src/Dev-*`) before fixing — several turned out to be real, previously-undiscovered gaps rather than cosmetic issues.
  - **Price leaking into notifications and OrderDetail's own History card, despite `seePrices` — took 3 rounds to fully close, each round caught live by the user in their own browser:**
    1. **First fix**: `useNotifications.ts` rendered `order_history.what` raw. Masked `Rp`-formatted figures (COD reconcile / delivered / outstanding COD) with a regex.
    2. **Second fix**: the mask missed `OrderEdit.tsx`'s line-price-change summary (`` `Line "${name}" price ${origPrice}→${newPrice}` ``) — no "Rp" prefix at all, so the Rp-only regex never matched it. User caught this live (screenshot showed raw "price 0→50000" still visible to a Warehouse... which led to a false alarm, see below).
    3. **Scope correction**: briefly (wrongly) concluded Warehouse/Production's `seePrices: true` default itself was the bug and asked the user before touching `domain.ts` — user corrected: only Courier should lack `seePrices`; Warehouse/Production keep it, no capability changes needed. The real, still-open bug was that masking wasn't the right treatment at all.
    4. **Final fix, per explicit product decision**: replace masking with full removal. A `price A→B` clause reads as obviously-censored either way ("price •••→•••" still shouts "a price was here"), so the whole clause is now stripped from the message instead of blanked — new shared `src/lib/redactHistory.ts` (`redactHistoryPrices()`), used by both `useNotifications.ts` (was inline before) and, newly discovered in the same conversation, **`OrderDetail.tsx`'s own right-sidebar History card** (`~line 4274`), which rendered `order_history.what` completely raw with no `seePrices` check at all — a second, separate leak surface never caught by the first two rounds because nobody had looked at that panel yet. Splits an "X — a; b; c" summary on the em-dash, filters out any `Line ".*" price [\d.,]+→[\d.,]+` clause, and — since a purely-price-only edit then has zero clauses left — returns `null` to signal "drop this entry entirely" rather than show a hollow "Edited — " with nothing after it. Rp-formatted figures (COD messages, which aren't clause-lists) still get masked, not dropped — removing "Rp" from "Delivered — COD ␣ collected" would read as broken, whereas masking still reads as a complete sentence. Verified via `node -e` against the user's exact two-entry screenshot before shipping — output was cleaner than the user's own manual mock-up (no dangling `Line "X"` label left over from a partial removal).
    - `tsc -b` ✓, `eslint` ✓, `npm run build` ✓ after every round.
  - **Quick-action cards didn't match the prototype's per-role set**: `domain.ts`'s `ALLOW.Admin` granted `viewDeliveryRun: true` — in the prototype "My deliveries" is Courier-only. Since `viewDeliveryRun` gates both the Dashboard card and the actual `/deliveries` route, removing the one default grant fixed both consistently. `viewPickList`/`reconcileCOD` already matched the prototype exactly.
  - **"Replacement in Transit" tile wasn't visually distinct for Admin**: `ROLE_FOCUS.Admin` already included the bucket (so the pill was technically "highlighted"), but `statusColor()` deliberately returns neutral grey for it (the bucket spans 4 roles, so no single role's colour was ever assigned — see `RETURN_BUCKET_ACTOR`'s doc comment) — indistinguishable from unhighlighted. `ReturnWorkflowsPanel.tsx` now special-cases this one bucket to `var(--state-error)` locally in its own render, matching the prototype's flat danger-red treatment, without touching the shared `statusColor()` function — verified this can't bleed into a replacement order's own `StatusPill` (which still colours by real current stage + the separate `isReplacement` badge), since `statusColor()` is only ever called elsewhere with real `stage` values, never a return-bucket key.
  - **Finance approval action was entirely missing — a real, previously-undiscovered gap**: `payment_confirmed` was never written anywhere in the frontend, despite the `approveFinance` capability, a `finance` entry in `OrderDetail.tsx`'s `STAGE_FLOW`, and a "Needs Attention" digest bucket all existing. (Verified this wasn't a count/filter mismatch like past bugs — `useDashboardCounts.ts`, `useOrders.ts`, and `useAttentionItems.ts` all already correctly use `financeParallelQueueFilter()` rather than a naive `stage==='finance'` match, since `stage` deliberately never becomes `'finance'` in this port — Cold Storage and Finance are parallel gates, `stage` stays `'cold'` throughout.) Built the missing action: new live `orders.payment_confirmed_at` field (nullable timestamp, added via curl + admin token, mirroring the `cod_received_at` precedent) with Finance's `orders.update` grant extended to include it (was `[payment_confirmed, cod_reconciled, cod_received_at]`, now adds `payment_confirmed_at`; Admin/Owner already cover it via `fields: ["*"]`/`admin_access`). New "Finance gate" card on `cold`-stage orders (`showFinanceApproveRow`, gated on `approveFinance`) with an "Approve Payment" button (`handleApproveFinance`) writing the flag + timestamp and logging "Payment cleared — Finance". Also added an Undo the prototype has but this port initially lacked: a quiet "Undo payment clearance" link (`showFinanceUndoRow`, same capability, no stage restriction) that flips the flag back — simpler than the prototype's own undo (which has to move `stage` back to `'finance'` if the order already advanced; inapplicable here since `stage` never becomes `'finance'` in the first place, so flipping the boolean is a fully self-contained, safely-repeatable write with no snapshot needed).
  - **Orders page lost its stage/search filter on Back navigation**: `OrderRows.tsx` (shared by `Orders.tsx` and the Dashboard's Open Orders panel) passed only `location.pathname` as the Back-button target, dropping the `?stage=...&search=...` query string `Orders.tsx` keeps in its URL. One-line fix: `location.pathname + location.search`.
  - **Order edit lock gate (`editAfterLock`) — scaffolded but never wired up**: the capability existed in `domain.ts` (type union, `CAPABILITIES`, `PERMISSION_GRID` label) but no code ever called `auth.can("editAfterLock")`; the actual `canEdit` gate only checked cancelled/delivered, missing the prototype's `hasLeftWarehouse` lock (delivered, or dispatch-and-actually-taken — not merely sitting at dispatch unassigned) and the `outstanding`/`returned` cases entirely. New shared `isOrderLocked()` in `pipeline.ts` (ported byte-for-byte from the prototype's `hasLeftWarehouse()` + its call site's extra terminal-stage check, re-verified against the actual prototype source directly, not a paraphrase). Wired into both `OrderDetail.tsx`'s Edit-button gate and `OrderEdit.tsx`'s self-guard: `!isOrderLocked(order) || auth.can("editAfterLock")` — Owner passes automatically via the existing Owner short-circuit in `can()`.
  - **Table sort UX replaced across all 4 tables** (Dashboard Open Orders panel, Orders, Customers, Products): new shared `src/components/SortableTh/` — a `<th>` that sorts on click (ascending first, descending on a second click) instead of a separate dropdown-button, hover state, and — only on the active column — a single `circleArrowUp` icon (already registered in `icons.ts`, confirmed by direct read) rotated 180° via CSS `transform` transition for descending, rather than swapping between two icon assets. Wired into all 4 tables' existing server-side `sort` params (no hook changes needed — they already accepted an arbitrary Directus field string). The Items column (Dashboard/Orders) has no backing DB column (a joined line count), so it sorts client-side on just the current page, shadowing the server sort rather than being sent to it. Customers/Products previously had no sort mechanism at all — added `sortBy` state feeding their existing hardcoded `sort: ['name']` query param. Also flipped the actual default-sort *state* (not just the header's icon) from descending to ascending in 3 places that still defaulted to `"-no"` (`Orders.tsx`, `Dashboard.tsx`, `OpenOrdersPanel.tsx`'s own prop default, plus `useOpenOrders.ts`'s fallback) — a mismatch here would've shown an ascending icon on a still-descending first query.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live schema/ACL change verified via curl before and after. Not yet browser-tested — see the Current Goal note above.

- **In-app WhatsApp-message intake flow restored, Dashboard triage panel kept removed (2026-08-14), per explicit user correction:** the 2026-08-11 removal (see the entry lower down) had conflated two different things under one "WhatsApp intake" umbrella — the user clarified their original intent was only to remove the Dashboard's automated triage panel (`IntakePanel`, which read raw incoming messages from the Evolution API → n8n → Directus pipeline), not the admin-initiated paste-and-parse flow. Restored everything from the 2026-08-11 removal **except** `IntakePanel`/`useIntakeMessages`/`readMessages`/the `viewIntakePanel` capability/the `messages` schema — those stay gone.
  - **Restored components**: `src/components/ChannelSelectModal/` (step 1 — pick Horeca/Meatfellas, Meatfellas still disabled/"Soon") and `src/components/IntakeModal/` (step 2 — paste a message, POST to `/order-api/parse-order`, or "Skip — enter manually"), both reconstructed verbatim from the pre-removal commit (`5c46296~1`) since neither had any later revisions to reconcile.
  - **`OrderNew.tsx`**: restored the `prefill`/`rawText`/`attachments` router-state fields, the collapsible side panel (`isPanelOpen`, shows the original pasted message + any attachments), the `dateGuessed`/`multiCustomer` banners, per-line `parseStatus`/`learned` status chips, and the post-create `upsertCorrection()` + attachment-upload calls — merged into the current file rather than overwritten, so the delivery-proof/COD/undo-snapshot work built after the removal was untouched. Alert calls use the current `useDialog()` convention, not the pre-removal `window.alert`.
  - **`directus.ts`/`schemas.ts`/`types/directus.ts`**: restored `parseOrderText()` (+ `ParsedOrderDraft`/`ParsedOrderLine` types), `readCorrections()`, `deleteCorrection()`, `aggregateCorrections()`, `upsertCorrection()`, and `CorrectionsCollectionSchema`/`CorrectionsCollection`. Deliberately did **not** restore `readMessages()`/`MessagesCollectionSchema` — panel-only, stays removed.
  - **Dashboard.tsx / Orders.tsx**: "New Order" goes back to opening the channel-select → intake modal flow (`orderStep` state machine) instead of navigating straight to `/orders/new`. Dashboard's panels grid stays 2-column (Return Workflows | Needs Attention) — the `IntakePanel`/`viewIntakePanel` gate was not restored, so this is unaffected by the rest of the restore.
  - **Settings.tsx / useSettings.ts / new `useCorrections.ts`**: restored the "Intake Learning" section (learned-matches count card + reviewable/deletable corrections list), using the current file's `useDialog()` `confirm`/`alert` instead of the pre-removal `window.confirm`/`window.alert`.
  - **i18n**: re-added the ~20 keys the 2026-08-11 removal pruned that are actually used by the restored code (`Parse & Continue`, `Parsing…`, `WhatsApp Intake`, `recognized`/`probable`/`unrecognized`/`learned`, `Intake Learning`, the corrections-review strings, etc.). Left several other pruned keys unrestored on purpose — grepped first and confirmed they're genuinely orphaned, belonging to an unrelated already-removed "copy order confirmation to WhatsApp" clipboard feature, not this flow.
  - **CSS**: restored `OrderNew.module.css`'s side-panel/layout-grid/status-chip/pre/attachments-note rules and the 992px collapse media query; restored `Settings.module.css`'s `.correctionsCard`/`.correctionRow`/etc. (left the `.checkbox`/`.checkboxOn` rules removed — those were superseded by the new `Checkbox` component, unrelated to intake).
  - **Backend note, unchanged**: the `/order-api/parse-order` service and the `corrections` table were never touched by the 2026-08-11 removal (frontend-only scope both times) — `VITE_INTERNAL_TOKEN`/`VITE_DIRECTUS_URL` are already configured in `.env`, no backend work needed.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓ (381 modules, up from 376). **Not yet browser-tested** — the full paste → parse → prefill → create → learned-correction cycle needs a real walkthrough, including confirming the `corrections`/`messages` Directus collections and their ACLs are still live and unchanged since 2026-08-11.

- **`useCashUp.ts` now reflects partial COD collections and surfaces `outstanding`-stage orders (2026-08-14), closing the follow-up flagged from the COD-outcome rework below:** previously the Cash-up queue only queried `stage IN ('dispatch', 'delivered')` and always priced each row from `order_lines` (qty × price), so a short/zero COD delivery — which `OrderDetail.tsx`'s `handleConfirmDelivery` now routes to `stage: 'outstanding'` with the courier's actual take recorded in `delivery_proofs.cash_collected` — never showed up in the queue at all, and a `delivered` order with a partial collection would've shown the full order total instead of what was actually collected.
  - `useCashUp.ts`: added `'outstanding'` to the stage filter; added a `readDeliveryProofs(orderIds)` fetch alongside the existing `order_lines`/`customers`/`users` calls, building a `collectedByOrder` map from each order's current (non-archived) proof's `cash_collected`. Each row's `amount` now prefers that collected figure, falling back to the `order_lines` total only when no proof has been recorded yet (e.g. an order still sitting at dispatch, pre-confirm) — per the original spec note in the file's own doc comment.
  - Deliberately scoped to the cash actually in the courier's hand, not the customer's remaining balance: an `outstanding` order's uncollected remainder is a receivable this queue doesn't chase (no such UI exists yet) — only the courier's partial collection gets reconciled here, same "Confirm" write as before.
  - `CashUpOrderRow` gained `isOutstanding` (from the order's `stage`, now included in the `readOrders` `fields` list); `CashUp.tsx` renders a small amber "Partial — balance outstanding" badge next to the order number when true, reusing the `--state-warning` `color-mix()` pattern `OrderDetail.tsx`'s COD segment control already established, so a partial-collection row can't be mistaken for a full one at a glance. New i18n key: `"Partial — balance outstanding"`.
  - `tsc -b` ✓, `eslint` ✓, `npm run build` ✓. Not yet browser-tested — see the Current Goal note above.

- **Drop-location distance ("~Nm") + real address-pin source, replacing the "Delivery location captured" fallback with a verification distance (2026-08-13):** the delivered-order banner's drop-location line previously always showed the generic fallback because no customer/order address had a geocoded reference point to compare `deliver_geo` against — flagged and deliberately left un-fabricated earlier in this session (never printing a guessed "~40m"). Built the real reference point instead of live-geocoding the free-text address: a free-text Indonesian Horeca address geocodes to a street centroid, not the real door, which would make "~40m" look authoritative against an unreliable anchor, plus it needs a billed API key exposed client-side (this project's env vars are all public-by-convention). A stored pin is ground truth, set once, free forever.
  - **New live schema**: `customers.address_geo` — nullable JSON `{lat, lng}`, no `at` timestamp (new `LatLngSchema` in `schemas.ts`, distinct from `GeoStampSchema` — this is a fixed reference point, not a dated event). Admin already had `customers.update` `fields: ["*"]`, no change needed. New field-scoped grant: Courier gets `customers.update` limited to `["address_geo"]` only (needed for the bootstrap prompt below; Courier still can't touch name/credit-limit/etc. on a customer).
  - **Bootstrap path, not manual-entry-required**: on a successful own-courier delivery (`handoffMode === "delivery"`) with a real GPS fix and a matched customer that has no `address_geo` yet, `handleConfirmDelivery` offers a one-tap confirm ("Set this as {customer}'s delivery location?") and writes that delivery's real `deliver_geo` as the customer's pin. Most customers get a real, accurate pin automatically from ordinary delivery activity — no dedicated data-entry effort. Only offered once per customer (skipped once a pin exists) and only when there's a real fix to offer (skipped for pickup/3rd-party hand-offs or a denied/failed GPS capture).
  - **Manual path for first-time/correction cases**: `CustomerEdit.tsx` gets a "Delivery Location Pin" text field accepting either a plain `"lat, lng"` paste or a pasted Google Maps URL (`parseLatLng()` matches the `@lat,lng` and `?q=lat,lng` URL forms, falls back to the plain-pair form) — invalid input blocks save with an inline error rather than silently discarding it. Deliberately a text paste field, not an interactive map picker — this app has no map-library dependency anywhere (existing convention is keyless static Maps links/embeds only), and a picker would be new UI-library scope for what's meant to be a rare correction path.
  - **Distance calc**: new `haversineMeters()` in `OrderDetail.tsx`; `dropDistanceM` derived value computes it whenever both `order.deliver_geo` and `matchedCustomer.address_geo` are present, else `null`. The banner renders `"Dropped at delivery address · ~{N}m · HH:MM"` when a distance exists, otherwise keeps the original `"Delivery location captured · HH:MM"` fallback — never a fabricated figure. Raw lat/lng stays behind the existing "Map" link only, never printed inline.
  - `context/schema/snapshot.json` regenerated live to include the new field/permission.
  - `tsc -b` ✓, `npm run build` ✓, `eslint` ✓ (same pre-existing unrelated `AddItemModal.tsx` error, untouched). Not yet browser-tested — see the Current Goal note above.
- **Delivered-order redesign to match the Figma mockup — GPS drop-location stamp, quiet Undo (distinct from Reopen), Follow-ups pending card, read-only Items on delivered orders, "Delivered & Closed" banner drop-location row (2026-08-13):** implements `context/designs/OrderDetail.png`'s target layout for `stage === "delivered"` orders in `OrderDetail.tsx`, reusing only existing primitives (`Card`, `Button`, `Icon`, `StatusPill`) — no new components.
  - **New live schema** (curl + admin token, live-verified after each write): `orders.pickup_geo`, `orders.deliver_geo`, `orders.undo_snapshot` — all nullable JSON; `orders.cod_received_at` — nullable timestamp. Courier's `orders.update` field-scoped permission extended to include `pickup_geo`/`deliver_geo`/`undo_snapshot`; Finance's `orders.update` extended to include `cod_received_at`. New `GeoStampSchema` (`{lat, lng, at}`) and `UndoSnapshotSchema` (`{prevStage, changedFields, proofId, who, at}`) in `schemas.ts`, exported as `GeoStamp`/`UndoSnapshot` types.
  - **GPS drop-location stamp, best-effort and never blocking**: `captureGeoStamp()` wraps `navigator.geolocation.getCurrentPosition()` in a `Promise` that resolves `null` on missing API, denial, or timeout (8s) rather than rejecting — every call site treats "no geo" as a normal, silent outcome, never an error path. `pickup_geo` captures at condition-photo upload (delivery mode only); `deliver_geo` captures inside `handleConfirmDelivery`. Both follow the file's existing "staged locally, committed at confirm" pattern — held in React state, only written in the same `updateOrder()` call that advances the stage, so an abandoned attempt never leaves a partial geo write. No distance-to-address figure is computed or fabricated (no geocoded reference point exists anywhere in the schema for a customer/order address) — the banner always reads "Delivery location captured · HH:MM" plus a "Map" link (reuses `Deliveries.tsx`'s `mapsUrl()`-style `google.com/maps/search` pattern), never a fabricated "~40m".
  - **Undo, a second and distinct tool from the existing "Re-open to Dispatch"**: Reopen is a deliberate re-delivery (resets fulfilment, clears `docs_returned`/`cod_reconciled`, stays in the bottom actions, logs "Stage returned: delivered → dispatch") — unchanged. Undo un-happens a *mistaken* close: restores the exact pre-delivery snapshot (`stage`, `delivered_at`, `pickup_geo`, `deliver_geo`) written to `undo_snapshot` at the moment delivery was confirmed, archives the delivery-proof row that was created, and logs the distinct "Undid — back to dispatch". Gated to the acting user or Owner (`canUndo = order.undo_snapshot.who === userId || auth.role === "Owner"`) and to being the *last* action on the order — every other order-mutating handler (`handleSendBack`'s reopen path, `handleConfirmDocsReturned`, `handleReconcileCOD`, `handleCancel`, `handleHold`) now explicitly clears `undo_snapshot: null` in its own write, so a non-null snapshot reliably means "nothing has happened since delivery was confirmed." Rendered as a quiet text link (not a button) directly under the "Delivered & Closed" banner.
  - **Follow-ups pending card replaces the old standalone "Signed DO & SI returned?" card**: groups two office loose-ends as quiet rows inside one `Card` instead of one full-width primary-button card — COD-reconcile row (`showCodRow = stage === "delivered" && isCodOrder && !order.cod_reconciled && auth.can("reconcileCOD")`, cash icon, full-Rupiah sub-line "collected by courier", secondary "Confirm received" button) and DO/SI-returned row (`showDocsRow`, file icon, secondary "Mark returned" button — same `handleConfirmDocsReturned` write as before, just relocated/restyled). Card renders only when at least one row applies, disappears once both resolve. New `handleReconcileCOD()` performs the identical write `useCashUp.ts`'s own Cash-up "Confirm" button makes (`cod_reconciled: true`, `cod_received_at: now`) — a second entry point for the same action, not a parallel mechanism, so the two screens can never disagree about whether an order's cash is settled.
  - **Items card read-only on delivered orders — explicit product decision, not a literal Figma port**: the Figma mockup shows live weighing inputs/camera/"Add weighing" on a delivered order, but this reuses the cold-stage item component in a delivered frame as a mock artifact, not a real spec intent, and directly contradicts the `canWeighHere` stage-gate fixed earlier this session. Confirmed with the user before building: a delivered order is a closed, already-invoiced record — final weights render as plain text + photo thumbnails (thumbnails still open the read-only image modal), with no input, camera, "Add weighing", or delete control. A genuine post-delivery weight correction must go through the reopen/edit path (which resets state and logs history), never an inline edit on a closed order.
  - **"Delivered & Closed" banner extended, not rebuilt**: the existing single-banner replacement for the 8-step stepper (built earlier this session) now also shows the drop-location row (pin icon + human-readable text + "Map" link) when `deliver_geo` is present, above the existing "Received by {name}" line.
  - **Deliberately deferred**: the spec's "ghost Undo (reconcileCOD only)" sub-feature — see the Current Goal note above.
  - `tsc -b` ✓, `npm run build` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` `react-hooks/set-state-in-effect` error remains, not touched this session). Not yet browser-tested — see the Current Goal note above.

- **Delivery Proof COD payment reworked from a blocking checkbox into a recorded outcome (2026-08-13):** previously `proofCod` was a boolean checkbox that `handleConfirmDelivery` never even read — a COD order could be marked delivered with cash uncollected, and hard-gating on it instead would have tempted couriers to falsify "collected." Fixed by separating "goods changed hands" (still gates the Confirm button — photos + receiver name, unchanged) from "cash was collected" (never gates it — only changes how the delivery is recorded).
  - **New live schema**: `delivery_proofs.cash_collected` — nullable numeric, Rupiah amount actually collected. Live-verified Courier's existing `delivery_proofs` create/update permissions are unrestricted (`fields: ["*"]`), so no ACL change was needed.
  - **New state model**: `codOutcome: "full" | "partial" | "none" | null` (+ `partialAmountInput`, `outstandingReason`) replaces the single `proofCod` boolean. `codAmount` reuses the order-total calc already in this file (`orderTotal` — same qty×price sum `useCashUp.ts` independently computes, since neither `orders` nor `delivery_proofs` has a dedicated COD-amount column). `codApplies = isCodOrder (customers.pay_timing === 'cod') && handoffMode !== 'third'` — the row doesn't render for non-COD orders or 3rd-party hand-offs (the courier service owns that hand-off's cash).
  - **`handleConfirmDelivery` branches by outcome, never blocks on it**: not-yet-recorded (COD, `codOutcome === null`) → a click-time alert asking the courier to pick an outcome ("none" is always available, so this is a nudge, not a gate satisfiable only by "full"); short/zero (`codOutcome !== "full"`) → also requires a one-tap reason (`OUTSTANDING_REASONS`: will transfer / paid part / no cash on site) before proceeding, then writes `stage: "outstanding"` (the same state `handleHold()` already uses) with a descriptive `order_history` entry including the shortfall and reason; full → `stage: "delivered"` as before, with a COD-specific history entry. `delivered_at` is written in every branch — the goods still changed hands at that moment regardless of the cash outcome.
  - **Restyled to match the photo rows**: the COD row moved inside the same `.proofsContainer` as condition/receiver/signed (previously a separate `.proofCod` block after it), using the identical `.proofFieldRow` shell (stateful border, `check` icon empty/filled) so it reads as one of the group. Right side is a 3-segment outcome control (`.codSegment`, green on Full, amber on Partial/None) instead of a checkbox, a `color-mix()`-tinted "Collect COD {amount}" chip (reusing the existing `Collect COD` i18n key) shows the amount owed, and a confirmation line + reason chips appear once an outcome is picked. All new CSS reuses existing tokens (`--state-success`/`--state-warning`/`--border-subtle` etc.) via the same `color-mix(in srgb, ${color} 13%, transparent)` pattern `StatusPill.tsx` established — no new colour values. Removed the now-unused `.proofCod` CSS class and the `Checkbox` import (no longer used anywhere in this file).
  - **`useCashUp.ts` needs a follow-up, not yet done** — see the Current Goal note above.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-tested (see Current Goal).
- **OrderDetail's pipeline stepper no longer shows "Delivered" as pulsing/in-progress once the order is actually delivered (2026-08-13):** the stepper's `isActive`/`isCompleted` logic treated the *current* stage as always "active" (hollow dot, `pulseGlow` animation, muted label) — correct for a genuinely in-progress stage like `packing`, but wrong for `delivered`, which is terminal. Added an `isTerminalDelivered` check (`s.key === "delivered" && stage === "delivered"`) that routes that one case to the "completed" styling instead (filled `--accent-primary` dot, no pulse, `stepLabelCompleted`) — `isActive` is now `false` whenever this applies, since `.stepDotActive`'s hollow/pulse rules are defined after `.stepDotCompleted`'s in the stylesheet and would otherwise still win when both classes are applied together.
- **Delivery Proof rebuilt to match `context/designs/delivery-proof/` — staged multi-photo capture, attempt archiving, read-only confirmed record (2026-08-11):** replaces the old single-photo-per-slot, everything-visible-at-once form in `OrderDetail.tsx`'s dispatch-stage delivery-proof Card.
  - **New live schema/ACL** (curl + admin token, live-verified after each write, per established habit): `attachments.proof_id` — nullable UUID, M2O relation registered to `delivery_proofs.id` — links a proof photo attachment to the delivery_proofs row (attempt) it belongs to; `attachments`'s `doc_type` gets 3 new values (`cond`/`recv`/`signed`) alongside the existing document types. Granted Courier `create` on `attachments` (was read-only) — Courier already had create/read/update on `delivery_proofs`, confirmed live before assuming it. No new `orders` column needed.
  - **Attempt archiving reuses schema that already existed but was dead code**: `delivery_proofs.archived` and `readDeliveryProofs()` (excludes archived by default) existed live/in-code but `readDeliveryProofs()` was never called anywhere. Now wired up for real: `readDeliveryProofs([orderId])` loads the current (non-archived) attempt on page load; a new `updateDeliveryProof()` marks a superseded attempt `archived: true` instead of deleting it — reproduces the prototype's `proofLog` "evidence is never deleted" guarantee as a real relational model (row-per-attempt + archive flag + `proof_id` FK) rather than a copied counter.
  - **New `archiveDraftAttempt()`**: called by "Change method" and the new "Delivery failed — bring back & retry" action *before* resetting the hand-off fields — if anything was staged (photos and/or receiver name), it creates a `delivery_proofs` row for that abandoned attempt, immediately archives it, and links every staged photo to it via `proof_id`. A hand-off reset with nothing staged yet creates no row (nothing to archive).
  - **Staged capture is local-only until confirm**: `condPhotos`/`recvPhotos`/`signedPhotos` are now arrays (not single `{fileId,url}`), uploaded to Directus Files immediately but not attached to any DB record until `handleConfirmDelivery` or `archiveDraftAttempt` runs — matches this specific form's pre-existing local-staging convention (unlike the weighing/refuse-photo flows elsewhere in the same file, which persist each photo immediately; this form never did, and multi-photo support didn't change that). Hover-trash on a staged thumbnail is a pure local-array removal, no API call.
  - **Visual rewrite per the 4 reference designs**: condition photo is the only field shown until it has ≥1 photo (design 01); once it does, receiver/signed rows + "Change method" (now `tertiary` + `reload` icon, top-right inline with the header) + the 3 action buttons reveal (design 02/03) — `Confirm delivery` (icon `tick`, disabled until required fields are filled), `Customer refused / returned` (icon `returned` — moved in from a standalone always-visible button elsewhere in the stage-actions row, now scoped to an in-progress capture like the design shows), `Delivery failed — bring back & retry` (icon `cancelled`, new — the i18n key already existed, orphaned, before this build). Each field row gets a `check` icon (grey/`--text-muted` empty, `--accent-primary` filled) per design 03. A "Taken by X on {weekday day month year  HH:mm}" line shows for own-courier hand-offs, sourced from the most recent `Handover: own courier` `order_history` entry during capture, or `delivery_proofs.created_at` once confirmed.
  - **New read-only "Delivery proof" card** (design 04) renders whenever a confirmed (non-archived) `delivery_proofs` row exists for the order — independent of `stage`, so the record persists after the order leaves dispatch. Reuses the existing `thumbnailsContainer`/`thumbnailItem`/`thumbnailHoverTrash` hover-delete pattern (already established for weighing/refusal photos) and the existing `activeImageModal`/`handleDeleteDocument` click-to-enlarge-and-delete path via `attachmentId`, rather than inventing a second image-viewer.
  - New i18n: `"on"`. Reused existing `"Taken by"` (capitalization differs slightly from the reference design's lowercase "taken by") rather than adding a near-duplicate key.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. **Not yet browser-tested** — no Playwright/browser-automation tooling in this environment; the whole capture → confirm → archive → read-only cycle needs a real walkthrough (see Current Goal).
- **Fixed "Delivered Orders" metric showing 0 under the Today filter despite a same-day delivery (2026-08-11):** root cause — `handleConfirmDelivery` in `OrderDetail.tsx` flipped `stage` to `"delivered"` but never wrote `orders.delivered_at`, and `useDashboardCounts.ts`'s Today/date-range filter for the Delivered metric filters on exactly that field. The order showed up under "All time" (no date filter) but never under any date-scoped range. Fixed by adding `delivered_at: new Date().toISOString()` to that same `updateOrder()` call. Live-verified Courier's `orders.update` ACL already includes `delivered_at` in its field list (added in an earlier session, apparently in anticipation of this — just never used) — no ACL change needed.
- **Fixed "Replacement in Transit" always showing Warehouse's colour regardless of the replacement order's actual current stage (2026-08-11):** the bucket's filter (`is_replacement: true`, stage not in delivered/cancelled) spans every stage, not just Cold Storage, so a replacement sitting at Finance or Dispatch was still painted Warehouse-red — a real mismatch between the colour and who actually holds the order. Removed `replacement_transit` from `RETURN_BUCKET_ACTOR` in `lib/pipeline.ts` (now `Partial<Record<...>>`) so the Dashboard's aggregate Return Workflows tile falls back to neutral for that bucket instead of claiming a single role's colour it can't actually promise. Added a proper per-order fix instead: `StatusPill` gained an `isReplacement` prop that renders a small neutral "Replacement" badge next to the pill (icon `reload`) without touching the pill's own colour — the pill still colours by `statusColor(order.stage)` (whichever role currently holds it), the badge just flags "this is a re-send." Wired `order.is_replacement` → `isReplacement` through `useOpenOrders.ts`/`useOrders.ts` (`OpenOrder.isReplacement`, new `is_replacement` field in both hooks' `fields` arrays — live-verified already present in every role's `orders.read` ACL, no grant needed) and `CustomerDetail.tsx`'s own inline order list. `tsc -b` ✓, `eslint` ✓, `npm run build` ✓.
- **Dashboard's role-owned stage pills (pipeline row + Return Workflows panel) now highlight in the owning role's colour, not a generic blue accent (2026-08-11):** per explicit user request, following up on the earlier `StatusPill` role-colour refactor — `StagePill.tsx` gained a `color` prop (pass `statusColor(stage.stage)` from `lib/pipeline.ts`, the same role-resolution function `StatusPill` already uses) applied via inline `style` (background tint, border, text — same `color-mix()` approach as `StatusPill.tsx`) when `highlight` is true; `ReturnWorkflowsPanel.tsx`'s pill buttons got the identical treatment inline (no shared sub-component existed to change once). Both also set a `--stage-color` CSS custom property so `.pillHighlight:hover` (in each component's own module CSS) can intensify toward that same role colour instead of the old hardcoded `--accent-primary` hover. Non-highlighted pills are unaffected — the ask was specifically for the highlighted (current-role-owned) ones. `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.
- **Dashboard notifications now load in batches of 20 as the popover is scrolled, instead of one eager fetch of 60 (2026-08-11):** `useNotifications.ts` rewritten to fetch `PAGE_SIZE = 20` rows per call (`offset`-paginated against `readOrderHistoryFeed`), accumulating into `byDayRef`/`labelByOrderIdRef` mutable refs across calls so re-renders don't refetch already-loaded pages. New `loadingMore`/`hasMore`/`loadMore()` in the hook's return shape. `NotificationsPopover.tsx`'s `.scroll` container gained an `onScroll` handler that calls `loadMore()` once scrolled within 80px of the bottom, plus a "Loading more…"/"No more activity." footer. Order-number label resolution (`readOrders` batch lookup) now only queries ids not already cached from a prior page, instead of re-resolving the same set on every batch. Trade-off worth noting: the bell's unread-count badge only reflects entries loaded so far (first 20 until the user scrolls) rather than the full recent history — acceptable given this is a small-team ops tool where the realistic unread backlog is rarely more than a page. `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.
- **New `Modal` component + `DialogProvider`/`useDialog()`, replacing every `window.alert`/`window.confirm` call app-wide (2026-08-11):** per explicit user request — "better make the alerts to modals... a modal component that can be used for all alerts."
  - **`src/components/Modal/`**: generic presentational shell (backdrop + card, optional title/footer), built to the baseline already documented in `ui-registry.md`'s "### Modal" section (overlay `rgba(0,0,0,0.4)`, `--radius-xl`, `--space-xl` padding, `--shadow-lg`, 600px max-width) rather than either existing ad-hoc modal's own styling (`AddItemModal`'s 0.6-opacity+blur backdrop, `ImageDetailsModal`'s dark photo-viewer chrome) — this one is the actual baseline implementation, not a third bespoke variant.
  - **`src/hooks/dialog-context.ts` / `DialogProvider.tsx` / `useDialog.ts`**: same 3-file split as `LanguageProvider` (context/provider/hook separated to satisfy `react-refresh/only-export-components`). `useDialog()` returns `{ alert(message, opts?), confirm(message, opts?) }` — both promise-based (`alert` resolves on dismiss, `confirm` resolves `true`/`false`), rendering through one shared `Modal` instance mounted once via `<DialogProvider>` in `App.tsx` (inside `LanguageProvider`, since button labels are translated; outside/around `AuthProvider`). A `queueRef` chains concurrent calls so a second `alert()`/`confirm()` fired while one is still open waits its turn instead of clobbering the pending one — matches `window.alert`/`confirm`'s own blocking behavior, which no caller in this codebase ever raced against.
  - **Replaced all 72 call sites across 7 files** (`Settings.tsx`, `CashUp.tsx`, `OrderDetail.tsx` — the bulk, 51 sites — `OrderNew.tsx`, `ProductDetail.tsx`, `ProductEdit.tsx`, `Products.tsx`): `window.alert(msg)` → `alert(msg)` (fire-and-forget, same as before — nothing in this codebase depended on `window.alert`'s blocking-until-dismissed semantics for control flow); `if (!window.confirm(msg)) return;` → `if (!(await confirm(msg))) return;` (every call site was already inside an `async function`, so no signature changes were needed). Destructive confirms (delete team member, delete product, delete document, cancel order, reset all permissions) pass `{ danger: true }` for a red confirm button. Where a local variable was already named `confirm` (`CashUp.tsx`'s `useCashUp().confirm`), the dialog's own `confirm` was destructured as `confirmDialog` instead to avoid shadowing.
  - New i18n key: `"OK"`. `"Cancel"`/`"Confirm"` already existed.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Not yet browser-driven end-to-end (no Playwright/browser-automation tooling installed in this environment) — every call site was individually read and converted, not scripted blind, but a manual click-through is still owed before calling this fully verified.
- **New reusable `Checkbox` component, sizes `sm`/`md` (2026-08-11):** `src/components/Checkbox/` — an icon-button checkbox (`role="checkbox"`, tick icon on a bordered square), not a native `<input>`, mirroring the existing `Toggle` component's shape/conventions (`label` prop is the accessible name, visually hidden; pair with visible copy beside it). Built from the Settings Roles & Permissions grid's checkbox button (previously a bespoke inline `<button>` in `Settings.tsx`), then applied everywhere else the app had an equivalent-but-separately-implemented checkbox: `Settings.tsx` (permissions grid, `md`), `PickList.tsx` (pulled-toggle, `md`), `ProductEdit.tsx` (catch-weight, `sm`, replacing a native styled `<input type="checkbox">`), `OrderDetail.tsx` (COD-collected on the delivery-proof form, `sm`, replacing an unstyled inline native checkbox). Each site's now-orphaned bespoke `.checkbox`/`.checkboxOn`/`.checkboxChecked`/`.checkboxIcon` CSS was removed from its page's own module (`Settings.module.css`, `PickList.module.css`, `ProductEdit.module.css`). Swept the rest of the app for `type="checkbox"` / `role="checkbox"` afterward — none remain outside the new component. `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.
- **Fixed the "expired token after the app sits open a while" bug (2026-08-11):** root cause was `directus.ts`'s `login()` calling `authClient.setToken(result.access_token)` right after `authClient.login(...)` — the SDK's `setToken()` unconditionally overwrites the *entire* stored auth record with `{access_token, refresh_token: null, expires: null, expires_at: null}`, wiping the refresh_token/expires_at that `login()` itself had just correctly written. From then on there was no refresh_token for the SDK's per-request lazy-refresh check (built into `rest()`'s `getToken()`, independent of the `autoRefresh: false` setting) to use, so once the original access token actually expired there was no recovery short of a full logout/login. Fix: removed the extraneous `setToken()` call — `authClient.login()` already awaits its own correct token storage write before resolving. Confirmed via direct read of the (minified) `@directus/sdk` source, not guessed. `tsc -b` ✓.
- **"Needs attention today" digest restricted back to Owner-only (2026-08-11):** was `role === "Admin" || role === "Owner"`; user clarified Admin should *not* see it. `showDigest = role === "Owner"` in `Dashboard.tsx`.

- **WhatsApp intake removed entirely from the frontend (2026-08-11), per explicit user request:** the in-app "paste a raw WhatsApp order message and auto-fill the form" flow, the Channel Select modal, the automated Evolution API → n8n → Directus draft-order dashboard panel, and the per-team "learned corrections" matching memory were all fully deleted — not hidden, not feature-flagged.
  - **Deleted files**: `components/ChannelSelectModal/`, `components/IntakeModal/`, `pages/Dashboard/sections/IntakePanel.tsx`(+css), `hooks/useIntakeMessages.ts`, `hooks/useCorrections.ts`.
  - **`directus.ts`**: removed `readMessages()`, `readCorrections()`, `deleteCorrection()`, `aggregateCorrections()`, `upsertCorrection()`, `parseOrderText()`, and the `ParsedOrderDraft`/`ParsedOrderLine`/`RawParsedOrderLine`/`RawParsedOrderDraft` types. `readAttachments`/`createAttachment`/`uploadFile` were kept — `attachments` is a genuinely shared collection (WhatsApp-sourced rows from n8n OCR *and* manually-logged documents both live there).
  - **`schemas.ts`/`types/directus.ts`**: removed `MessagesCollectionSchema`/`CorrectionsCollectionSchema` and their inferred types + array schemas. `AttachmentsCollectionSchema` kept.
  - **`domain.ts`**: removed the `viewIntakePanel` capability (had no `PERMISSION_GRID` row — was never Owner-toggleable, so no ACL/UI gap left behind).
  - **`OrderNew.tsx`**: removed the whole prefill-from-parsed-draft path, the side panel ("Original WhatsApp message" + attachments note), `multiCustomer`/`dateGuessed` banners, and the per-line recognized/probable/unrecognized/learned status chip. The layout collapsed from a 2-column grid-with-collapsible-panel to a single flat column. The free-text per-line matcher in `AddItemModal` (used by both `OrderNew` and `OrderDetail`'s edit mode) is unrelated/local and was explicitly kept untouched.
  - **`Dashboard.tsx`/`Orders.tsx`**: removed the `orderStep` channel-select/intake state machine; "New Order" now navigates straight to `/orders/new`. Dashboard's panels grid went from 3 columns (Return Workflows | Needs Attention | Intake) to 2 (Return Workflows | Needs Attention).
  - **`Settings.tsx`/`useSettings.ts`**: removed the "Intake Learning" section (learned-matches count + deletable corrections review list) and `aggregateCorrections`/`learnedMatches` from the settings hook.
  - **i18n**: pruned ~25 now-orphaned keys from `translations.ts` (Intake/WhatsApp-paste/parse-status/learned-match strings) after verifying via grep that nothing referenced them — the file's own "don't prune speculatively" convention was overridden here since the user explicitly asked for a full removal, not a hide.
  - **CSS**: removed the now-dead `.layoutGrid`/`.layoutGridWithPanel`/`.layoutGridFull`/`.sidePanelColumn`/`.panelToggleBtn`/`.sidePanelStickyContent*`/`.notesCard`/`.statusChip*`/`.pre`/`.attachmentsNote*` rules (and their 992px media query) from `OrderNew.module.css`; removed the 3-column `.panelsGrid` rule from `Dashboard.module.css`.
  - **Backend note**: this was frontend-only scope. n8n's WhatsApp-intake workflow, the Evolution API webhook, and the shared `/order-api/parse-order` parsing service were not touched and may still be running — they're now orphaned from this app's perspective (nothing reads `messages`/`corrections` anymore). See `architecture.md`'s Stack table and `project-overview.md`'s Out of Scope section for the flag.
  - `tsc -b` ✓ (369 modules, down from 377), `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` `set-state-in-effect` error remains), `npm run build` ✓.

- **`StatusPill` collapsed to a single label, never two lines in one pill (2026-08-11):** the user's screenshot showed a real (different) problem from the earlier bug report — dispatch pills correctly showed the right sub-label ("Out for delivery" for taken orders, would've correctly shown "Awaiting driver" for unassigned ones — confirmed by the 2026-08-11 investigation above), but rendered it stacked *underneath* a separate "DISPATCH" line, i.e. two competing labels in one pill. Per the user: exactly one label should ever show — the more specific one wins.
  - **`StatusPill.tsx`**: `subLabel` now *replaces* the fallback stage label instead of stacking under it — `const displayLabel = subLabel ?? fallbackLabel`. Removed the `key === 'dispatch'` special-case gate entirely; the rule is now generic ("if a subLabel is passed, show it; otherwise show the normal label") and works the same way for every stage/role, not just dispatch, so any future caller can pass a more-specific label without touching `StatusPill` again.
  - Simplified back to a single `<span className={styles.label}>` — removed the now-unused `.labelStack`/`.subLabel` CSS (two-line stacking, smaller sub-line font) that only existed to support the old dual-label layout.
  - Colour is unaffected — still `statusColor(status)`, so a dispatch pill showing "Out for delivery" is still Courier red regardless of which text won.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **`StatusPill` refactored from per-stage colours to role-based colours + a leading dot + the dispatch sub-label folded into the pill (2026-08-10), superseding the "second line under the pill" approach below:** colour now encodes *who's responsible for the order right now* (role), not which stage — stages owned by the same role render identically (intake + finalise both Admin blue; cold + packing both Warehouse green), fixing a real defect in the old per-entry-hex `STATUS_MAP` where intake and dispatch had accidentally collided on the same blue.
  - **New `--role-*`/`--state-done`/`--state-returned`/`--state-neutral` tokens** in `tokens.css` (light + dark, dark values ~15-20% lighter following this file's existing Tailwind-shade pattern) — documented in `ui-context.md`'s colour tables per its own "update docs before editing tokens" header rule. All 6 role colours (+3 terminal states) chosen to be pairwise distinct in both themes.
  - **`pipeline.ts` gained `ROLE_COLOR` and `statusColor(key)`** (resolves any stage/return-bucket key to its role's colour token, with dedicated overrides for delivered/returned/cancelled/awaiting since those aren't owned by a role). Deliberately did **not** add a new `Role` type or `STAGE_ACTOR` map as an earlier draft of this instruction assumed — both already existed (`Role` from `domain.ts`, `ACTOR` in `pipeline.ts` itself, already `intake:'Admin', cold:'Warehouse', ...` verbatim) — reused them instead of forking a duplicate. Added a small `RETURN_BUCKET_ACTOR` map (a separate keyspace from `ACTOR`, since return buckets aren't `order.stage` values) for the 4 return-workflow keys' colours.
  - **`StatusPill.tsx` rewritten**: `STATUS_MAP` → `STATUS_LABELS` (labels only, no more per-entry colour — collision risk is now structurally impossible, one function decides colour for everyone); colour comes from `statusColor()`; a small dot renders before the label (`color-mix(in srgb, ...)` for the tinted background/border instead of the old `${hex}22` string-suffix trick, since colour is now a CSS custom property reference, not a raw hex string); new optional `subLabel` prop. ~~When `status === 'dispatch'` and a `subLabel` is passed, renders as a stacked two-line pill~~ — **superseded 2026-08-11, see the entry above**: two labels in one pill read as competing information, so `subLabel` now replaces the fallback label instead of stacking under it. Pill labels are now translated via `t()` (previously hardcoded English, a pre-existing i18n gap closed as a side effect).
  - **New `dispatchSubLabel()`** in `pipeline.ts` — thin wrapper around the existing `dispatchSubStatus()` predicate (added in the entry below) that returns the label string directly for `StatusPill`'s `subLabel` prop, so callers don't need the typed enum. Still one canonical predicate underneath, not a second copy.
  - **Reverted the "second line under a separate pill" markup** from `OrderRows.tsx` (`.subStatus` span + CSS) — `<StatusPill status={order.status} subLabel={dispatchSubLabel(order)} />` now handles it internally.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Visual distinctness of the 6 role colours + 3 state colours across both themes is still an eyeball-in-a-browser check, not yet done — see Current Goal.

- **Dispatch sub-status label ("Out for delivery" / "Awaiting driver") on the Orders table + Dashboard's Open Orders panel (2026-08-10, later superseded by the entry above — kept for the parts still true):** the sub-status is explicitly *not* a stage — an "out for delivery" order is still `stage === 'dispatch'`; it's a presentational annotation, not part of the stage enum/filters/pipeline counts.
  - **New shared helper** in `pipeline.ts`: `dispatchSubStatus(order)` (returns `'out_for_delivery' | 'awaiting_driver' | null`, `null` for every non-dispatch order) + `DISPATCH_SUBSTATUS_LABELS`. Single source of truth for the same predicate `OrderDetail.tsx`'s `handoffMode` already computed inline — refactored `handoffMode` to reuse it (`dispatchSubStatus(order) === "out_for_delivery"` gates the null case; `handoffMode` still separately resolves *which* of the 3 modes, which the list-view label doesn't need to distinguish). **Still current** — this helper wasn't reverted, only how its result gets rendered.
  - **`useOpenOrders.ts` and `useOrders.ts` were missing the 3 hand-off fields entirely** (`taken_by`/`pickup`/`third_party` weren't in either hook's `fields` array, despite existing live since the courier hand-off build) — added to both, plus to the `OpenOrder` view-model type (`takenBy`/`pickup`/`thirdParty`) both hooks map into. Live-verified first (per project habit) that all 5 roles' Directus ACLs can actually read these 3 fields — Warehouse/Production's restricted `orders.read` field list already included them (added incidentally during the courier hand-off ACL work), so no gap to fix this time. **Still current.**
  - ~~Rendered in `OrderRows.tsx` as a small muted second line under the `StatusPill`~~ — **superseded**, see the entry above. The sub-label is now internal to `StatusPill` itself.
  - New i18n key: "Awaiting driver" ("Out for delivery" already existed).
  - **Found a real tooling gap while verifying**: `npx tsc --noEmit` against the root `tsconfig.json` type-checks zero files (see Current Goal) — re-ran with `npx tsc -b` instead, which caught 2 real errors in the unused `mockDashboard.ts` (missing the 3 new `OpenOrder` fields) that `--noEmit` had silently missed. Fixed by adding `takenBy: null, pickup: false, thirdParty: false` to both mock rows.
  - Verified the derivation logic directly (own-courier / customer-pickup / 3rd-party / unassigned / non-dispatch — all 5 cases resolve correctly) without mutating live order data — see Current Goal for what's still owed as a live browser QA pass.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **Three small `OrderDetail.tsx` item-list polish fixes (2026-08-10):**
  - A line with no `price` set (invoiced separately in Accurate, common for some Horeca terms) now shows `t("No price on the order — invoiced in Accurate.")` instead of a `Rp 0` calc — new `hasPrice` check (`line.price != null && line.price !== ""`) distinguishes "no price recorded" from a legitimate zero.
  - The per-line weight total (`.totalWeight`) is hidden at the `intake` stage — nothing's been weighed yet at that point, so a "Total: " line with no figure (or a stray "0.00 kg" for a kg/loaf item) was noise; continues the same intake-is-read-only cleanup as the earlier weighing-stage-gate fix.
  - `OrderDetail.tsx`'s own stepper/pipeline-label array now shows `"New Order"` for the `intake` stage instead of `"Intake"` (existing i18n key, no new translation needed) — matches the label used everywhere else in the app (Dashboard's "New Order" button, etc.).
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **Order-action IA fixes on `OrderDetail.tsx` — consolidated group, Reorder, weighing-stage regression (2026-08-10):** three fixes from a user-authored spec comparing the port against `Dev-OrderDetail.jsx`'s action layout and weighing gate.
  - **Consolidated "Order actions" group**: the prototype keeps every cross-stage override (Reorder, Put on hold, Send back, Restore, Cancel — "Reopen" is the delivered→dispatch case of Send back) in one card at the bottom, same place at every stage, so users don't hunt for them as they move per stage. The port had scattered this — Send Back rendered inline next to the primary advance button, Reorder didn't exist. Moved Send Back into the existing bottom `orderActions` group (which already had Restore/Hold/Cancel) and added Reorder there. The top zone now only has the stage's own forward action + genuine stage-work ("Customer refused / returned" at dispatch — a delivery outcome, not an override).
  - **Send-back label now derives from the shared pipeline stage-label map** (`STAGE_LABELS`, `lib/pipeline.ts`) instead of a hardcoded string per `STAGE_FLOW` entry — can't drift from the Dashboard's pipeline-tile names anymore (e.g. dispatch's target is "Print DO/SI", not the internal `finalise` key). Composed from two separately-translated pieces (`t("Send back to")` / `t("Re-open to")` + `t(STAGE_LABELS[prev])`) rather than one static per-phrase i18n key, matching the app's existing pattern for dynamic strings and fixing a real i18n gap along the way (the old hardcoded "Send back to Print DO/SI" had no matching dictionary entry, so it silently fell back to English in Bahasa mode).
  - **Fixed a real hand-off bug**: `handleSendBack()` only patched `stage`, never resetting `taken_by`/`pickup`/`third_party`/`courier_service`. Sending an order back from dispatch (e.g. to reprint DO/SI) left it still marked as taken by a courier — so when it came back around to dispatch, the 3-way hand-off chooser wouldn't show again. Now resets all four fields when sending back specifically from `dispatch` (ported from the prototype's `HANDOVER_RESET`).
  - **New Reorder action** (was completely absent from the port — a real feature gap, not polish; the prototype's own comment names weekly Horeca repeats as the reason it exists at all). Clones the order into a new intake-stage order for the same customer: same customer/channel/contact/address (all copied straight from the order snapshot — payment method/timing come along for free since it's the same `customer_id`), all non-removed lines + their cuts, delivery date = tomorrow, a fresh order number, and a `"Reorder of #<original>"` history entry — then navigates to the new order. Gated on `createOrders` (office roles only), available regardless of the source order's stage (including cancelled/delivered). Deliberately reuses the exact same Directus-backed creation path `OrderNew.tsx` already uses (`getNextOrderNo`, `createOrder`, `createOrderLines`, `createLineCut`) instead of porting the prototype's client-side order-number scan — no new `directus.ts` functions needed, and order lines are created as real rows (batch `createOrderLines`), not a nested-array deep copy.
  - **Fixed weighing controls leaking into every stage**: the weight-entry inputs (`weighingSection` block) and the item-photo camera button had no stage gate at all — a kg/loaf line showed scale inputs and a camera button at intake, dispatch, everywhere, not just cold storage where weighing is actually the warehouse's job. Only the "needs photo" hint was correctly gated. Introduced `canWeighHere = stage === "cold" && auth.can("weighColdStorage")` (mirrors the prototype's `weighing` flag, `Dev-OrderDetail.jsx:172`) and wrapped both the weighing-lines block and the item-photo camera in it; already-uploaded photo thumbnails still display read-only at every stage (only the *upload* control is gated). `weighColdStorage` naturally excludes Finance (their only job at cold is clearing the payment gate) without needing a separate check.
  - New i18n keys: "Re-open to", "Reorder", the reorder confirm/error strings. "Send back to" already existed.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **Fixed "Failed to log document" on `OrderDetail.tsx` for Admin (2026-08-10):** Admin's Directus role had **only `read`** permission on the `attachments` collection — no `create`, no `delete` — despite the app's own capability matrix granting `printDocuments` (which gates the whole add-document form) to Admin. `createAttachment()` was hitting a real, correct Directus 403. Granted `create` + `delete` (the only two actions the app actually performs on this collection — no `update` call site exists) to Admin's role, live, verified with a follow-up read. `directus_files` (the upload target) and `order_history` were already fully permissioned for Admin, so this was the one missing piece.

- **Fixed `/deliveries` crash + 2 latent copies of the same bug (2026-08-10):** user reported `Failed to load deliveries: Invalid order_lines response: ... expected string, received undefined` at `path: [0, "name"]`. `useDeliveries.ts`'s `readOrderLines()` call requested `fields: ['id', 'order_id', 'qty', 'price']` — missing `name`, which `OrderLinesCollectionSchema` requires as non-optional (`name: z.string()`, no `.nullable().optional()`) — so Directus returned rows without a `name` key and zod validation failed for the whole array, not just that one field. Swept every `readOrderLines()`/`readCustomers()`/`readProducts()` call site in the app for the same pattern (a call with an explicit `fields` array missing a schema-required field) and found two more copies of the identical bug: **`useCashUp.ts`** (pre-existing, from an earlier session — the Cash Up page's "filter shape returns 200" verification back on 2026-08-09 checked the network call succeeded, not that the zod parse of the response did) and **`useTodayDigest.ts`** (this session's own Unit 5 code). Fixed all three by adding `'name'` to their `fields` arrays (unused by any of the three, but required to satisfy the schema — same reasoning as the pre-existing `id`-must-be-requested comments already next to each call). All other `readOrderLines`/`readCustomers`/`readProducts` call sites in the app already correctly include `name` or omit `fields` entirely (Directus returns everything by default in that case) — no further instances found.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **Capability-matrix audit against `prototype-audit.md` §1.2, applied across pages (2026-08-10):** triggered by the user reporting "Failed to load orders" for Warehouse/Production and asking for the OpenOrdersPanel to be expandable (with price hidden) for Courier.
  - **Root cause of the Orders-page crash for Warehouse/Production**: their Directus `orders.read` ACL is field-restricted (correctly excludes `sales_rep`, matching their denied `seeCustomerContact`), but `useOrders.ts`/`useOpenOrders.ts` always requested `sales_rep` regardless of role — Directus rejects the *entire* request when it names a field outside a restricted role's allow-list, so the whole page 403'd. Same root cause, different field, explains why Courier could never expand the Dashboard's Open Orders panel: their `order_lines.read` ACL correctly excludes `price` (matches denied `seePrices`), but the hooks always requested `price` too, so the entire line-items query silently failed and every order showed 0 lines (never just "lines without a price" — never expandable at all).
  - **Fix**: both hooks now build their `fields` arrays conditionally — `sales_rep` only requested when `can('seeCustomerContact')`, `price` only requested when `can('seePrices')`. This alone fixed both the Warehouse/Production crash and unblocked Courier's expand — the price-hiding itself was already correctly built in `OrderRows.tsx` (`canSeePrices &&`), it just never got a chance to run.
  - **Live ACL gap found + fixed**: Warehouse and Production both have `seePrices: true` in the app's own capability matrix, but their `order_lines.read` permission didn't actually include the `price` field — granted it to both (live, verified with a follow-up read).
  - **Matrix comparison result**: Admin, Production, Finance, and Courier's `ALLOW` blocks (`domain.ts`) match `prototype-audit.md` §1.2 exactly, capability-by-capability. Warehouse had one known/tracked deviation (`manage_products: true`, audit says denied) — resolved below.
  - **New `flag_out_of_stock` capability**, split out of `manage_products` (closes F-10 — the same field previously had two different gating mechanisms depending on where you were: the Products list toggle and the ProductDetail form both leaned on the broad `manage_products`, when only the OOS flip was actually intended for Warehouse per `Settings-Owner.png`). `Warehouse` now has `flag_out_of_stock: true` instead of `manage_products: true`. Not added to `PERMISSION_GRID` yet (Owner can't re-toggle it from the Settings UI) — explicit scope decision, coded default only for now.
    - **`Products.tsx`**: split the old single `canToggleOOS` (which was actually gating *both* the OOS toggle *and* the "New Product" button) into `canManageProducts` (New Product) and `canToggleOOS` (the toggle itself, table column, colSpan). Also fixed a silent-failure bug found while testing this: the OOS toggle's optimistic-revert-on-error path never told the user *why* it reverted — added a `window.alert`.
    - **Second live ACL bug found**: Warehouse had **zero** write permission on `products` in Directus (read-only) — meaning the OOS toggle had likely been silently failing and reverting for Warehouse this entire time, unnoticed because of the exact silent-failure bug just mentioned. Granted a new `products.update` permission scoped to `fields: ["oos"]` only — this is also the real server-side enforcement behind the new capability (matches "Warehouse can flip OOS, nothing else," not just a UI-level restriction).
    - **`ProductEdit.tsx`**: page access is now `canManage || canToggleOOS` for an existing product (still `canManage`-only for creating a new one). All non-OOS fields (name, accurate name, category, origin, grade, brand, catch-weight) are disabled for an OOS-only user; the Delete button is hidden for them entirely; the save payload sends only `{oos}` for an OOS-only user instead of the full form (would otherwise 403 against the new scoped ACL, same field-mismatch bug class as above) — full payload only ever sent when `canManage` is true (guaranteed for the `isNew`/create path).
  - **`OrderEdit.tsx` route-guard gap fixed**: unlike its `CustomerEdit.tsx`/`ProductEdit.tsx` siblings (both redirect away on mount if unauthorized), `OrderEdit.tsx` had no route guard *and* no self-guard — only the final Save button was disabled for a role without `editOrderLines`. A Warehouse/Production/Courier user could open `/orders/:id/edit` directly via URL and freely interact with the whole form, just couldn't save. Added the same self-guard pattern (`canEdit = can('editOrderLines') && !isCancelled && !isDelivered`, redirect on mount if false), mirrored from `OrderDetail.tsx`'s own "Edit" button gate so both pages agree on when editing is allowed.
  - Also fixed, while sweeping: two stray TS errors in `ImageDetailsModal.tsx` (unused `Icon` import, unused `deleteLabel` prop — wired `deleteLabel` into the delete button's `title` instead of leaving it dead, since no caller passed it but the intent was clear) — these were blocking a clean build.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Live-verified every ACL change with a follow-up `GET` before trusting it.

- **`OrderDetail.tsx` Contact/Address rows fixed for Owner (and everyone else) (2026-08-10):** the rows required *both* `can('seeCustomerContact')` *and* the order's own `customer_contact`/`customer_address` field to be non-empty before rendering at all — but live data showed `customer_address` is `null` on essentially every order (checked a 10-row sample: 10/10 null), so the row silently disappeared for every role, Owner included, misread as a permissions bug. Fixed to only gate on the capability (matching how "Sales Rep" already behaves in the same grid), falling back to the matched customer record's own `contact`/`address` (already fetched, just unused) when the order-level snapshot is empty, and "—" only if neither exists.

- **Root-caused and fixed: every `settings` read AND write was silently broken (2026-08-10).** After the Settings toggle fixes below, the user reported the Team-section flicker was still happening on a different toggle, plus `require_photo`/`dispatch_proof_required` toggles "still can be set on" — i.e. never actually stuck. Fixing the write surfaced a second bug immediately: an `alert("Settings not loaded yet")` on every single toggle click, meaning `settings` was `null` in React state at the moment of every click. Root cause of both: **`settings` is a Directus singleton collection** (exactly one row, id always 1) — the whole read/write pair was calling the wrong SDK composables for that shape.
  - **Read bug**: `readSettings()` called `readItems("settings", {limit: 1})` and checked `Array.isArray(raw)` to find the row. Confirmed live via curl that `GET /items/settings` (a singleton) always returns a single object, never an array, regardless of query params — so `Array.isArray(raw)` was always `false`, `rows` was always `[]`, and `readSettings()` returned `{data: null}` on literally every call. `settings` in `useSettings()` was never populated, at all, ever — which is also why the toggles' checked state and the tolerance-% defaults never reflected the real DB values. Fixed by switching to `readSingleton("settings")`, which returns the object directly.
  - **Write bug**: `updateSettings()` called `updateItem("settings", id, patch)`, which the SDK sends as `PATCH /items/settings/1` — confirmed live via curl to 404 with `"Route /settings/1 doesn't exist."` (singletons have no per-id route). `Settings.tsx`'s `onChange` handlers never awaited/checked `update()`'s result, so this error was swallowed — the toggle looked clickable but never persisted. Fixed by switching to `updateSingleton("settings", patch)` and dropping the now-unnecessary `id` parameter. Verified live via curl: `PATCH /items/settings` (no id) succeeds and round-trips on a follow-up `GET` — tested and reverted `require_photo` clean.
  - **`Settings.tsx`**: added a small `handleSettingUpdate()` wrapper that awaits `update()` and `window.alert`s on error, wired to both toggles and both tolerance-% inputs, so a future regression here fails loudly instead of silently.
  - Also fixed a second, distinct flicker source the user pointed out directly: `handleToggleActive()` (the Team section's per-member active/inactive toggle) still called `reloadTeam()`, same bug class as the permission-grid fix below but a call site that fix didn't touch. Gave it the same optimistic-local-update treatment — `useTeamSettings` now also returns `setMembers`.
  - This means **every `settings`-gated feature this session (and possibly earlier) has never actually been configurable** until now: Cold Storage photo requirement, Dispatch proof requirement, weigh tolerances, and language default. Worth a real click-through smoke test now that both read and write are fixed.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **Settings toggle bug fixes (2026-08-10):** two bugs confirmed by reading the port's own code (not assumed from the prototype) while building the digest above.
  - **Bug A — every Roles & Permissions grid click flickered the unrelated Team section.** `toggleCell()`/`handleResetPermissions()` both routed through `reloadTeam()`, which bumps `useTeamSettings`'s nonce and refetches members + roles + permRows together — the Team section (gated on the same combined `teamLoading`) visibly unmounted/remounted on every single permission click. Fixed with an optimistic local update: `useTeamSettings` now also returns `setPermRows` (the raw state setter); `toggleCell()` flips the cell in local state immediately, persists via `setRolePermission()` in the background, and reverts + alerts only on error. `setRolePermission()` (`directus.ts`) now returns the written row's id (previously discarded) so a newly-created override cell's placeholder id can be swapped for the real one without a refetch. `handleResetPermissions()` gets the same treatment (clears `permRows` locally, restores on error). `reloadTeam()` stays reserved for genuine full-refresh cases (member added/removed, role reassigned) — untouched at the time, but see the entry above: the Team member-toggle call site still had the same bug and was fixed in a follow-up pass.
  - **Bug B — `require_photo` was written but read nowhere.** Confirmed via project-wide grep: zero consumers existed. Wired for real in `OrderDetail.tsx`, mirroring the existing `dispatch_proof_required`/`proofRequired` gate shape: (1) `handleAdvance()` now blocks with an alert at the `cold` stage when `require_photo` is on and no line has any captured item photo yet; (2) a small "Needs photo" warning-colored badge renders next to each line's photo-upload button at `cold` stage under the same condition, reusing the existing `itemPhotosMap` state — no new fetch.
  - `dispatch_proof_required`'s key-naming/inversion logic was investigated and found correct — see the entry above for the actual root cause found once the user reported the toggle still wasn't sticking (a singleton-collection routing bug in `updateSettings()`, unrelated to key naming). Live-verified the Owner role has `admin_access: true` (bypasses all ACLs), ruling out a permissions-row explanation for either bug — both were genuine frontend wiring issues.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **Courier hand-off (own courier / customer pickup / 3rd-party) + live GPS tracking + "Needs attention today" digest (2026-08-10):** closes out the last major deferred item from the prototype port — `orders.taken_by`/`third_party`/`pickup` existed live but were completely unused, and `trackCourier` had no UI to gate.
  - **Schema/ACL**: added `orders.courier_service` (nullable text, live column) + `CourierLocationsCollectionSchema`/`createCourierLocation()`/`readLatestCourierLocation()` (`courier_locations` already existed live, one location stream per courier via `user_created`, no `order_id`). Extended Courier's `orders.update` permission fields to include `pickup`/`third_party`/`courier_service`; granted Finance `read` on `courier_locations` (closes a gap — `trackCourier` defaults `true` for Finance in `domain.ts` but had no live ACL row). All changes live-verified with a follow-up `GET` before being wired into code.
  - **`OrderDetail.tsx` hand-off chooser** (`dispatch` stage, mode derived fresh each render from `taken_by`/`pickup`/`third_party` — no separate stored toggle): 3-way chooser (own courier / customer pickup / 3rd-party service+ref) when no mode is set yet; the existing proof-capture card relabels per mode (pickup swaps to "Proof of pickup"/"Collected by", hides destination; 3rd-party only requires the handover photo and hides the COD checkbox); a "Change method" button resets all 4 fields. Every hand-off action logs an `order_history` entry via a shared `commitHandoff()` helper — a deliberate improvement over the prototype, which wrote none.
  - **`confirmDocsReturned` wired for real**: a small Admin-gated card at `delivered` stage writes `docs_returned: true` + history — previously a matrix-only capability with no UI anywhere.
  - **Live GPS**: new `src/components/CourierLiveLocation/` — `useDriverLive()` (silent, `watchPosition` + a ~20s-throttle ref gate before writing to `courier_locations`, mounted only when the current user is the assigned courier and stage is `dispatch`) and `CourierLiveLocation` (visible "● live · Xs ago" badge + a keyless Google Maps iframe embed, polls every ~20s, gated on `trackCourier` capability). No new npm map dependency, matching this app's existing external-Maps-link convention.
  - **`useDeliveries.ts`/`Deliveries.tsx`**: now returns `{ mine, others }` instead of a flat `stops` list; a new "With other drivers" read-only section shows stops assigned to someone else. Fixed a proof-capture bypass: the page's "Mark delivered" button previously called the hook's bare stage-flip directly, skipping the new photo/hand-off flow entirely — now navigates to the order detail page instead, so that flow has one source of truth.
  - **"Needs attention today" / "Done today" digest** on `Dashboard.tsx`, gated `role === 'Admin' || 'Owner'` (an Admin's day-to-day tool, not an ownership view — redesigned from the prototype's flat 7-cell Owner-only grid per explicit user feedback, matching `context/designs/Screenshot 2026-08-10 094825.png`). Top row: 5 tap-through tiles in order — COD pending (full Rupiah, reuses the Dashboard's existing `useCashUp()` call), DO/SI not back, on the road, returns in flight, new orders today — where only the first two go "loud" (amber) and only when their count is > 0; the other three stay neutral regardless of count. Bottom: a quiet non-clickable "Done today" strip (delivered count, COD collected amount). New `src/hooks/useTodayDigest.ts` (only fetches when the role gate passes) computes every count via `aggregateOrders()`; "returns in flight" reuses a new `returnsInFlightFilter()` (`pipeline.ts`) that combines all 4 return buckets into one `_or` so a return sitting in two buckets isn't double-counted (deliberately different from the Dashboard's existing Returns Workflow strip, which does count bucket-by-bucket); "COD collected today" reads `useCashUp.ts`'s own `order_history` writes (`what: 'COD cash reconciled'`) instead of adding a new timestamp column, joined to `order_lines` for the amount. Also added a `"today"` stage-filter entry to `Orders.tsx`/`useOrders.ts` for the "new orders" tile's click-through.
  - Every new/changed Directus filter shape (courier hand-off writes, `courier_locations` create/read, `returnsInFlightFilter`, on-the-road, docs-not-back, the two digest aggregate queries) was live-verified via direct `curl` against the real API before being wired into a hook.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **Corrections review admin UI + favicon fix (2026-08-09):** closes out the last "Next Up"/"Current Goal" item that wasn't the open-ended courier-GPS subsystem. No prototype UI existed to port here (the old prototype only ever showed a bare "N learned matches" count, same as this app had) — this is genuinely new scope.
  - **New `readCorrections()`/`deleteCorrection()`** in `src/lib/directus.ts` (+ `CorrectionsCollectionArraySchema` import), alongside the existing `upsertCorrection()`.
  - **New `src/hooks/useCorrections.ts`**: reads all `corrections` rows (sorted `times_used` desc), joins `product_id` → product name and `created_by` → display name (same batch-join pattern as `useCashUp.ts`/`OrderDetail.tsx`), exposes a `remove()` that deletes a row and drops it from local state on success.
  - **Settings.tsx "Intake Learning" section extended**: the existing "N learned matches" summary card is unchanged; added a scrollable list below it (`token_key → product name`, times-used badge, "Added by X · date") with a per-row delete button gated to `canManageSettings` (Admin/Owner) — `window.confirm`-gated, matches the `deleteTeamMember`/`deleteAttachment` pattern.
  - **Explicitly decided with the user**: delete is real and persists (not read-only), since a wrong learned mapping should be forgettable. Updated the section's copy from "Cannot be cleared" (an earlier session's stale claim — individual rows were never actually reviewable before this) to "...reviewed here."
  - **Found and fixed a real pre-existing bug while smoke-testing live**: the Directus `corrections` collection had **zero permission rows for any non-Owner role** — meaning the pre-existing "N learned matches" counter and the intake correction-saving flow (`upsertCorrection`, called from `OrderNew.tsx`) had likely been silently failing for every Admin user all along (only Owner's token bypasses permissions entirely). Only Admin has `createOrders` by default among non-Owner roles, so granted Admin full `create`/`read`/`update`/`delete` on `corrections` to match its existing app-level capability. Verified via the live `/permissions` API before and after.
  - **Favicon fix**: `public/favicon.svg` was still the default Vite-scaffold purple icon (`#863bff`/`#7e14ff`) — never swapped for the real brand mark despite `public/IPP Icon.svg` (black `#030303` + red `#EC1C25`, the same asset `Logo.tsx` already uses for the in-app sidebar/login logo) existing since early in the project. Replaced `favicon.svg`'s contents with the real mark.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Verified live: the `readCorrections()` filter/sort shape returns 200 (table is currently empty on the live instance, so the join path itself wasn't exercised against real rows, only verified to construct valid queries), and the new Admin permission rows were confirmed via a live `/permissions` re-read after granting them.

- **Real persisted COD reconciliation for Cash Up (2026-08-09):** second of the two "Next Up" items from the 2026-08-07 session — "Confirm" on the Cash Up page was previously ephemeral (in-memory `Set`, reset on reload) because no `cod_reconciled`-style field existed anywhere in the live schema.
  - **Live Directus schema change** (applied directly via the REST API using a fresh admin token — the `.env` token had been rotated since 2026-08-07 and needed regenerating in the Directus admin UI first): added `orders.cod_reconciled` (boolean, default `false`), naming/pattern matched to the existing `payment_confirmed` column. Extended Finance's `orders.update` permission (id 220) from `fields: ["payment_confirmed"]` to `["payment_confirmed", "cod_reconciled"]` — Admin's `orders.update`/`orders.read` are already `fields: ["*"]`, so no change needed there. Verified with a live write+revert against a real order row before wiring the app.
  - **`useCashUp.ts`**: the queue query now excludes already-reconciled orders (`cod_reconciled: { _neq: true }`, mirrors the prototype's `!o.payment.codReconciled`). `confirm(orderId)` is now `async`, calls `updateOrder(orderId, { cod_reconciled: true })` then `appendOrderHistory()` ("COD cash reconciled", matching the prototype's history entry), and only marks the row confirmed in local state on success — new `confirmingIds` set tracks in-flight rows so the UI can disable/label a button mid-request without waiting on a full refetch.
  - **`CashUp.tsx`**: `handleConfirm` awaits `confirm()` and surfaces a `window.alert` on failure instead of assuming success; the per-row Confirm button now has a 3rd state (`Confirm` / `Saving…` / `Confirmed`) driven by `confirmingIds`/`confirmedIds`.
  - **`schemas.ts`**: `OrdersCollectionSchema` gained `cod_reconciled: z.boolean().nullable().optional()`, alongside `payment_confirmed`.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Verified live: the exact `useCashUp.ts` filter shape returns 200 against the real API, and a real PATCH `cod_reconciled: true` → `false` round-trip succeeded against a live order row (reverted after testing, no real order state changed).

- **Delivery-proof capture wired to "Mark as Delivered" (2026-08-09):** first of the two "Next Up" items from the 2026-08-07 session — `delivery_proofs` had a real live schema and a `readDeliveryProofs()` read function since 2026-08-07 but zero write path or UI; "Mark as Delivered" was a bare stage-flip with no proof capture.
  - **New `createDeliveryProof()`** in `src/lib/directus.ts` (+ `DeliveryProofsCollectionSchema` import, was only imported as the array variant before) — mirrors the existing `createAttachment()`/`uploadFile()` pattern exactly.
  - **`OrderDetail.tsx`**: clicking "Mark as Delivered" at the `dispatch` stage no longer calls `handleAdvance` directly — it opens an inline proof-capture `Card` (same `showRefuseForm`-style toggle pattern) with 3 photo upload slots (condition / receiver / signed-doc, each `uploadFile()` → stores the returned file id directly, no junction table needed since `delivery_proofs` holds the 3 photo FKs inline), a receiver-name text input, and a COD checkbox pre-filled from `matchedCustomer?.pay_timing === 'cod'`. Confirming calls `createDeliveryProof()` then the same `updateOrder(stage: 'delivered')` + `appendOrderHistory()` sequence `handleAdvance` used, so history/audit trail is unchanged.
  - **Wired the previously-inert `settings.dispatch_proof_required` toggle** (Settings page already had this Owner/Admin toggle built 2026-08-07, but nothing read it): `OrderDetail.tsx` now calls `useSettings()` and enforces all 3 photos as required only when the flag is `true`; when off, photos are optional (receiver name is always required). Every other stage's advance button is unaffected — this branch only applies at `dispatch`.
  - New i18n keys added (`Delivery proof`, `Condition photo`, `Receiver photo`, `Signed doc` / `(optional)`, `Upload`, `Uploading…`, `Receiver's name`, `Confirm delivery`, plus 2 validation-alert strings); `COD collected` reused an existing key.
  - `tsc -b` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓. Verified via dev-server `curl` that the changed route transforms cleanly (no live Directus credentials available this session to smoke-test an actual delivery confirmation against real data).

- **QuickActionCard row fills full width regardless of visible-card count (2026-08-07):** `.quickActionsRow` used a hardcoded `repeat(3, minmax(0, 1fr))`, so a role seeing only 1 or 2 of the 3 capability-gated cards (Deliveries/Pick list/Cash-up) got dead grid space instead of the visible cards stretching to fill the row. Fixed by computing the visible count in `Dashboard.tsx` (`[canViewDeliveryRun, canViewPickList, canReconcileCOD].filter(Boolean).length`) and passing it through as a `--quick-action-count` CSS custom property (inline `style`), with the CSS module reading `repeat(var(--quick-action-count, 3), minmax(0, 1fr))`. Documented as a reusable pattern in `ui-registry.md` for any future role-gated grid row with a variable number of visible children. `ui-registry.md` also gained the `QuickActionCard` component entry itself (previously undocumented — added retroactively per `.agents/CLAUDE.md`'s "update ui-registry.md after every feature" rule).

- **Dashboard quick-action row rebuild + prototype-audit §9 checklist review (2026-08-07):**
  - **Deliveries / Pick list / Cash-up moved from the top-row button strip to a new row under the metric cards**, restyled as `QuickActionCard` (new shared component, `src/components/QuickActionCard/`): `--bg-surface-hover` background, icon in a small square, label on the left, a bold count/value + suffix text ("to deliver"/"to pick"/"to reconcile") on the right — matches the reference screenshot. The row itself is wrapped in `{(canViewDeliveryRun || canViewPickList || canReconcileCOD) && (...)}` so a role with none of the three (e.g. Production) renders no empty container, not an empty row.
  - **Each card's count now comes from the same hook its target page uses**, so the Dashboard number always matches what the user sees after clicking through: `usePickList(todayISO()).orderCount` for Pick list (the exact field `PickList.tsx`'s own header badge reads), `useDeliveries(currentUserId).stops.length` for Deliveries, `useCashUp().remaining` for Cash-up. These three hooks are called unconditionally (outside the `isLoading` gate) so they don't slow the Dashboard's first paint — previously the Dashboard computed no count at all for these buttons, so there was nothing to have been "wrong" in code, but the fix ensures no future drift between the two surfaces.
  - **Cash-up value formatted as "Rp 3.85 jt"** (`formatRupiahShort()`, divides by 1,000,000, 2 decimals, "jt" suffix) instead of a full Rupiah string.
  - **"No data" renders `-` instead of hiding the card**: deliberately different from the prototype's per-role conditional-visibility-when-empty — this port always shows a capability-gated card, falling back to `-` when the count/value is 0, per explicit instruction.
  - New translation key added: `"to pick"` (distinct from the existing `"to pick up"`).
  - **`prototype-audit.md` §9 "Verification checklist" re-reviewed against current code** (not live-tested — verified via capability configs/gates). 5 of 17 rows struck through with a note: two describe features never ported (a "Today at a glance" Owner digest; a `/orders` sort-by-Stage option), two contradict deliberate, already-documented architecture decisions (all 8 pipeline stages always visible to every role, only highlighting differs; Needs Attention shows all 6 buckets to every role, sorted not filtered), and one (backup-JSON-has-no-`pin`-field) is moot twice over (no PIN field exists anywhere in this app's schema, and no backup export is implemented yet). One row (Warehouse/dispatch/delivery-address) flagged as a stale role label from an earlier role model rather than struck outright — the underlying intent already holds via Courier's `seeCustomerContact` default. One row (Owner Settings' `viewPickList` toggle actually hiding Warehouse's Pick list card) is newly *testable* now that the Roles & Permissions grid exists, but not yet manually verified end-to-end.
  - `tsc --noEmit` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error remains), `npm run build` ✓.

- **Full i18n rollout across the app + Team edit-block redesign + Language-toggle bugfix (2026-08-07):**
  - **`src/i18n/translations.ts` replaced with a merged superset dictionary** (934 keys, sorted alphabetically instead of hand-grouped): programmatically merged the prototype's full `Dev-i18n.js` dictionary (592 keys) with this session's existing app-specific keys (81 keys), then filled remaining gaps by hand per-page as each surface got wired. Prior file only covered Dashboard/Login/Add-New-Order; every page below now reads from the same dictionary.
  - **Every remaining page wired to `useLanguage().t()`**: Products, ProductDetail, ProductEdit, Customers, CustomerDetail, CustomerEdit, Orders (+ shared `OrderRows` component), Reports, PickList, Deliveries, CashUp, OrderNew (+ shared `AddItemModal`), OrderEdit, OrderDetail (the largest file, ~2360 lines — stage-flow button labels, the returns sub-flow's 4 parallel-bucket panels, document log, notes/history side panel). Settings.tsx's new Team/Roles & Permissions sections (built earlier this session, before i18n) also wired.
  - **Scope boundary held consistently**: static labels, headings, buttons, placeholders, empty/loading states, and `window.confirm`/validation `window.alert` text are translated; dynamic `` `Failed to X: ${apiError}` `` runtime error strings are left English-only (translating a live API error message adds little value and there are dozens of these call sites) — matches the posture already set earlier in the session for CustomerDetail/CustomerEdit before this pass expanded it project-wide.
  - **`order_history.what` is written in English at mutation time** (unchanged — it's stored data, not a live-rendered label) **but now translated at display time**: `OrderDetail.tsx`'s history list renders `{t(h.what)}` instead of the raw string, so the fixed-text entries ("Order cancelled", "Order created", etc.) translate correctly while free-form ones (e.g. `` `Stage advanced: ${stage} → ${next}` ``, `` `Note: ${text}` ``) fall through to their English original, since `t()` only matches exact strings. Same treatment applied to `PIPELINE_STAGES`/`STAGE_FLOW`/`RETURN_DOC_OPTIONS` labels in `OrderDetail.tsx` and `STAGE_OPTIONS`/`STAGE_COPY`/`SORT_OPTIONS` in `Orders.tsx` — these stay plain-English module-level consts (used as object keys elsewhere), translated only at their JSX render sites via `t(label)`.
  - **Found and fixed a real bug while wiring `Orders.tsx`'s document-type `<select>`**: the map callback's loop variable was named `t` (`DOC_TYPES.map((t) => ...)`), shadowing the new `useLanguage()` `t` in that scope. Renamed the loop variable to `docTypeOption` — no runtime break existed (the shadowed scope never called the real `t()`), but it was a landmine for the next edit in that block.
  - **`CustomerDetail.tsx`/`CustomerEdit.tsx`'s "not found" fallback fixed to avoid a `react-hooks/exhaustive-deps` warning**: initially translated inside the data-fetch `useEffect` (`setError(res.error || t("Customer not found."))`), which both triggers the lint warning (effect doesn't depend on `t`) and goes stale if the user switches language after the error is set. Fixed by storing the raw English fallback in state and translating at render time (`{t(error)}`) instead — matches the pattern `OrderDetail.tsx`/`OrderEdit.tsx` already used. Applied the same fix retroactively to `ProductDetail.tsx`/`ProductEdit.tsx`'s equivalent "Product not found." case, which had been left completely unwired.
  - **Settings.tsx Team edit-block redesigned per user feedback on `Settings-Owner.png`**: the header row (name/role/edit-pencil/active-toggle) now stays visible when editing, with the edit form (Name input + Role dropdown) expanding *underneath* it instead of replacing it — matches the design screenshot. Added a real "Remove member" trash action (`deleteTeamMember()` → `deleteUser()` via the Directus SDK, `window.confirm`-gated) in the edit footer alongside Cancel/Save. The design's Username/PIN fields were added then removed again per explicit user correction — this app authenticates via real Directus email/password, there's nowhere for those fields to persist.
  - **Language-toggle bug fixed**: the Settings page's English/Bahasa buttons compared against `settings?.lang` (a separate, slower `useSettings()` Directus fetch) instead of the already-active `useLanguage().lang`, so clicking a language updated the rest of the app instantly but the buttons themselves lagged or silently failed to highlight if that second fetch was slow or errored. Switched the buttons to read `lang` directly from `useLanguage()`.
  - Two small Dashboard fixes bundled in: MetricCard's "All" range option relabeled to "All time" (button text + the Open Orders card's static range label in `useDashboardCounts.ts`, both spots).
  - `tsc --noEmit` ✓, `eslint` ✓ (only the pre-existing, unrelated `AddItemModal.tsx` set-state-in-effect error remains), `npm run build` ✓.

- **Owner Settings role-permission editor, Reports top-5 filters, Dashboard "All time" range (2026-08-07):**
  - **Reports (`useReports.ts`/`Reports.tsx`):** "Volume by customers" capped to the top 5 (`.slice(0, 5)` after the existing sort); "Demand by product" excludes the `pcs` unit group entirely (counted-unit items aren't a meaningful weighed/boxed/packed-style leaderboard) — the per-unit top-5 row cap for loaf/box/kg/pack was already in place from an earlier pass. Both section headings now read "(Top 5)".
  - **Dashboard MetricCards:** added an "All time" option (`DateRangeType` gained `'all'`) to the Total/Delivered/Cancelled Orders range dropdown — `useDashboardCounts.ts`'s `getFilterForField()` returns `{}` (no date restriction) for `type: 'all'`. Made "All time" the default range for all three cards (was "Today").
  - **Owner Settings — Team + Roles & Permissions built per `context/designs/Settings-Owner.png`:**
    - `domain.ts` gained 10 new capabilities (`holdResume`, `sendBackStage`, `reopenOrders`, `helpOtherStages`, `confirmDocsReturned`, `overrideCreditLimit`, `exportCSV`, `backupRestore`, `resetData`, `editAfterLock`) plus a `PERMISSION_GRID` (section-grouped `{cap,label}` rows) and `PERMISSION_GRID_ROLES` (Admin/Warehouse/Production/Finance/Courier column order) — both consumed directly by the Settings grid UI, sourced from a row-by-row read of the design image.
    - `ALLOW` defaults updated to match the design's checked cells, including two intentional behavior changes from prior session defaults: **Warehouse and Production now default to `seePrices: true`** (design shows both checked; previously only Admin/Finance/Courier had prices), and `exportCSV` defaults `true` for all 5 roles. These are default overrides an Owner can still toggle live via the new grid — flagged here since they reverse an earlier explicit price-gating decision.
    - `OrderDetail.tsx`'s `canHold`/`canSendBack`/`canRestore` decoupled from the shared `advanceStage`/`flow.capability` checks onto their own new capabilities; `canAdvance` now also passes if the actor holds `helpOtherStages` (floor-helper override), independent of stage ownership.
    - `directus.ts` gained `readTeamMembers`/`updateTeamMember`/`readAllRoles` (Team section) and `readRolePermissionRows`/`setRolePermission`/`deleteRolePermissionRows` (Roles & Permissions grid), all following the existing `{data,error}` tuple + `as never`/`as unknown as T` cast pattern already used for the rest of the file (no zod schema added for these — matches how `loadRolePermissions()` in `domain.ts` already reads `role_permissions` without zod).
    - New `src/hooks/useTeamSettings.ts` (nonce-bump reload pattern, matching `useDeliveries.ts` — a `useCallback`'d loader called directly inside `useEffect` trips the `react-hooks/set-state-in-effect` lint rule, an inline effect-local loader plus external nonce dependency does not).
    - `AuthState` gained `refreshPermissions()` (re-runs `loadRolePermissions()` and updates `RoleContext`'s `overrides`) so a live edit in the grid takes effect for the editing session immediately, without requiring re-login.
    - **Settings.tsx**: "Team" section (member list, edit-pencil → inline Name + Role-dropdown form, active/inactive `Toggle` mapped to `directus_users.status`) gated by `manageRoles`; deliberately dropped the design's Username/PIN fields — this app uses real Directus email/password auth, not the prototype's PIN quick-login. "Roles & Permissions" grid (checkbox matrix, click-to-toggle, "Reset permissions to defaults" deletes all `role_permissions` override rows) gated `role === 'Owner'` directly (hardcoded, not capability-based — granting the ability to edit permissions is itself the highest-privilege action in the app, matching the prototype's own accepted hardcoded gate for this exact section). Cold Storage/Dispatch sections now gated by `manageSettings` (Admin+Owner); Data section's Backup/Restore buttons gated by `backupRestore`, Export CSV button by `exportCSV`; Account/Intake Learning/General(language)/Logout remain universally visible.
    - Two design rows were **not** wired to new independent capabilities and reuse existing ones instead, to avoid regressing features built earlier this session: "Create/edit products" reuses `manage_products` (still Admin+Warehouse — Warehouse needs it for the Products page OOS toggle built this session; the design shows Warehouse unchecked for this row, a discrepancy worth a follow-up decision) rather than being split into a separate "toggle stock" capability.
    - Confirmed via grep before building: no existing UI exists anywhere for "Confirm signed DO & SI returned" or "Clear an order over the credit limit" — both new capabilities are matrix-only for now (same "capability exists, feature doesn't yet" posture already established for `trackCourier`), ready to wire up when those features land.
  - `tsc --noEmit` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` set-state-in-effect error remains), `npm run build` ✓, dev server smoke-tested reachable via `curl`.

- **PickList polish + Cash-up, Deliveries, Reports, Settings pages built (2026-08-07):** all 4 remaining action-card-row/Sidebar-nav routes are now real (no more `<Placeholder>` stubs anywhere in the app — that component was deleted). Full designs: `context/designs/Cash-up.png`, `Deliveries.png`, `Reports.png`, `Settings.png`.
  - **PickList fixes** (per an updated design pass on `context/designs/Pick list.png`): progress bar (`.progressBar`/`.progressBarFill`) added beside the "N of M pulled" text instead of text-only; `orderCount` now derives from the distinct order IDs actually present in `rows` (`new Set(rows.flatMap(...))`) instead of the raw pre-line-filter pool size from `readOrders`; product name + cutting/weigh badges now render inline (`flex-direction: row` on `.productInfo`) instead of stacked, same for qty value + unit (`.productQty`); the date input's native calendar glyph is now visually replaced by a custom `Icon name="calendar"` overlaid via the `::-webkit-calendar-picker-indicator` opacity-0-but-clickable trick (`.dateField`/`.dateIcon`).
  - **New icons** (`src/components/Icon/icons.ts`): `loaf` (`hugeicons:steak`), `box` (`hugeicons:package`, a Reports/Products-context alias distinct from the nav `products` icon), `pack` (`hugeicons:packaging`), `language` (`hugeicons:language-square`), `download` (`hugeicons:download-01`), `export` (`hugeicons:file-export`), `restore` (`hugeicons:file-sync`), `calendar` (`hugeicons:calendar-04`), `location` (`hugeicons:location-01`), `navigation` (`hugeicons:navigation-03`). `checkmark` was renamed to `tick` (`hugeicons:tick-02`) to match naming given in this session's icon list, and `picklist`'s value changed from `hugeicons:task-01` to `hugeicons:license-draft` per explicit instruction.
  - **New capabilities**: none added this pass — `viewPickList`/`viewDeliveryRun`/`reconcileCOD` already existed from the previous PickList-building session and now gate the Cash-up/Deliveries buttons + pages for real.
  - **Schema additions**: `DeliveryProofsCollectionSchema` and `SettingsCollectionSchema` added to `src/lib/schemas.ts` (+ derived types in `types/directus.ts`), plus `readDeliveryProofs()`, `readSettings()`, `updateSettings()`, `aggregateCorrections()` in `src/lib/directus.ts` — all confirmed against the live schema in `context/schema/snapshot.json` (not just `target-db-schema.md`, which has drifted from reality before — see the `products.active` precedent). `delivery_proofs` ended up **not** used by Cash-up/Deliveries (see below) — kept as groundwork since it's now a real, verified read path.
  - **`useCashUp.ts` + `CashUp.tsx`**: COD-ness comes from `customers.pay_timing === 'cod'` (not `delivery_proofs.cod`, which is only populated *after* the courier submits proof — too late for a pre-reconciliation queue). Amount = order total (`order_lines` qty×price, same calc as `OrderDetail`'s `orderTotal`). Grouped by `orders.taken_by` (the courier). **"Confirm" is ephemeral, not persisted** — neither `orders` nor `delivery_proofs` has a `cod_reconciled`-style column in the live schema, and writing to a field that doesn't exist hard-errors in Directus (the exact `products.active` failure mode, documented 2026-08-05). A confirm click still shows a `window.confirm("Confirm Rp X received?")` gate per the design notes, it just doesn't write anywhere yet — Expected/Collected/Remaining reset on reload. Adding a real `cod_reconciled` column is schema-first work, a separate unit.
  - **`useDeliveries.ts` + `Deliveries.tsx`**: pools `stage='dispatch'` orders assigned to the signed-in user OR unassigned (ported from the prototype's `myDeliveries`/`takenByMe` — including its "unassigned counts as mine for anyone" caveat, already flagged as an F-01 compounding issue in `prototype-audit.md`). **Sequence numbers are `deliver_at` order, not a real route-sequence field** — the live schema has no `runSeq`/sort column for the courier's route, so ordering by delivery time is a stand-in, noted in the hook's doc comment. "Mark delivered" is real (`updateOrder` + `appendOrderHistory`, gated on the existing `dispatch` capability). "Navigate" opens a Google Maps search URL built from the address (`window.open`, external link — not a data mutation, no confirmation needed).
  - **`useReports.ts` + `Reports.tsx`**: reads the whole date range's orders + their lines + their customers client-side in one pass (not 6+ separate `aggregate()` calls) since the widgets share the same underlying rows — documented as a scale tradeoff to revisit if a range routinely spans thousands of orders. Range picker: Today/30 days/90 days/All are pure date-math; Month/Range reveal an inline native `<input type="month">` / two `<input type="date">`. Fulfillment donut is a plain CSS `conic-gradient` ring (no chart library — none is installed, and this is a simple enough shape not to need one); Clean/Close-short/Back-ordered map to `--accent-primary`/`--state-warning`/`--state-success` matching the design's own (slightly unconventional — "Clean" isn't green) color choice, each with a text label so identity is never color-alone. Fulfillment status derivation: back-ordered = `stage==='returned'` or `partial_return`; close-short = any line with `short===true`; clean = the remainder. On-time Delivery and Weight Variance/Shrinkage render as a single stat line (not a chart) per the design's own "No data in this period" empty-state precedent, computed from `delivered_at` vs `deliver_at`/`delivery_date`, and `order_lines.weight` vs `qty` respectively. Volume-by-customers and Demand-by-product are plain CSS bar lists (`div` width percentages), grouped/sorted client-side.
  - **`useSettings.ts` + `Settings.tsx`**: reads/writes the `settings` singleton (`id` always 1) directly on each change — no draft/save-button state, matching how simple a toggle/number-field change is. "N learned matches" is a real `aggregateCorrections()` count. **Backup/Restore/Export CSV buttons are visually present but inert** (`window.alert` stub) — F-08 in `prototype-audit.md` already established no export feature exists anywhere in the port; building real backup/restore/CSV-export is out of scope for a Settings-page pass and would need its own schema-first/feature unit. Logout is real (reuses `useAuth().logout()`, same as the Sidebar).
  - **New shared components**: `src/components/StatCard/` (plain bordered number+label block — used by Cash-up's Expected/Collected/Remaining and Reports' 6 stat cards; deliberately lighter than `MetricCard`, which carries an icon+dropdown neither of these needs) and `src/components/Toggle/` (pill switch, used by both Settings toggles).
  - **`Placeholder` component deleted** — after this pass, no route uses it anymore (confirmed via repo-wide grep before deleting).
  - Routes registered in `App.tsx`: `picklist`, `cashup`, `deliveries`, `reports`, `settings` all now point to real page components (`reports`/`settings` were already routed, just to `<Placeholder>`; `cashup`/`deliveries` were stubs from the previous PickList-building pass).
  - `tsc --noEmit` ✓, `eslint` ✓ (only the pre-existing, unrelated `AddItemModal.tsx` set-state-in-effect error remains, untouched), `npm run build` ✓. Verified via `curl` that every new page/component module transforms cleanly through Vite — no live Directus credentials available this session to smoke-test against real data (see the 2026-08-06/07 Session Notes entry on the stale `.env` token).

- **Token longevity, WhatsApp intake flow, Products & Customers CRUD, and Dossiers (2026-07-16):**
  - Token refresh retry flow on Directus 401 API errors (auto refresh with SDK tokens, fallback check on mount).
  - Metrics cards correct calculation (CORS config with headers, cancelled orders mapping to DB boolean `cancelled: true`).
  - Added new pages: `/customers` (clickable paginated search table), `/customers/:id` (credit profile + exposure + order history + create/update), `/products` (paginated active/OOS toggles), `/products/:id` (metadata fields + OOS flag + delete safeguards).
  - WhatsApp Intake Modal: 3-step checkout flow (Horeca select -> WhatsApp text parse API call with `x-internal-token` header -> prefilled `NewOrderModal` review and corrections mapping).
  - Corrections mapping: inline unrecognized line mapping writes to Directus `corrections` for server-side parser training.
  - Order Detail Dossier (`/orders/:id`): progress stepper, client-side order total, print stylesheet, WhatsApp invoice copy builder, and team notes/history logging.
  - Router wiring and navigation links updated in `App.tsx`, `Customers.tsx`, `Products.tsx`, and stylesheets.
  - Compilation is clean (`npm run build` runs successfully).

- **Auth persistence & UI enhancements (2026-07-14):**
  - Token persistence changed from `sessionStorage` to `localStorage` to keep the user logged in until explicit sign-out.
  - Wired `MetricCard` range selection dropdowns with options (Today, This Week, Select Month, Select Year, Select Specific Date) sending queries to parallel backend aggregate endpoints.
  - Gated the "Add New Order" CTA button on the Dashboard so it completely disappears for roles without the `createOrders` capability.
  - Linked Dashboard `StagePill` buttons (both current pipeline and return workflow) to navigate to the Orders registry page with the pre-selected filter applied.
  - Reduced height and added overflow scrollbars to WhatsApp Intake and Needs Attention panels to keep the page structure compact.
  - Updated Orders Page list display to show `orders.order_id` in the ID column and output `"-"` for 0-item entries.
  - Upgraded Stage Selection on the Orders page and order listing header to support styled custom dropdown toggle elements.
  - Added functioning sort buttons/dropdowns in the order table header.
- `src/styles/tokens.css` — full CSS custom property token system (colors, type scale, spacing, radius, shadows, transitions, focus, dark theme overrides) wired into `main.tsx`
- `src/styles/reset.css` — modern CSS reset (box-sizing, margin/padding zero, form font inheritance, focus-visible, reduced-motion scroll, selection color)
- `src/styles/global.css` — base element styles using tokens (body, headings, links, code, form defaults, buttons, tables, scrollbar, `.sr-only` + `.truncate` utilities)
- `main.tsx` — load order set: tokens → reset → global (old `index.css` import removed)
- `context/ui-tokens.md` — quick-reference token lookup for AI sessions
- `context/ui-registry.md` — baseline established via `/imprint audit` (Card, Button, Input, Badge, Nav item, Stage pill, Table, Notification item, Avatar, Modal patterns). **Moved from workspace root → `context/` on 2026-07-07; all context now lives under `context/`.**
- Removed the default Vite scaffold (`src/App.tsx`, `src/App.css`, `src/index.css`, `src/assets/hero.png`, `src/assets/react.svg`)
- Installed `react-router-dom` + `lucide-react` (both in the approved stack)
- App shell: `src/App.tsx` (BrowserRouter + routes), `src/layouts/AppLayout/` (TopNav + Outlet), `src/layouts/TopNav/` (sticky top bar)
- Shared components: `Card`, `Button`, `Avatar`, `MetricCard`, `StagePill` (all token-based, CSS Modules)
- **Admin Dashboard** (`src/pages/Dashboard/`): welcome + New Order CTA, 3 metric cards, 2×4 stage-pill grid, WhatsApp Intake panel, Need approval panel, Open Orders table, Notifications column — matches `context/designs/Dashboard.png`
- Dashboard sections in `src/pages/Dashboard/sections/`: `IntakePanel`, `ApprovalPanel`, `OpenOrdersPanel` (notifications moved out — see below)
- **Notifications moved from page column → bell popover** (`src/components/NotificationsPopover/`): toggled from the TopNav bell, closes on outside-click/Escape. Removed the dashboard's right-column `NotificationsPanel` section + the two-column dashboard grid (now single full-width column).
- Route stubs (`src/pages/Placeholder/`) for Orders / Customers / Reports
- Mock view-model data in `src/data/mockDashboard.ts` + types in `src/types/dashboard.ts` (UI-only, to be replaced by Directus reads)
- Brand logo copied to `src/assets/logo.svg` (from `public/IPP Icon.svg`)
- `index.html` title → `IPP-OrderFlow`
- **Directus wiring (first proof-of-wiring, 2026-07-08):**
  - Installed `@directus/sdk` + `zod` (both in the approved stack)
  - `src/lib/directus.ts` — single Directus client wrapper. Static token auth (`VITE_DIRECTUS_TOKEN`) for the first read-only wiring; will be replaced by email/password login flow later. Exposes typed `readOrders()` / `readMessages()` methods returning `{ data, error }` tuples per code-standards.md.
  - `src/lib/schemas.ts` — zod schemas for Directus collection responses (`OrdersCollectionSchema`, `MessagesCollectionSchema`). Boundary validation per code-standards.md.
  - `src/types/directus.ts` — collection types derived from the zod schemas (single source of truth).
  - `src/vite-env.d.ts` — TypeScript types for `VITE_DIRECTUS_URL` + `VITE_DIRECTUS_TOKEN`.
  - `.env` / `.env.example` — Directus URL + static token (gitignored).
  - `src/hooks/useOpenOrders.ts` — first real data hook. Replaces the `openOrders` mock. Fetches `orders` filtered to `status: 'Open'`, sorted by `-order_id`, validated through zod, mapped to the `OpenOrder` view-model.
  - **OpenOrdersPanel** updated: new "Items" column showing item count (`N items`/`N item`) with a chevron arrow toggle to expand/collapse the line-detail sub-row. Orders with 0 items show a disabled "No items" button. Arrow rotates 180° when expanded.
  - VPS Directus CORS configured: `CORS_ENABLED=true`, `CORS_ORIGIN` includes `http://localhost:3001`, `CORS_METHODS=GET,POST,PATCH,DELETE,OPTIONS` (OPTIONS was missing — required for browser preflight).
  - `npm run build` ✓, `npm run lint` ✓, `tsc` clean
- **Dashboard metrics + stage counts wired to real Directus data (2026-07-08):**
  - `aggregateOrders()` added to `src/lib/directus.ts` — uses `@directus/sdk` `aggregate()` for counts (per code-standards.md: never `readItems()` + `.length`).
  - `src/hooks/useDashboardCounts.ts` — single aggregate call grouped by `status`, maps raw DB statuses to the 8 pipeline stages + the 3 metric buckets (Total / Delivered / Returned). Replaces the mock `metrics` + `stageCounts` exports.
  - Dashboard now reads real counts for metric cards + stage pills; only `intakeMessages`, `approvals`, and `notificationGroups` remain mock (they depend on collections not yet in the schema).
- **Open Orders pagination (2026-07-08):** `useOpenOrders` now accepts `page` + returns `total` / `page` / `pageSize` / `setPage`. Page size = 20. Fetches the current page (`limit` + `offset`) and the total count (`aggregate`) in parallel. `OpenOrdersPanel` renders a pagination footer (`1–20 of N`, prev/next buttons, `page / totalPages` indicator).
- **Scrollbar layout-shift fix (2026-07-08):** `scrollbar-gutter: stable` on `html` in `global.css` — reserves space for the scrollbar gutter whether or not it's visible, so the viewport width no longer changes when a scrollbar appears/disappears.
- **Security: `.env.example` sanitized** — the real Directus static token was replaced with a placeholder. `.env` remains gitignored. `docker-compose.txt` uses `${...}` env var placeholders for all secrets (no hardcoded passwords).
- **Dashboard UI refresh from `context/designs/ui-implementation.md` (2026-07-13):**
  - Switched icon system from `lucide-react` to HugeIcons via Iconify (`@iconify/react` + `@iconify-json/hugeicons`) through a centralized `src/components/Icon/` wrapper.
  - Updated light-theme tokens in `src/styles/tokens.css`: page base white, muted gray surfaces (`#f0f5f5`), border gray (`#d6d6d6`), secondary text (`#7c7c7c`), new primary blue (`#0c4458`).
  - TopNav updated: gray navbar background, HugeIcons tabs/search/settings, tab icon size 16px + text size 16px.
  - Metrics row updated: "Add New Order" moved to the row end as a dedicated accent CTA card; metric icon/label/value styles updated to spec (blue accents, 24px icon, 16px medium text for value+label).
  - StagePill redesigned to stacked layout (20px bold count over 14px medium label) with role-stage highlighting in main blue.
  - New stage model added in `src/lib/pipeline.ts`: main pipeline labels (`New Orders` → `Delivered`) + return workflow stages (`Awaiting Return`, `Admin Action Required`, `Awaiting Signed DO/SI`, `Replacement in Transit`), with Admin-highlight stage mapping.
  - "Need approval" replaced by "Need attention" (`AttentionPanel`) and reordered dashboard layout to: attention + WhatsApp intake side-by-side, Open Orders full width below.
  - Open Orders table updated for the new column set (order id, status, order date, delivery date, sales rep, customer, item). Expansion affordance moved to row-start arrow; full row is now clickable to expand.
  - `useDashboardCounts` updated to emit the new stage labels/workflow and map current DB statuses to the updated stage keys.
- **TopNav + Dashboard top-row layout tweak (2026-07-13):**
  - TopNav tabs moved to the horizontal center of the navbar (`.links` now `flex: 1 1 auto` + `justify-content: center`); nav links stretch to the full nav height (`align-items: stretch` + `align-self: stretch`) so the active tab's bottom underline sits flush on the navbar bottom border (`bottom: -1px`).
  - Dashboard top row split into two grid tracks: welcome (160px) | `.metricsRow` (fills remaining). The inter-track gap is `--space-xl` (24px) so the metric cards are no longer flush against the welcome block; the metric cards + "Add New Order" CTA keep a tight `--space-lg` (16px) gap inside `.metricsRow`. Responsive breakpoints (1024px / 640px) updated to match the new structure.
- **Directus auth + Create New Order flow (2026-07-13):**
  - `src/lib/directus.ts` rebuilt on `authentication('json')` + `rest()` (in-memory JWT). New methods: `login()`, `logout()`, `refresh()`, `readMe()`, `hasToken()`, `readCustomers()`, `readProducts()`, `readOrderLines()`, `getNextOrderNo()`, `createOrder()`, `createOrderLines()`, `appendOrderHistory()`, `updateOrder()`. Static-token fallback kept for the migration transition. All return `{ data, error }` tuples + zod validation at the boundary.
  - `src/lib/schemas.ts` + `src/types/directus.ts` extended with zod schemas + types for `CustomersCollection`, `ProductsCollection`, `OrderLinesCollection`, `OrderHistoryCollection`; `OrdersCollectionSchema` extended with the target-schema fields (`no`, `customer_id`, `stage`, `channel`, `sales`, `deliver_at`, `taken_by`, return/payment flags). Legacy fields stay optional so existing reads don't break.
  - `src/lib/domain.ts` — the domain layer. Exports the 6-role `Role` enum, the 14-key `Capability` enum, coded `ALLOW` defaults (Owner always allowed + short-circuited), synchronous `can(role, capability, overrides?)`, `normalizeRole()` that maps Directus role names to the 6-role enum, and async `loadRolePermissions()` that reads `role_permissions` overrides (with coded fallback on error). Every order mutation passes through `can()` before the SDK call (per ai-workflow-rules.md).
  - `src/hooks/auth-context.ts` + `RoleContext.tsx` + `useAuth.ts` — split across three files to satisfy react-refresh/only-export-components. `AuthProvider` holds the signed-in user, normalized role, capability overrides, and a `can()` bound to them. `useAuth()` / `useCan()` / `useRole()` / `useCurrentUserName()` are the consumer hooks. Tokens are persisted in `sessionStorage` (via the SDK's custom storage option) so reloads within the same tab keep the user signed in; closing the tab logs out.
  - `src/pages/Login/` — Directus email/password login page per Card + Input + Button baselines. Routes: `/login`. On success → redirect to `/` (or the page the user came from). Blocks all other routes when unauthenticated via `<ProtectedRoute>` in `App.tsx`.
  - `src/components/NewOrderModal/` — modal form per Modal baseline. Customer picker (loaded via `readCustomers`) + delivery date + sales rep (auto-filled from the signed-in user's name) + notes + dynamic order lines list (each line = product picker OR free-text name + qty + unit). Submit: `can('createOrders')` gate → `getNextOrderNo()` → `createOrder()` → `createOrderLines()` → `appendOrderHistory()`. On success, `useOpenOrders.refetch()` refreshes the Open Orders table so the new row appears. Disabled when the role can't create orders.
  - `useOpenOrders` gained a `refetch()` method (nonce-based) so the dashboard can refresh after a create.
  - `App.tsx` — `AuthProvider` wraps the router; `/login` route + `<ProtectedRoute>` wraps `<AppLayout>`; already-signed-in users hitting `/login` are redirected to `/`.
  - `TopNav` updated to show the real signed-in user (avatar initials + name + role from `useAuth`) instead of the mock `currentUser`; added a sign-out button that calls `logout()` + navigates to `/login`.
  - Seeded 3 test customers + 5 test products on dev Directus so the New Order form has pickable options.
  - `npm run lint` ✓, `tsc -b` clean, `npm run build` ✓. End-to-end verified via API: order + 2 lines (1 with product_id, 1 free-text) + history all created successfully; CASCADE delete confirmed.
- **Auth persistence + Orders page + order_lines expansion (2026-07-14):**
  - **Reload→login fixed**: SDK's `authentication('json')` now uses a `sessionStorage`-backed custom storage (`sessionAuthStorage` in `directus.ts`) instead of the default in-memory storage. Reloads within the same browser tab keep the user signed in; closing the tab clears sessionStorage and logs out. Auth state only — no business data in sessionStorage (architecture.md invariant #2 not violated). `hasToken()` now checks both the SDK's in-memory token + sessionStorage fallback. `logout()` clears sessionStorage explicitly.
  - `RoleContext.tsx` mount effect now calls `rehydrate()` (which calls `readMe()`) when `hasToken()` is true on mount — so a reload rehydrates the user from the SDK's sessionStorage token without requiring a fresh login.
  - **Username sync fixed**: Dashboard welcome section now uses `useCurrentUserName()` (from `useAuth`) instead of the mock `currentUser.name`. TopNav already used it; Dashboard was missed.
  - **order_lines in Open Orders**: `useOpenOrders` now fetches `order_lines` for each page of orders in a single batch query (`filter[order_id][_in]=id1,id2,…`), groups them by `order_id`, and attaches them to each order's `lines` array. The expandable rows in `OpenOrdersPanel` now show each line's name + qty + unit (e.g. "Beef Tenderloin — 2.5 kg"). `OpenOrderLine` type extended with optional `qty` + `unit` fields. The legacy `order_items` text blob parsing is dropped.
  - **Orders page built** (`src/pages/Orders/`): full order management page with working stage filter dropdown (All stages + 8 pipeline stages + 4 return stages) + search input (searches by order number, legacy order_id, or customer_name). Uses new `useOrders` hook (`src/hooks/useOrders.ts`) — accepts `stageFilter` + `search` params, builds Directus filter with `_or` for stage/status fallback + `_and` for search, fetches orders + order_lines in batch, same expandable-row pattern as the dashboard's OpenOrdersPanel. All props wired to OpenOrdersPanel (loading, error, total, page, pageSize, onPageChange). Clean CSS modules (no inline styles, no raw class names). Page resets to 1 on filter/search change (via event handlers, not effects).
  - `npm run lint` ✓, `tsc -b` clean, `npm run build` ✓.
- **Sidebar + Dashboard redesign (2026-07-20):**
  - **Sidebar layout** (`src/layouts/Sidebar/`): collapsible left navigation replacing the horizontal TopNav. Expanded = 260px (logo + text labels), Collapsed = 72px (icons only). State persisted in `localStorage` key `ipp_sidebar_collapsed`. Pushes main content (no overlay) via `SidebarContext` + `useSidebar` hook. 5 nav links (Dashboard, Orders, Customers, Reports, Settings) + collapse toggle button + user block + logout button (uses shared `Button` component). Split across `sidebar-context.ts` (context), `Sidebar.tsx` (provider + component), `useSidebar.ts` (hook) to satisfy react-refresh/only-export-components.
  - **AppLayout updated**: `display: flex; flex-direction: row`, `<Sidebar />` + `<main>` with `margin-left` transitioning with sidebar width. `SidebarProvider` wraps `AppLayout` in `App.tsx`. Added `/settings` route placeholder.
  - **Button component** (`src/components/Button/`): added `size?: 'sm' | 'md' | 'lg'` prop. `sm` = 32px height, `md` = 40px (default), `lg` = 48px. Refactored CSS — shared `.button` base class + size modifiers + variant modifiers. Added `:disabled` + `:focus-visible` states.
  - **Dashboard TopRow reworked**: Welcome/Name (left) | NotificationsPopover + `Button` variant="primary" size="lg" for New Order (right). Metrics moved to their own 4-column row below. Old custom `.newOrderCard` styles removed. Search bar removed from TopRow.
  - **ReturnWorkflowsPanel** (`src/pages/Dashboard/sections/`): new panel rendering return workflow stages as horizontal list pills (count + label side-by-side) inside a Card. Replaces the old grid of `StagePill` components for returns.
  - **3-column panels grid**: `[Return Workflows | Needs Attention | WhatsApp Intake]` when Admin/Owner, `[Return Workflows | Needs Attention]` for other roles. WhatsApp Intake hidden (not just transparent) for non-admin/owner.
  - **Needs Attention borderless**: removed border + border-radius + background from `.row` items. Now borderless padded text rows with subtle hover background only.
  - **All buttons registered to shared Button component**: Login submit, NewOrderModal cancel + submit, CustomerDetail save, ProductDetail save + delete, Sidebar logout — all now use `<Button>` instead of custom `<button>` + CSS.
  - New tokens: `--sidebar-width-expanded: 260px`, `--sidebar-width-collapsed: 72px` in `tokens.css`.
  - Fixed pre-existing lint errors in `IntakeModal`, `Customers`, `Products` (setState-in-effect pattern).
  - `npm run lint` ✓, `tsc -b` clean, `npm run build` ✓.

- **Order Detail dossier enhancements (2026-07-23 / 2026-07-24):**
  - **Progress stepper fix**: `completedPct` computed from `currentStageIndex / (PIPELINE_STAGES.length - 1) * 100` and applied to the stepper fill width. First stage = 0%, last = 100%.
  - **Customer section navigation**: tapping the avatar + name row in the Order Detail header navigates to `/customers/:id` for recorded customers. Implemented `profileRowClickable` hover style in CSS Modules.
  - **Sticky side panel**: Notes & History column is now `position: sticky; top: 80px; height: calc(100vh - 100px); overflow-y: auto` so it stays in view while the main column scrolls.
  - **View mode quantity fix**: Items list now shows the item's actual `qty` value (e.g. `2`) not the list index (`1`, `2`, …).
  - **Directus `order_lines` write fix (HTTP 500)**: Added `isUuid()` + `sanitizeUuidFields()` helpers in `src/lib/directus.ts` that null-out non-UUID string keys (e.g. legacy product slugs) before POSTing to Directus. Prevents `invalid input syntax for type uuid` Postgres 500 errors. Applied to `updateOrderLine()`, `createOrderLine()`, and `createOrderLines()`.
  - **Schema parse fix in `directus.ts`**: `updateOrderLine()` and `createOrderLine()` now use `OrderLinesCollectionSchema.safeParse(raw)` (was incorrectly calling `.element` on the array schema). `updateProduct()` similarly fixed from `ProductsCollectionArraySchema.element.safeParse` → `ProductsCollectionSchema.safeParse`.
  - **Photo upload for non-weighed items**: View mode now shows a `📷` camera button for items with non-weighed units (`Box`, `Pack`, `pcs`, `ekor`) — mirrors the existing camera button on weighed (Loaf/kg/gram) items. Uploads to Directus Files and appends thumbnail to the item row. Handler: `handleUploadItemPhoto(lineId, e)`.
  - **Add Item Modal (Edit Mode)**: Replaced the bare inline "+ Add Item" button with a full-featured `AddItemModal`:
    - **Step 1**: `<textarea>` free-text input with `✨ Match` button (disabled when empty, also triggers on `Enter`).
    - **Step 2**: After matching, shows parsed `qty` (number input), `unit` (select from `UNIT_OPTIONS`), and best-matched product from catalog (select dropdown). Falls back to a custom name text input when no catalog match found.
    - `parseFreeTextLine()` helper parses patterns like `"2 kilo short rib"`, `"1 Box Wagyu Striploin"` into `{ qty, unit, name, productId }`.
    - `Add to order` button appends the resolved `EditableLine` to `editLines`; `Cancel` / backdrop click closes cleanly.
    - Modal styled consistent with `NewOrderModal` — uses `modalBackdrop` overlay + `addItemModalCard` panel with `addItemTextarea`, `matchedResultRow`, `matchDivider`, `modalActionsRow` CSS classes.
  - **Rules of Hooks fix**: The 3 `useState` declarations for `isAddItemModalOpen`, `addItemText`, `matchedItem` were inadvertently placed after `useEffect`. Moved them to the top-level state block (before `useEffect`) to comply with React Rules of Hooks.

- **OrderDetail / OrderEdit split & shared modals (2026-07-30):**
  - Extracted edit mode out of `OrderDetail.tsx` into `/orders/:id/edit` (`OrderEdit.tsx` + `OrderEdit.module.css`). Cleanly separates view mode from edit mode.
  - Replaced inline Add Item modals with shared `components/AddItemModal/AddItemModal.tsx`.
  - Extracted image preview overlay into `components/ImageDetailsModal/ImageDetailsModal.tsx`.

- **Dashboard StagePills, MetricCards & Prototype Stage Parity (2026-07-31):**
  - **Dashboard StagePill Aggregation Fix**: Updated `useDashboardCounts.ts` to aggregate directly by `stage` from Directus, eliminating status double-counting. Added Finance Review parallel queue calculation (`stage === 'finance'` OR (`stage === 'cold'` and unpaid)).
  - **MetricCard Specifications Rework**: Reworked top 4 metric cards:
    - Open Orders (`range: 'All'`)
    - Total Orders (with range selector: Today, Week, Month, Year, Date)
    - Delivered Orders (with range selector)
    - Cancelled Orders (with range selector)
  - **Prototype Stage Parity**: Expanded `PipelineStage`/`Stage` types in `pipeline.ts` and stage filter dropdown in `Orders.tsx` to include `outstanding`, `awaiting`, `cancelled`, and `returned`. Updated `useOrders.ts` to support special filters (`active`, `pending-docs`, `completed`, `finance`, `cancelled`).
  - **Dashboard 3-column row height constraint**: Bounded `.panelsGrid` and `.panelsGridTwo` row height to `ReturnWorkflowsPanel` wrapping height in `Dashboard.module.css`.

- **Shared OrderRows component + Orders page count badge (2026-08-04):**
  - Extracted the duplicated table-row implementation (`OrderRows`) out of `Orders.tsx` and `OpenOrdersPanel.tsx` into `src/components/OrderRows/OrderRows.tsx` + `OrderRows.module.css`. Both the Orders page and the Dashboard's Open Orders panel now render the same expandable-row component (chevron toggle, line-item sub-row, `expandedGroup` rounded-corner treatment) against the shared `OpenOrder` view-model — no more visual drift between the two tables. Each `OrderRows` instance owns its own `<tbody>` (rows are `tbody`-grouped, not wrapped in one page-level `<tbody>`), matching what `Orders.tsx` already did; `OpenOrdersPanel.tsx` was updated to stop wrapping its own `<tbody>` around the list.
  - Row-level CSS (`orderGroup`, `clickable`, `chevron`/`chevronOpen`, `linesCell`, `lines`, `lineRow`, `lineName`, `lineQty`, `itemsCount`, `arrowCell`) moved out of `Orders.module.css` and `OpenOrdersPanel.module.css` into `OrderRows.module.css`; each page's CSS module now only keeps page-chrome classes (header, table headers, dropdowns, pagination, search).
  - **Orders page header count**: `<h3 className={styles.heading}>` now shows a pill-styled count (`{total}`) beside the stage headline, reusing the exact `.count` styling from `OrderDetail`'s heading (`--text-caption`, `--text-muted`, `--bg-surface`, `--border-default`, `--radius-xl`, `2px 10px` padding). `total` already comes from `useOrders()` filtered by the active stage (+ search), so the count follows the applied stage filter automatically.
  - Fixed a handful of pre-existing `tsc -b` build errors unrelated to this change so `npm run build` is clean again: `OpenOrder.no` wasn't being populated by `useOrders.ts` / `useOpenOrders.ts` / `mockDashboard.ts` (added `no: row.no ?? row.order_id ?? '—'`), removed a dead unused `handleOrderCreated` function + its now-unused `refetchOrders`/`refetchCounts` destructures in `Dashboard.tsx`, and removed an unused `Icon` import in `Login.tsx`.
  - `npm run build` ✓ (`tsc -b` + `vite build`).

- **Products dossier split + filter fix (2026-08-05):**
  - **Products list**: added a `Grade` column (between Category and Brand) to `Products.tsx`'s table + `readProducts()` field selection.
  - **Root-caused and fixed "Invalid filter key \"active\" on \"products\""**: `products.active` is spec'd in `context/schema/target-db-schema.md` ("soft-deactivate old SKUs") but was never actually migrated onto the live Directus DB — `context/schema/snapshot.json` has no `active` field for `products`, only `oos`. The frontend was nonetheless filtering, reading, and writing a `product.active` field that doesn't exist server-side, which Directus tolerates in `fields[]` (silently empty) but rejects in `filter` (hard `INVALID_QUERY` error) — exactly the reported bug when the Active/Out-of-Stock filter buttons were used. Fixed by deriving "active" purely from the real `oos` column everywhere in Products (`isActive = !p.oos`) and dropping all `active` filter/read/write references. Verified directly against the live Directus REST API: the old filter shape reproduces the exact reported error message; the new one returns data. `ProductsCollectionSchema.active` is left in `schemas.ts` as a forward-compatible optional field for whenever the DB migration lands, but nothing reads/writes it anymore.
  - **Products / ProductDetail / ProductEdit split**: mirrors the existing Customers / CustomerDetail / CustomerEdit and Orders / OrderDetail / OrderEdit three-page pattern.
    - `ProductDetail.tsx` is now purely read-only: sticky header with Back (left) + Delete/Edit buttons (right, gated to `manage_products`), Card showing name/accurate-name heading, a divider, a Category/Origin + Grade/Brand 2×2 field grid, and disabled checkboxes for catch-weight + out-of-stock (matches the reference screenshot). The inline edit form that used to live here was removed.
    - New `src/pages/ProductEdit/` (`ProductEdit.tsx` + `.module.css`): editable version of the same fields, following `CustomerEdit.tsx`'s change-tracking pattern exactly — `orig*` state captured on load vs a working copy, `hasChanges` boolean, `canSave = hasChanges && !!name.trim() && !saving` so Save Changes is disabled until an actual edit is made. Header: Back (left), Delete + Cancel + Save Changes (right) — Delete is kept on this page per the "keep the delete button" requirement, gated the same way as `ProductDetail`'s (blocked with an alert if the product is used by existing `order_lines`).
    - New route `products/:id/edit` in `App.tsx`. `/products/new` still resolves through `ProductDetail` (isNew view) → Edit button → `/products/new/edit` → `ProductEdit` creates via `createProduct()`, mirroring the existing Customers new-record flow.
  - **ProductDetail pills instead of disabled checkboxes**: replaced the two read-only/disabled checkboxes with pills, matching the legacy prototype's chip pattern (`IPP-OrderFlow-COMPLETE-2026-07-05/.../ProductDetail.jsx`). Stock-status pill sits top-right of the name/subtitle heading row inside the Card (`In-stock` in the neutral Badge/Pill baseline colors, `Out of stock` in an error-tinted variant when `oos` is true); a `Catch-weight` pill renders below the field grid only when `catch_weight` is true (omitted entirely otherwise, not just hidden). Also fixed a `var(--status-danger)` typo → `var(--state-error)` in both `ProductDetail.tsx` and `ProductEdit.tsx` (the former token doesn't exist in `tokens.css`, so the OOS warning text was silently not turning red).
  - `npm run build` ✓, `npm run lint` ✓ (pre-existing unrelated `AddItemModal.tsx` set-state-in-effect error still present, not touched). Smoke-tested via dev server (routes 200, TSX transforms clean) and the live Directus REST API (see filter fix above).

- **Needs Attention + Notifications wired to Directus (2026-08-05):** first slice of "Wire remaining Dashboard panels to Directus" — `attentionItems` and `notificationGroups` swapped from `mockDashboard.ts` to real reads (`intakeMessages` is still mock, deferred).
  - **`useAttentionItems` hook** (`src/hooks/useAttentionItems.ts`): 6 high-signal buckets per `project-overview.md`'s "Needs Attention" spec (late deliveries, finance approval, print DO/SI, returns awaiting warehouse receipt, returns needing admin action, signed DO/SI not returned), each a real `aggregateOrders()` count. A bucket's `id` doubles as the Orders-page stage-filter key, so `AttentionPanel` items are now clickable (`onItemClick` prop, wired in `Dashboard.tsx` to `navigate('/orders', { state: { stage: item.id } })` — same pattern as `StagePill`/`ReturnWorkflowsPanel`). Buckets with `count === 0` are dropped so the panel only ever shows a "high-signal" list; the count itself renders as a pill badge on each row (`.countBadge`, same visual language as the Orders/Products page header counts). Verified every bucket's filter shape directly against the live Directus API first (including confirming `orders.hold` — used by the pre-existing finance-parallel-queue filter in `useOrders.ts`/`useDashboardCounts.ts` — is a real column despite not being modeled in `schemas.ts`).
  - **New Orders-page filter `late`** (`useOrders.ts` + `Orders.tsx`): "past delivery date" = non-terminal stage + `delivery_date`/`deliver_at` before today. Added as a real `STAGE_OPTIONS` dropdown entry (not just a deep-link-only filter) so it's independently discoverable, not only reachable via the attention item.
  - **`useNotifications` hook** (`src/hooks/useNotifications.ts`) + new `readOrderHistoryFeed()` in `directus.ts`: reads the most recent 60 `order_history` rows across *all* orders (the existing `readOrderHistory(orderId)` is scoped to one order, used by `OrderDetail`/`OrderEdit` — left untouched), batch-resolves each row's `order_id` UUID to a human order number via a follow-up `readOrders({filter:{id:{_in:...}}})` call (same batch-join pattern as `useOpenOrders.ts`'s line-grouping), then groups into `NotificationGroup[]` by local calendar day (newest day first), with day headings formatted via `Intl` in `id-ID` locale (e.g. "Selasa, 04 Agustus 2026") rather than the mock's hand-written mixed EN/ID strings.
  - **`NotificationsPopover.tsx`** now calls `useNotifications()` itself (no props threaded from `Dashboard`) and renders loading/error/empty states in the scroll area; the entry line changed from the mock-specific `Order <strong>{orderId}</strong> {action}` template to `<strong>#{orderId}</strong> {action}` since real `order_history.what` values are already complete sentences (e.g. "Stage advanced: intake → cold", "Document logged: DO 123") rather than the mock's single Indonesian verb ("ditambahkan").
  - Removed the now-unused `attentionItems` and `notificationGroups` exports (and their now-unused type imports) from `mockDashboard.ts`; `AttentionItem`/`NotificationGroup`/`NotificationEntry` types in `types/dashboard.ts` are unchanged and still the shared contract.
  - `npm run build` ✓, `npm run lint` ✓. Every `aggregateOrders`/`readOrderHistoryFeed`/`readOrders` filter shape used here was verified directly against the live Directus REST API (not just typechecked) before wiring it into the hooks, including the UUID→order-number join.

- **Notification bell: unread count + mark-as-read + click-through (2026-08-05):**
  - **`NotificationEntry` gained `at` (raw ISO timestamp) and `orderUuid`** (`types/dashboard.ts`, populated in `useNotifications.ts`) — `time`/`orderId` stay display-only, `at`/`orderUuid` back the new read-tracking and navigation.
  - **New `useNotificationReadState` hook** (`src/hooks/useNotificationReadState.ts`): a single "last read" timestamp cursor, persisted to `localStorage` (`ipp_notifications_last_read_at`). `order_history` has no read-tracking field in Directus and adding one is a schema-first unit of its own (ai-workflow-rules.md), so this is deliberately a local UI cache, not new business-data storage — Directus stays authoritative for the notification data itself, this only remembers where the viewer left off (doesn't conflict with architecture.md invariant #2, which is about *business data* not living in `localStorage`). `markAllRead()` bumps the cursor to `now`; `isUnread(at)` is a plain string comparison (valid because ISO 8601 UTC timestamps sort correctly as strings).
  - **Bell icon**: the plain red `.notifDot` was replaced with an actual unread-count badge (`--accent-primary` background, `--text-on-accent` text, capped at `99+`), and it disappears entirely once unread count hits 0 rather than always showing.
  - **Popover header restructured**: the "N new" count badge moved next to the "Notifications" heading (`.titleRow`); a `variant="ghost" size="sm"` `Button` "Mark as read" was added on the right (disabled when nothing's unread).
  - **Read vs. unread text color**: unread entries render `--text-primary`, read ones `--text-muted` (`.textMuted` modifier) — this flips immediately for every currently-loaded entry the moment "Mark as read" is clicked, which is the intended bulk-cursor behavior, not per-entry tracking.
  - **Entries are clickable**: click navigates to `/orders/:id` using `entry.orderUuid` (`order_history.order_id`, which is the same UUID `readOrder()` expects) and closes the popover; entries whose order couldn't be resolved (`orderUuid` empty) no-op rather than navigating to a broken route.
  - `npm run build` ✓, `npm run lint` ✓. Smoke-tested via dev server (all changed files transform cleanly).

- **Dark mode toggle + logo theming (2026-08-05):**
  - **`src/hooks/theme-context.ts` / `useTheme.ts` / `ThemeProvider.tsx`**: new theme context following the exact `SidebarProvider`/`useSidebar` split pattern (context/hook/provider in separate files to satisfy react-refresh/only-export-components). Persists `'light' | 'dark'` to `localStorage` key `ipp_theme`; falls back to `prefers-color-scheme` on first visit (no stored value); applies `data-theme` on `document.documentElement` via `useEffect`. `App.tsx` wraps the whole tree (including `/login`, outside `AuthProvider`) in `<ThemeProvider>` so theme persists across auth state.
  - **Missing dark-token overrides filled in** (`src/styles/tokens.css` `[data-theme='dark']` block): `--bg-surface-hover-dark`, `--bg-muted`, `--accent-primary-light`, `--accent-secondary`, `--state-error/success/warning/info`, `--notification-accent` — these existed as light-only values before and were unchanged in dark mode. `context/ui-context.md`'s Dark Theme color table updated to match.
  - **Sidebar toggle**: new icon-only `Button` next to the existing collapse-toggle button (same `variant="tertiary"` / styling pattern), sun icon (`hugeicons:sun-01`) shown in dark mode, moon icon (`hugeicons:moon-02`) in light mode — both added to `src/components/Icon/icons.ts`'s `ICONS` registry.
  - **Logo dark-mode fix**: the brand mark's black paths were hardcoded `#030303` in `src/assets/logo.svg`, rendered via `<img src={logo}>` — an `<img>` can't read page CSS variables, so it stayed black in dark mode. Fixed by adding `src/components/Logo/Logo.tsx`, an inline-JSX SVG component with the two black paths using `fill="var(--logo-mark)"` (new token: `#030303` light / `#FFFFFF` dark) and the brand-red paths kept as literal `#EC1C25` (brand color, not theme-dependent). Replaces the `<img>` in `Sidebar.tsx` and `Login.tsx`; `src/assets/logo.svg` itself is left in place (still referenced by history/docs) but no longer imported by any live component.
  - `tsc --noEmit` ✓, `eslint` on changed files ✓.

- **WhatsApp Intake panel wired to Directus (2026-08-05):** third slice of "Wire remaining Dashboard panels to Directus" — `intakeMessages` swapped from `mockDashboard.ts` to a real read, completing that unit (`attentionItems` + `notificationGroups` were done earlier the same day).
  - **New `useIntakeMessages` hook** (`src/hooks/useIntakeMessages.ts`): reads the 20 most recent `messages` rows where `order_id` is still null (i.e. not yet triaged into an order — the existing manual paste-and-parse `IntakeModal` flow is what clears a message off this list by linking it to an order) via the pre-existing-but-unused `readMessages()` in `directus.ts`. `message_id` must be requested even though it's unused in the UI — it's a non-optional field in `MessagesCollectionSchema`, so omitting it from `fields` fails zod validation on the response.
  - **Sender display**: `messages` has no `customer_id` link, and `customers` has no reliable phone-number field to match `sender_number` against (`contact` is free-text) — so the panel shows the raw phone number rather than guessing a name, per explicit decision.
  - `IntakePanel.tsx` gained `loading`/`error` props and empty/loading states, matching the pattern already used in `AttentionPanel`/`NotificationsPopover`. Removed the now-unused mock `intakeMessages` export from `mockDashboard.ts`.
  - `npm run build` ✓, `npm run lint` ✓. Verified the `order_id: { _null: true }` filter shape against the live API before trusting it.

- **Returns sub-flow UI — full parity build (2026-08-06):** the returns workflow had zero UI before this (Dashboard's "Return Workflows" panel only showed counts). Built from a fresh read of the old prototype's `domain.js`/`OrderDetail.jsx` returns logic, which revealed the real model is **parallel buckets, not a linear 4-step sequence** — the same principle the app already uses for Finance running alongside Cold Storage. Once `stage === 'returned'`, an order can sit in multiple buckets at once (`awaiting_return`, `admin_action`, `awaiting_signed_doc`, `replacement_transit`), membership driven by field state, not by `stage` taking 4 different string values.
  - **`returnBucketsForOrder()`** (`src/lib/pipeline.ts`) — the single source of truth for bucket membership, ported from the prototype's `returnBuckets()`. `RETURN_STAGES`/`STAGE_LABELS` keys are unchanged; only what determines membership changed.
  - **`useDashboardCounts.ts` / `useOrders.ts`**: the 4 return-bucket dashboard pills and their Orders-page deep-links previously (wrongly) assumed `stage` itself equals `'awaiting_return'` etc. — replaced with 4 parallel field-based `aggregateOrders` filter queries (same pattern as the pre-existing Finance-parallel-count), verified live before wiring in.
  - **New Directus SDK functions/schemas**: `line_return_photos` (courier refusal-evidence photos per line) and `return_documents` (Accurate return-note / signed-DO/SI evidence, append-only like `order_history`) — both collections already existed live and unused; added CRUD functions in `directus.ts` + zod schemas in `schemas.ts` mirroring the existing `line_photos` pattern exactly. `orders.return_received/return_settle/return_doc/return_inbound/is_replacement/partial_return/returned_reason` and `order_lines.delivered/returned/returned_weigh_photo` were also already live and unused — no schema changes needed for those.
  - **`OrderDetail.tsx`**: entry point is a "Customer refused / returned" button alongside "Mark as Delivered" at the `dispatch` stage (same `dispatch` capability, no new one needed) — expands an inline per-line refused-qty + photo + reason form, writes `order_lines.returned` + `stage: 'returned'`. A new "Customer Return" card (shown whenever `stage === 'returned'`) renders one sub-section per active bucket: **receive** (warehouse verifies/re-weighs with a scale photo → `order_lines.returned_weigh_photo`), **settle** (admin picks one of 4 Accurate document types — 2 close the return directly or move it to `sign`, 2 re-enter the order at Cold Storage as a replacement with `is_replacement: true`), **sign** (upload the signed doc back → closes). All gated by the pre-existing-but-previously-unused `processReturns` capability, now also granted to Warehouse by default (`domain.ts`) alongside its existing Admin-only default, since the Owner Settings toggle page doesn't exist yet to grant it per-role.
  - **Deliberate scope trim**: the prototype's `sign` bucket includes a full courier hand-off/pickup/GPS-tracking chooser. That subsystem doesn't exist anywhere in the current app — not even for normal forward dispatch — so it was left out; the `sign` bucket here just tracks "signed doc uploaded → closed," not who physically carried it. Flagged in Next Up.
  - `npm run build` ✓, `npm run lint` ✓ after every unit (schema/SDK, bucket logic, dashboard counts, Orders filters, OrderDetail entry point + panel, capability default — verified incrementally, not batched). All new/changed Directus filter shapes verified live via curl first.

- **`OrderDetail.tsx` price/contact gating (2026-08-06):** closed the data-exposure gap flagged in `context/prototype-audit.md` §10.1 Enforcement row (the port had regressed vs. the prototype — `OrderDetail.tsx` rendered order total, per-line prices, and customer contact/address completely ungated for every role).
  - **New capability `seeCustomerContact`** added to `src/lib/domain.ts` (`Capability` union + `CAPABILITIES` list), defaulted `true` for Admin/Finance/Courier per the audit's default matrix (Courier needs it to complete deliveries — same reasoning as the prototype's `ALLOW.Courier.seeCustomerContact`), `false` (unset) for Warehouse/Production.
  - **`OrderDetail.tsx`**: `canSeePrices = auth.can("seePrices")` (capability already existed, was just never checked here) and `canSeeCustomerContact = auth.can("seeCustomerContact")` (new) now gate: the Sales Rep / Contact / Address fields in the customer details grid, the per-line price+total row (`.priceCalc`), and the order total row (`.totalRow`). Item weight totals and line names/qty stay visible to everyone (matches the audit's "always visible" list).
  - Scope: only `OrderDetail.tsx` (the read-only dossier) was touched — `OrderEdit.tsx` already implicitly requires `editOrderLines` (Admin/Owner-only by default) to reach, so it wasn't in scope for this pass.
  - `context/prototype-audit.md` updated: §10.1 Enforcement row, §10.2 F-09 note, §10.3 checklist rows for Courier prices / Warehouse contact, and the "Net read" summary all revised to reflect the fix.
  - `tsc --noEmit` ✓, `eslint` on changed files ✓, `npm run build` ✓.

- **Role-aware dashboard stage highlighting / `ROLE_FOCUS` (2026-08-06):** fixed the confirmed live bug from `context/prototype-audit.md` §10.1 ("Highlight application bug") — `Dashboard.tsx:142` was highlighting `ADMIN_HIGHLIGHT_STAGES` for every role with no role check, so a Courier/Warehouse/etc. user saw Admin's stages highlighted as "theirs."
  - **`ROLE_FOCUS: Record<Role, Stage[]>`** added to `src/lib/pipeline.ts`, replacing the flat `ADMIN_HIGHLIGHT_STAGES` array — one map covering all 6 roles across both pipeline stages and return-bucket keys (merges the prototype's `ROLE_FOCUS` + `RETURN_BUCKETS[].roles` into a single source, resolving audit finding F-05's "two competing role→work mappings"). `Owner: []` by design (oversees everything, nothing is highlighted "theirs" — matches the prototype).
  - **`Dashboard.tsx`**: `focusStages = role ? ROLE_FOCUS[role] : []` now drives `highlight` on both the pipeline strip (`StagePill`, unchanged prop) and the Return Workflows panel (`ReturnWorkflowsPanel`, which previously had no highlight concept at all).
  - **`ReturnWorkflowsPanel.tsx`** gained a `focusStages?: Stage[]` prop; its pills now get a `.pillHighlight` class matching `StagePill.module.css`'s existing highlight treatment (same accent border/background/text) — was a genuinely separate visual language before (nothing at all), now unified per F-05.
  - `tsc --noEmit` ✓, `eslint` on changed files ✓, `npm run build` ✓.
  - `context/prototype-audit.md` updated: §10.1 "Role → highlighted stages" / "Highlight application bug" rows, §10.2 F-04/F-05 rows, §10.3 Warehouse/Finance/Admin/Owner checklist rows, and the "Net read" summary all revised to reflect the fix.

- **`viewIntakePanel` capability replaces hardcoded Dashboard role check (2026-08-06):** fixed the F-07 instance in `context/prototype-audit.md` — `Dashboard.tsx:49` had `const isAdminOrOwner = role === 'Admin' || role === 'Owner'` gating the 3-column panel layout and the WhatsApp Intake panel with a hardcoded role check instead of the capability matrix, meaning an Owner couldn't delegate "sees the Intake panel" to another role without a code change.
  - **New capability `viewIntakePanel`** added to `src/lib/domain.ts` (`Capability` union + `CAPABILITIES` list), defaulted `true` for Admin only (Owner is always-allowed via the `can()` short-circuit) — reproduces today's behavior exactly, but is now overridable per-role through `role_permissions` once the Owner Settings UI exists.
  - **`Dashboard.tsx`**: `canViewIntakePanel = useCan()('viewIntakePanel')` replaces `isAdminOrOwner` at both call sites (the `panelsGrid`/`panelsGridTwo` layout switch and the `IntakePanel` render condition).
  - `context/prototype-audit.md` updated: F-07 row in §10.2 marked ✅, "Net read" summary revised.
  - `tsc --noEmit` ✓, `eslint` ✓, `npm run build` ✓.

- **Needs Attention role-partitioning / F-03 (2026-08-06):** fixed the role-blindness half of `context/prototype-audit.md`'s F-03 — every signed-in role previously saw the identical, unordered set of 6 attention buckets.
  - **`src/hooks/useAttentionItems.ts`**: each bucket now carries an optional `capability?: Capability` field (`printDocuments` for the docs-pending/finalise buckets, `approveFinance` for the finance bucket, `processReturns` for both return buckets; the role-agnostic `late` bucket stays untagged, matching the prototype's own choice to leave that entry unfiltered). After the counts load, items are stable-sorted so role-owned (+ untagged/universal) buckets surface first, then buckets owned by another role — every role still sees the full list (no hiding), just reordered.
  - No 6-item truncation exists in the port (never did), so the prototype's specific "buried past position 6" failure mode doesn't apply here — this fix addresses the still-open "role-blindness" half the audit flagged separately.
  - Updated the stale "`useAttentionItems` is role-agnostic for now" line in Open Questions below to reflect the new tagging/sorting (full per-role filtering is still an open call, tied to the broader Dashboard role-scope question).
  - `context/prototype-audit.md` updated: F-03 row in §10.2 marked ✅, related §10.3 checklist rows (Admin/Courier) revised, "Net read" summary updated.
  - `tsc --noEmit` ✓, `eslint` ✓, `npm run build` ✓.

- **F-09 server-ACL plan extended + corrected (2026-08-06):** while scoping F-09/Enforcement (`prototype-audit.md`), found that `context/schema/roles-and-permissions` (no `.md` extension — an earlier session's unfinished draft) already has a complete Directus role/permission plan: full 6-role collection-level CRUD matrix + `order_lines.price` field-level restriction. The audit's claim that this file "exists but is empty" was wrong.
  - Extended the plan to cover the new `seeCustomerContact` capability: added `orders.customer_contact`/`customer_email`/`customer_address`/`sales`/`sales_rep`/`sales_phone_number` to each role's field-level notes and to the summary "Field-level restriction" table (now cross-referenced to the app capability each row enforces, so the doc and `src/lib/domain.ts`'s `ALLOW` matrix don't silently drift apart).
  - Added a "Verification checklist" section (raw `GET /items/...` spot checks per role) so applying the plan can be confirmed against the actual API response, not just the admin UI's permission grid.
  - **Not applied yet** — this is Directus admin-panel configuration (or an admin-token script), not app code; the project's `.env` static token is documented/confirmed read-only (a live check returned `INVALID_CREDENTIALS`), so this session has no way to apply it directly. Whoever has Directus admin access needs to click through `context/schema/roles-and-permissions`'s "How to set this up" section.
  - `context/prototype-audit.md` updated: Enforcement row in §10.1 and F-09 row in §10.2 corrected + revised, "Net read" and header addendum updated.

- **Finance-parallel-queue predicate extracted / F-06 (2026-08-06):** fixed `context/prototype-audit.md`'s F-06 — the `stage='cold' AND hold≠true AND payment_confirmed≠true` condition (Cold+Finance run in parallel; a cold, unpaid, un-held order counts toward both tallies) was hand-rolled in **three** places, not the two the audit's original scan found.
  - **New `financeParallelQueueFilter()`** in `src/lib/pipeline.ts` (alongside `returnBucketsForOrder`/`ROLE_FOCUS`) — the single source for this Directus filter fragment.
  - Replaced the inline copies in `useDashboardCounts.ts`, `useOrders.ts`'s `finance` stage-filter branch, and — the previously-unflagged third copy — `useAttentionItems.ts`'s `finance` bucket filter.
  - Confirmed via `grep -rn "payment_confirmed.*_neq" src/` that no inline duplicate remains outside `pipeline.ts`.
  - `context/prototype-audit.md` updated: Finance-parallel-queue row in §10.1 and F-06 row in §10.2 marked ✅, header addendum + "Net read" summary revised.
  - `tsc --noEmit` ✓, `eslint` ✓, `npm run build` ✓.

- **`ACTOR` map + URL-as-filter-state on Orders (2026-08-06):** fixed the last two remaining code-executable findings from `context/prototype-audit.md`'s Section 8 port-gap table.
  - **`ACTOR: Record<string, Role>`** added to `src/lib/pipeline.ts` (9 entries, ported from the prototype's `ACTOR` — `delivered`/`cancelled`/`returned` intentionally excluded as terminal/off-pipeline). Wired into `src/pages/OrderDetail/OrderDetail.tsx` as a small read-only `.actorNotice` banner ("This order is currently with **Admin**.") shown when the signed-in role isn't the stage's actor, isn't Owner, and can't advance the stage through their own capability grant (`showActorNotice`, computed from the existing `canAdvance`) — purely informational, doesn't touch any existing button's capability gate.
  - **`src/pages/Orders/Orders.tsx`** rebuilt its `stage`/`search` filter state on `useSearchParams()` (`?stage=`/`?search=`) instead of `useState` seeded from router `location.state`; removed the old `prevLocKey` manual re-sync effect (no longer needed — URL changes re-render the page directly, from the dropdown, the search box, browser Back/Forward, or a dashboard deep-link). Search updates use `{ replace: true }` so typing doesn't spam browser history.
  - **`src/pages/Dashboard/Dashboard.tsx`**'s 3 navigate-to-Orders call sites (`StagePill`, `ReturnWorkflowsPanel`, `AttentionPanel`) switched from `navigate('/orders', { state: { stage } })` to `navigate(\`/orders?stage=${stage}\`)`.
  - `context/prototype-audit.md` updated: Stage→actor and URL-as-filter-state rows in §10.1 marked ✅, the related §10.3 checklist row, header addendum, and "Net read" summary all revised.
  - `tsc --noEmit` ✓, `eslint` ✓, `npm run build` ✓.

- **Pick List page + Dashboard action-card row started (2026-08-07):** first slice of `prototype-audit.md`'s "Action-card row" gap (§8/§10.1) — built Pick List for real; Deliveries and Cash Up are registered routes + Dashboard buttons but still `<Placeholder>` stubs (explicitly out of scope for this pass, per the user's request).
  - **New capabilities** in `src/lib/domain.ts`: `viewPickList` (Admin, Warehouse), `viewDeliveryRun` (Admin, Courier), `reconcileCOD` (Admin, Finance) — defaults reproduce the audit's suggested F-07 fix defaults (`§7 F-07`'s table).
  - **`src/hooks/usePickList.ts`**: aggregates every `order_lines` row across orders still at `intake`/`cold`, not on hold, not cancelled, with a delivery date (`deliver_at` or `delivery_date`) matching the chosen day — one Directus read pipeline (`readOrders` → batched `readOrderLines` → batched `readLineCuts` + batched `readProducts`), grouped by `(product_id ?? name) + '|' + unit` (ported from the prototype's `Dev-PickList.jsx` grouping key). Returns both a flat `rows` list and a `groups` list pre-bucketed by `products.category` (empty when no product in the pool has a category, so the page can degrade to a flat list).
  - **`src/pages/PickList/PickList.tsx`**: per the user's 6 requested changes vs. the prototype — (1) each order renders as its own row (`#no` + customer left, qty right, hairline divider) instead of a dot-separated run-on line; (2) a per-product checkbox (ephemeral `useState`, keyed by the same grouping composite, resets on date change) drives a "N of M pulled" progress line in the sticky header; (3) `product.catch_weight` (not the unit string) decides whether the total gets a `~` prefix — weighed totals are a target, counted totals are exact; (4) cutting jobs render as an amber pill (`--state-warning`) under the product name via `line_cuts` join, with the specific cut text(s) moved down beside the order row they belong to; (5) qty/unit stack vertically on the right instead of running inline; (6) rows are grouped under `product.category` subheadings when populated, one flat list otherwise.
  - **New capability-gated buttons** in `Dashboard.tsx`'s `topActions` (left of `NotificationsPopover`): Deliveries (`viewDeliveryRun`), Pick list (`viewPickList`), Cash Up (`reconcileCOD`) — all `variant="secondary"`.
  - **New icons** in `src/components/Icon/icons.ts`: `weight` (`hugeicons:weight-scale-01`), `picklist` (`hugeicons:task-01`), `cash` (`hugeicons:cash-01`), `checkmark` (`hugeicons:tick-02`, a plain glyph for the custom checkbox — distinct from the existing `check` circle-badge icon used elsewhere).
  - **Custom checkbox pattern**: a `role="checkbox"` `<button>` (not a native `<input>`) styled 22×22, `--radius-sm`, `--border-default` unchecked → `--accent-primary` border+fill checked, `checkmark` icon in `--text-on-accent` when checked. Chosen over a native checkbox for exact token-driven styling parity with the design.
  - Routes registered in `App.tsx`: `picklist` → `<PickList />`, `deliveries`/`cashup` → `<Placeholder title="…" />` (matches the existing Reports/Settings stub pattern).
  - Not built (deliberately, per audit's own "build after capabilities land" note and this session's scope): the Deliveries and Cash Up screens themselves, and capability-based route guards (F-01) — every route remains reachable by any authenticated role regardless of capability, same as the rest of the app today.
  - `tsc --noEmit` ✓, `eslint` ✓, `npm run build` ✓. Verified via `curl` that Vite transforms the new modules without error (no live Directus credentials available this session to smoke-test real data — see Session Notes).

- **Route guards for Pick List / Deliveries / Cash Up — F-01 (2026-08-07):** fixed the sharpest open item from `context/prototype-audit.md` §10.2 — those 3 routes exposed real sensitive data (delivery addresses, COD totals, cutting-job detail) with only their Dashboard *button* capability-gated, not the *route* itself; any authenticated role could reach them directly by URL.
  - **New `<Guarded cap="...">` wrapper** in `App.tsx` (alongside the existing `ProtectedRoute`/`LoginRoute` — kept inline there rather than a new component folder, matching how those two already live) — checks `useCan()(cap)`, redirects to `/` when denied. Auth itself stays `ProtectedRoute`'s job; `Guarded` only adds the capability layer, and always sits inside it.
  - **3 routes wrapped** in `App.tsx`: `picklist` → `viewPickList`, `deliveries` → `viewDeliveryRun`, `cashup` → `reconcileCOD`.
  - **Self-guards added inside all 3 pages** (`PickList.tsx`, `Deliveries.tsx`, `CashUp.tsx`) — same `useEffect` + `navigate('/', { replace: true })` pattern `ProductEdit.tsx` already uses for `manage_products` — so the check survives even if the route wrapper is ever dropped in a refactor. This is the defence-in-depth pattern the audit explicitly praised (`Dev-OrderEdit.jsx:27`'s self-guard) and asked to be copied.
  - `context/prototype-audit.md` updated: F-01 row in §10.2, the Route-guards row in §10.1, and the "Net read" summary all marked ✅/revised.
  - `tsc --noEmit` ✓, `eslint` ✓, `npm run build` ✓.

- **Directus server-side ACLs verified + corrected (2026-08-07):** the user had already clicked through `context/schema/roles-and-permissions`'s plan in the Directus admin UI. Audited the live result directly via the Directus REST API (`/permissions`, `/fields`) — the `.env` token turned out to be a genuine Owner/Administrator token with full read+write access (a prior session's "stale/`INVALID_CREDENTIALS`" note no longer holds), so this was checked and fixed without needing a separate admin session.
  - **Collection-level CRUD matrix**: matched the plan almost exactly. One over-grant found and fixed: Admin had full CRUD on `attachments`/`line_cuts`/`line_weighings`/`line_photos` where the plan specifies read-only — deleted the 12 excess create/update/delete permission rows (bulk `DELETE /permissions`).
  - **Field-level restrictions — the part actually worth doing this for — had real gaps**, all fixed via targeted `PATCH /permissions/:id` calls, verified by re-reading each row back afterward:
    - Warehouse's and Production's `orders.read` had `fields: "*"`, exposing `customer_contact`/`customer_email`/`customer_address`/`sales`/`sales_rep`/`sales_phone_number` — restricted to the full field list minus those 6 (fetched live from `/fields/orders`, 39 columns, not the possibly-incomplete `schemas.ts` model).
    - Production's `order_lines.read` had `fields: "*"`, exposing `price` — restricted (14 fields minus `price`).
    - Warehouse's `order_lines.update`/`delete` were unrestricted (`fields: "*"`) despite `create`/`read` correctly excluding `price` — a write-path bypass around an otherwise-correct read restriction. Restricted to match.
  - **Found and fixed a distinct bug class, twice — "backwards" row-filters that would have blocked the very action they were attached to**: Finance's `orders.update` (approve payment) had `permissions: {"_and":[{"payment_confirmed":{"_eq":true}}]}` — requiring the field *already* be `true` before the update could apply, meaning Finance could never actually set it to `true` in the first place. Same bug on Warehouse's `line_cuts.update` (`done:{_eq:true}`) and Courier's `orders.update` (`stage:{_contains:"delivered"}`) — all three cleared to an unrestricted row-filter (`{}`), since these updates are precisely how a row transitions *into* that state, not a precondition for editing it. Also fixed `fields: null` on Finance's and Warehouse's rows (ambiguous/likely blocks all writes) → set to the intended single-field list (`payment_confirmed`, `done`).
  - **Verification method**: inspected the Directus `/permissions` collection directly (exact `fields`/`permissions` values per role/collection/action) rather than only running the plan doc's suggested per-role login spot-checks — catches the same thing but doesn't require live credentials for all 5 non-Owner roles, which this session doesn't have.
  - Final sweep across all 269 remaining permission rows (down from 281 after the Admin cleanup) for any other non-`$CURRENT_USER` row-filter found only Directus's own standard system-collection defaults (`directus_shares`/`directus_flows` visibility) — nothing else app-relevant remained.
  - `context/prototype-audit.md` updated: Enforcement row in §10.1 and F-09 row in §10.2 marked ✅ (applied + verified, not just planned), "Net read" summary revised — this was the audit's last major open item.
  - No app code changed this pass — this was entirely live Directus configuration, verified read-only-safe against the running instance (no destructive collection/role changes, only permission-row field/filter corrections).

- **Reports/Deliveries/Cash Up bugs — missing-required-field pattern + taken_by type bug (2026-08-07):** user reported "Failed to load reports: Invalid customers response" (a zod error, `name` missing) and "Failed to load deliveries: An unexpected error occurred." (a vague Directus error). Also confirmed: all 13 numbered F-xx audit findings are already ✅/➖ — nothing left to "solve" there; both reported bugs were separate, new issues.
  - **Missing-required-field bug, swept across the whole codebase, not just the 2 reported spots**: `CustomersCollectionSchema.name`, `OrderLinesCollectionSchema.id`/`.name`, and `ProductsCollectionSchema.name` are all non-optional in the zod schema, so any `readCustomers`/`readOrderLines`/`readProducts` call whose `fields` array narrows the response but omits one of these fails zod validation on any non-empty result — the same root cause as the earlier `messages.message_id` bug (2026-08-05). Found and fixed **9 instances**, most never yet triggered in practice (an empty result array trivially passes `z.array(...).safeParse([])`, so the bug only surfaces once real matching rows exist): `useReports.ts` (customers missing `name`, order_lines missing `id`), `useDeliveries.ts` (customers missing `name`, order_lines missing `id`), `useCashUp.ts` (same two), `usePickList.ts` (products missing `name`), `ProductDetail.tsx` and `ProductEdit.tsx` (order_lines "used by" check missing `name` — only manifests once a product actually has existing order lines).
  - **Distinct, more significant bug in `useDeliveries.ts`**: filtered `orders.taken_by` (a UUID FK to `directus_users.id` — confirmed live via `/fields/orders/taken_by`) against the courier's **display name** (a string). Postgres rejects the string→uuid cast (`22P02 invalid input syntax for type uuid`), which Directus reports back as the generic "An unexpected error occurred." — reproduced directly against the live API before and after the fix. `useDeliveries(courierName: string)` → `useDeliveries(courierId: string | null)`, `Deliveries.tsx` now passes `useCurrentUserId()` instead of `useCurrentUserName()`.
  - **Related display bug found while checking, same root confusion**: `useCashUp.ts` grouped and displayed couriers by the raw `taken_by` UUID directly (`Avatar`/label would show a UUID string, not a name) — not a crash since grouping/display don't type-check, just wrong. Fixed by resolving `taken_by` through `readAllUsers()` into a display name, same pattern `OrderDetail.tsx` already uses for `who`/history entries.
  - Repo-wide `grep` swept every `readCustomers`/`readOrderLines`/`readOrders`/`readProducts`/`readMessages` call site with an explicit `fields` array (17 files) and every remaining `taken_by` reference — nothing else found.
  - `npm run build` ✓, `npm run lint` ✓. The `taken_by` fix was verified against the live API both before (reproduced the exact Postgres error) and after (clean response) the change.

- **Audit fully closed, `status`→`stage` migration, i18n layer, Toggle sizes (2026-08-07):** the last open item from `context/prototype-audit.md` — capability-resolver parity — plus three items from `progress-tracker.md`'s "Next Up" list, all done in one pass.
  - **Capability parity — audit now has zero open items.** Added the 5 prototype visibility capabilities the port never ported: `seeCustomerCredit`, `browseCustomers`, `browseProducts`, `accessReports`, `trackCourier` (`src/lib/domain.ts`, `Capability` union + `CAPABILITIES` array + `ALLOW` defaults matching the audit's own DEFAULT_PERMISSIONS table). Each was **wired to a real gate**, not left as a dead key:
    - `seeCustomerCredit` replaces `CustomerDetail.tsx`'s reused `seePrices` check on both credit-limit renders (the "Finance" card's field, previously fully ungated, and the "Credit Profile" card).
    - `browseCustomers`/`browseProducts`/`accessReports` gate `/customers`, `/customers/:id`, `/products`, `/products/:id`, `/reports` via `<Guarded cap="...">` in `App.tsx`, plus a self-guard inside each page (same `useEffect` + redirect pattern as `ProductEdit.tsx`), plus hide the matching Sidebar nav link (`NAV_ITEMS` gained an optional `cap` field, filtered via `useCan()`) — mirrors the prototype's own `Dev-Layout.jsx` nav-hiding behavior.
    - `trackCourier` has no live-map feature to gate (GPS tracking is a separate deferred item) — added to the matrix for parity, documented inline as currently inert.
  - **`products.active` vs `oos` — resolved for real, not just documented as an open question.** Checked the prototype directly: `Dev-domain.js`/`Dev-Products.jsx` have zero `active` concept anywhere — `oos` was always the one real field. `context/schema/target-db-schema.md`'s `products.active BOOLEAN DEFAULT TRUE` row was corrected to `oos BOOLEAN DEFAULT FALSE`, matching `context/schema/snapshot.json` (confirmed `oos` exists live as boolean, `active` matches zero rows anywhere in the snapshot). `ProductsCollectionSchema.active` (a "forward-compatible" optional field kept from the original 2026-08-05 fix, in case the migration ever landed) removed from `schemas.ts` — it never will land. `context/architecture.md`'s two `products.active` references corrected/annotated as resolved.
  - **`useOpenOrders`/`useDashboardCounts` migrated off the legacy `status` field.** `useOpenOrders.ts` filtered `{ status: { _in: ['Open'] } }` — a field most live rows never populate, silently hiding genuinely-open orders from the Dashboard panel while the "Open Orders" *metric card* right above it used a completely different, correct `stage`-based definition. The two could disagree. New `openOrdersFilter()` in `pipeline.ts` (same shared-helper pattern as `financeParallelQueueFilter()`) is now the single definition — used by `useDashboardCounts.ts`'s metric, `useOpenOrders.ts`'s panel, and, while auditing for the same drift risk, **3 more inline duplicates found and collapsed**: `useOrders.ts`'s `active` stage-filter branch, `useOrders.ts`'s `late` branch, and `useAttentionItems.ts`'s `lateBucket()` — the latter two extended with the delivery-date `_or` clause on top of the shared base.
  - **i18n layer built and wired end-to-end**, not just scaffolded: `src/i18n/translations.ts` (flat English-keyed dictionary, ported structure from the prototype's `Dev-i18n.js`, ~90 keys covering only strings this app actually renders) + `src/hooks/language-context.ts`/`useLanguage.ts`/`LanguageProvider.tsx` (exact 3-file split as `ThemeProvider`). Applied `t()` to Login, Sidebar, Dashboard + all 4 of its section components (`AttentionPanel`, `IntakePanel`, `OpenOrdersPanel`, `ReturnWorkflowsPanel`), and the "Add New Order" entry flow (`ChannelSelectModal`, `IntakeModal` — `NewOrderModal` as a distinct component no longer exists; `OrderNew.tsx`'s full form was left untranslated, out of scope for this pass). **Found Settings.tsx already had a language switcher UI wired to nothing** — `update({ lang })` persisted to the Directus `settings` singleton but nothing ever read it, so the buttons visibly did nothing. Fixed by making `LanguageProvider` treat `settings.lang` as the team-wide source of truth (fetched once on mount, `localStorage` only as a fast-load cache — this setting is explicitly *not* a personal preference like theme, per its own UI copy: "Sets the default UI language for the whole team") and having Settings' button handlers call the new `useLanguage().setLang()` so a click updates the live session immediately, not just the next reload.
  - **`Toggle` component gained `size?: 'sm' | 'md' | 'lg'`** (default `md` = today's 44×24; `sm` = 36×20; `lg` = 52×28 — proportional knob/translate-distance per size, same math as `Button`'s size scale). Replaced two hand-rolled OOS toggles with the shared component at `size="sm"`: `Products.tsx`'s list-row toggle (was a bespoke button+track+thumb trio, ~45 lines of dead CSS removed) and `ProductEdit.tsx`'s OOS checkbox. `Products.tsx`'s `handleToggleActive` simplified — it never actually used the click event for anything but `stopPropagation()`, which now lives on the wrapping cell instead, so the handler just takes the product.
  - `tsc --noEmit` ✓, `eslint` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓ after every unit, verified incrementally not batched. New `openOrdersFilter()` filter shape verified live via curl before wiring in.

- **Header structure consolidated across 8 form/detail pages (2026-08-24):** `OrderDetail.tsx`'s sticky-header pattern (`.header` as a column of two full-width rows — `.topActionsRow` with Back on the left and an `.actions` group on the right, followed by `.titleRow` holding the page's title content) was previously unique to that one page; every other page had its own inconsistent variant (a `.titleSection` column wrapping Back+title together, with `.actions` positioned as a separate sibling via a row-direction `.header`). Applied the same `topActionsRow`/`titleRow` structure to **OrderEdit**, **OrderNew**, **ProductDetail**, **ProductEdit**, **ProductNew**, **CustomerDetail**, **CustomerEdit**, **CustomerNew** — structure only, no content changes (button labels/icons/disabled-conditions/conditionals all preserved exactly).
  - Each page's `.module.css` gained `.header { flex-direction: column }` and `.topActionsRow`/`.titleRow` rules copied verbatim from `OrderDetail.module.css`, replacing whatever ad hoc `.titleSection` (and, where present, differently-shaped `.titleRow`) rules existed before.
  - Pages with extra title-area content kept it, just relocated *inside* the new `.titleRow` rather than restructuring it: `ProductDetail.tsx`'s `.headingRow` (name/subtitle/stock pills) and `CustomerDetail.tsx`'s Avatar+name/channel block (its old single-purpose `.titleRow` class was renamed to `.customerHeading` so the new outer `.titleRow` container name was free to reuse).
  - Grep-checked every repurposed class name (`.titleSection`, `.titleRow`) for other usages within each file before editing — all were single-use, so no collisions.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓ (393 modules).
  - Not committed yet — batch is ready if the user wants it committed.

- **Return Settlement card + Incoming Return (parallel inbound-return) card added to `OrderDetail.tsx`; on-hold order-actions gating confirmed already correct (2026-08-24):** user reported that as Warehouse, a replacement order in the prototype shows a persistent "Return settlement" record (visible to every role, unlike the Admin/Finance/Owner-only Documents section) plus a separate "Incoming return — receive & verify" card for when a replacement is settled before the returned goods physically come back — neither existed in this port. Also asked whether stageActions/orderActions should freeze during hold; **confirmed via prototype code (`Dev-OrderDetail.jsx`)** that only the "Put on hold" toggle itself is affected by hold (already correctly handled here via `canHold`'s `isHold ? "Resume order" : "Put on Hold"` label swap, plus `stageActions`'s existing `!isHold` gate) — Cancel/Send back/Restore/Reorder all stay available during hold in the prototype's actual `act` object, and the user confirmed to match that exactly, so no `orderActions` change was needed.
  - **Return Settlement card** — ungated (any role), renders whenever `order.return_doc` is set: shows the settlement document label, any lines with `returned > 0` (their qty/weight), and photo evidence (`returned_weigh_photo` per line, plus the latest `return_documents` row of kind `signed_doc`). Ported from the prototype's own ungated `order.returnDoc` block (`Dev-OrderDetail.jsx:1492-1508`).
  - **Incoming Return card** — renders whenever `order.return_inbound` is true (independent of `stage`, so it shows on a replacement order sitting at `cold` while the original return is still in transit); read-only list is visible to everyone, the weight-verification inputs/photo upload/"Confirm received & weighed" button are gated on the existing `canProcessReturns` capability (already covers Admin/Warehouse/Owner). Ported from the prototype's `order.returnInbound` card (`Dev-OrderDetail.jsx:1535-1561`), reusing this port's existing `receiveQtyMap` state and `handleUploadReceiveWeighPhoto` handler rather than adding parallel state. A quiet "Incoming return received · Undo" row shows once resolved, matching this file's established ghost-undo pattern (docs-returned, COD reconcile).
  - **`handleConfirmSettle`'s replacement branch reworked** to actually populate these: computes `pending = !order.return_received` (mirroring the prototype's own `pending` check) — when pending, each returned line's value is snapshotted into the new `inbound_return` field before `returned` is reset to 0, and the order patch now sets `return_inbound: pending` + `return_received: false` (previously neither was ever written, so `return_inbound` stayed permanently `false` and this whole feature was unreachable no matter what an admin chose). New `handleReceiveInbound()`/`handleUndoInbound()` handlers round-trip the value between `inbound_return` (pending) and `returned` (settled) — simpler than the prototype's two-field bookkeeping (`inboundReturn` snapshot + separate `returnedWeight`) since this port's `returned` field already doubles as both "claimed" and "verified" everywhere else (`handleConfirmReceive`'s own pattern).
  - **New live field**: `order_lines.inbound_return`, `numeric(12,3)` nullable (curl + admin token, matching the `sent` field's precedent precision). ACL: read extended to Warehouse/Production/Courier, create+update extended to Warehouse (the roles that already had `sent`/`returned_weigh_photo` in their grants). **Also fixed a latent, previously-unreachable ACL gap while wiring this in**: Warehouse's `orders.update` field grant never included `return_doc`/`return_settle`/`return_received`/`return_inbound`/`is_replacement` at all, even though Warehouse's `processReturns` capability already gated the Settle/Receive UI it renders — the underlying writes would have 403'd for Warehouse specifically (Admin's `fields:["*"]` grant masked this until now). Added all 5 fields to Warehouse's `orders.update` grant live.
  - `schemas.ts` (`OrderLinesCollectionSchema.inbound_return`), `context/schema/snapshot.json`, `context/schema/roles-and-permissions`, and `context/architecture.md`'s `order_lines` field list all updated to match (the last of these also backfilled the pre-existing `sent` field, which had never been added there despite being live since 2026-08-18).
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓. New field/ACL verified live via curl (field creation response + permission-row PATCH responses inspected directly). Not committed yet.

- **Return-documents display made clickable + multi-photo; return-note capture added; receive-weigh photo converted from single-field to multi-photo (2026-08-24):** user asked why the Return Settlement card's photo evidence wasn't viewable/clickable and whether `return_documents` could show more like the Documents section, then separately reported the Incoming Return card's camera-upload button silently not working and asked for multi-photo support.
  - **Return Settlement card now shows every `return_documents` row** (previously only the latest `kind: "signed_doc"` via `latestSignedDoc`), each a clickable thumbnail opening the shared `ImageDetailsModal` — plus **fixed a real duplication bug** found while doing this: the card's returned-lines/photos blocks had gotten nested *inside* the per-returned-line `.map()` (a live edit since the card was first built), so with 2+ returned lines every return-document/weigh-photo thumbnail would have rendered once per line instead of once total. Rewritten as one flat list.
  - **New `return_documents` write path**: `handleConfirmSettle`'s return-note branch never captured any photo at all (a real gap vs. the prototype's `retNotePhoto`) — added an optional photo-upload widget to the Settle form (`handleUploadReturnNotePhoto`, new `noteFileId` state), shown only when "Sales Return Note (no replacement)" is selected, writing a `kind: "note"` row on Confirm. `RETURN_DOC_KIND_LABELS` added for display (`signed_doc`/`signed_draft`/`note`; `signed_draft` stays unused — no intermediate courier-carries-it-for-signing step exists in this port).
  - **Root cause of "doesn't work": the Incoming Return card's camera button had been rewritten (live edit) into a `<label><Button onClick={ref-click hack}/><input/></label>` pattern** — fragile compared to this file's own established, reliable `<label className={styles.actionBtn}><input/><Icon/></label>` (used one card over, in the pre-existing "Awaiting Return" bucket, completely unaffected). Reverted to the reliable pattern. Also caught and fixed a genuine build-breaking bug in the same area while at it: `l.returned_weigh_photo.length > 0` — treating a nullable *string* field as an array — throws whenever the field is null/undefined (the normal pre-upload case); changed to a plain truthy check.
  - **Converted receive/weigh-back photo capture from a single `order_lines.returned_weigh_photo` field to multi-photo**, reusing the *existing* `line_return_photos` table + `createLineReturnPhoto`/`readLineReturnPhotos`/`deleteLineReturnPhoto` (previously only used for the courier's refusal-evidence capture, `refusePhotosMap`) rather than adding a parallel mechanism — same `{id, fileId, url}[]` shape, new persistent `receivePhotosMap` state (fetched on load via `readLineReturnPhotos`, unlike `refusePhotosMap` which resets per form-open since it's a different, ephemeral capture moment). Applied to both entry points that share `handleUploadReceiveWeighPhoto`: the "Awaiting Return" bucket (isReturned card) and the Incoming Return card — full gallery with click-to-enlarge + hover-trash delete, matching the weighing-photos gallery pattern already established elsewhere in this file. `line_return_photos`' schema/directus.ts doc comments broadened accordingly (no longer "courier's refusal-evidence" only).
  - **Real ACL gap found and fixed while wiring this in**: `line_return_photos` was Admin-read-only live (correct when this table only ever held courier-written refusal photos — a courier's job, not Admin's) — but Admin also has `processReturns` and can now reach the same multi-photo upload UI, so Admin needed the same full CRUD Warehouse already had. Granted live via curl (create/update/delete, `fields:["*"]`, matching Warehouse's existing grant exactly).
  - `orders.return_doc` was NOT deleted (user asked whether it should be) — kept because it's a distinct concern from `return_documents` (settlement-type *label* vs. photo *evidence*) and is structurally load-bearing (gates the Return Settlement card, feeds `returnBucketsForOrder()`).
  - Existing seeded demo data (order #260821022's line, previously using the old single-field `returned_weigh_photo`) migrated into 2 `line_return_photos` rows via curl, so the multi-photo gallery has something real to show immediately.
  - `context/schema/roles-and-permissions` (Admin's `line_return_photos` row + summary matrix), `src/lib/schemas.ts`, `src/lib/directus.ts` doc comments, and `src/i18n/translations.ts` (`"Return documents"` key) updated to match.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓. ACL change verified live via curl.

- **Customer Return section rebuilt against `context/designs/customer-return/*.png` (5 admin views + 2 warehouse views), replacing the stepper — a large unit spanning capability model, schema, and both return-workflow cards (2026-08-26):**
  - **Role gating split into 3 capabilities, matching the prototype's own 3 distinct checks** (`Dev-OrderDetail.jsx:1094-1096`) — the old single `processReturns` (Admin+Warehouse+Owner, used for everything) let Admin see interactive receive controls and Warehouse see the Settle decision form, neither of which matches the prototype or the supplied admin/warehouse designs (which never show a warehouse-only role picking the Accurate document, nor an admin weighing a scale). Replaced with `receiveReturns` (Warehouse+Owner — physically weighs the goods back in), `decideReturns` (Admin+Owner — picks the Accurate document type), `signReturns` (Admin+Courier+Owner — whoever carries the revised DO/SI captures the signed photo). `useAttentionItems.ts`'s two return-related digest buckets updated to the correct one each.
  - **New per-line `order_lines.return_verified` field** (boolean, curl + admin token) — the mockups show independent per-line confirmation (one line can show a green "confirmed" state with a checkmark while a sibling line in the same card is still pending weigh-in), which the prior single order-level `return_received` flag couldn't represent. `handleConfirmReceive`/`handleReceiveInbound` (previously one action confirming every returned line at once) rewritten as `handleConfirmReturnLine`/`handleConfirmInboundLine` — per-line writes, with the order-level flag (`return_received`/`return_inbound`) only flipping once every line in the batch is verified. `return_verified` is explicitly reset to `false` at every point a line's `returned`/`inbound_return` value is freshly (re)populated (courier's initial refusal record, the settle-time inbound snapshot, `handleUndoInbound`'s re-open) so a stale `true` from a finished cycle never bleeds into a new one. ACL extended to Warehouse (create/read/update) and Production/Courier (read), matching `inbound_return`'s existing grant.
  - **New shared `renderReturnLineBox()` helper** (plain function, called inline like this file's existing `renderPanel()`-style helpers — not a component, so it never remounts) — one bordered per-line box used by both the direct-return card and the parallel Incoming Return card: icon+qty+unit+name / "returned" label, the order's `returned_reason` repeated per box (this port only has an order-level reason field, unlike the prototype's per-line one — matches the designs, which repeat the same reason text in every box), then one of three states — a green "confirmed" summary with read-only photos and **no delete affordance** (per explicit request: once "Confirm received & weighed" is pressed, the uploaded photo can no longer be removed), an interactive weight input + camera upload + own "Confirm received & weighed" button (only for whoever has `receiveReturns`), or a plain muted status line for everyone else.
  - **Admin Settle form rebuilt to match the designs exactly**: radio-button list replaced with a `<select>` (the prototype's actual control, not the radio list this port had improvised), added the previously-entirely-missing required "Input in Accurate & printed" checkbox that gates the Confirm button on the return-note path (`Dev-OrderDetail.jsx:1246,1253`), Confirm button's label and the explainer text below it now vary by the selected document type exactly like the prototype (`Confirm & close` / `Send revised DO/SI for signing` / `Send replacement — back to Cold Storage`, plus the ONE-document / TWO-document / signing / order-closes-after-receipt helper copy) — every one of these strings was already sitting pre-translated in `translations.ts`, unused, confirming this is what the app was originally scoped for. Gated on `canDecideReturn` (was `canProcessReturns`).
  - **Incoming Return card restyled to match the warehouse designs and confirmed as the same feature** (user asked to verify this) — same `errorCard` red-border chrome and "Customer return" heading as the direct-return card, with a "Warehouse — receive & verify" sub-heading, using the same `renderReturnLineBox()` per line instead of its own bespoke single-button-for-all-lines layout.
  - **Duplicate "Customer Return" card deleted**: a live edit had (correctly, per the user's own direction) started migrating this card from its own standalone position further down the page into the hero-card ternary (replacing the stepper, same slot as the delivered/on-hold cards) but left the old copy in place — both rendered simultaneously whenever `isReturned`. Removed the old copy (was ~250 lines) now that the hero-ternary version is the complete, correct one.
  - **Stepper-replacement position and reappearance, checked directly against the prototype** (`Dev-OrderDetail.jsx:1299-1312`) rather than assumed: the prototype itself only replaces the stepper with a *minimal* colored-dot + stage-label line for `returned`/`cancelled`/`outstanding`/`awaiting` — the full detailed panel is a separate card further down (`renderPanel()`). This port instead puts the *full* card directly in the hero-ternary slot, matching the richer pattern this port had already established for `delivered`/on-hold (not a prototype-literal copy — flagged to the user as a deliberate consistency choice, not an oversight). Confirmed the stepper reappearance behavior: for a replacement decision (`single-replace`/`separate-replace`), `stage` flips to `cold` in the same write that settles the return, so the hero ternary's `isReturned` branch stops matching immediately and the normal stepper (with the existing "Replacement re-entered the pipeline…" note) shows again right away; for a non-replacement close (`return-note`/`revise-return`), `stage` stays `returned` forever by design (a closed return with no replacement never re-enters the pipeline), so the Customer Return card — not the stepper — remains the permanent record.
  - **Live demo data seeded** so the rebuilt UI has real content to view: order **#260821020** (fresh, undecided, goods not yet received) demonstrates the admin Settle form exactly as the 5 design screenshots show it; order **#260821023** now has 2 pending inbound lines (Ribeye + a newly added Tasmania Salmon Portion line, matching the warehouse mockup's 2-item layout) to exercise independent per-line confirm; order **#260821022** still shows the completed Return Settlement + Sign-bucket state from the prior session's seeding.
  - **Found and fixed a real duplicate-rendering bug, reported directly by the user off a screenshot**: order #260821022's pre-existing seed data had `is_replacement: true` and `return_inbound: true` while `stage` had never actually left `returned` (live-checked: `return_settle: "sign"` too — an "impossible" combination no real write path in this app produces, since `handleConfirmSettle`'s replacement branch always flips `stage` to `cold` in the same write that sets `return_inbound`). Because the Incoming Return card was gated on `order.return_inbound` alone with no stage check, this one order rendered *both* the isReturned hero card and the Incoming Return card simultaneously. Fixed the data (cleared the stray `return_inbound` on that order) **and** hardened the gate itself (`!isReturned && order.return_inbound`, same guard added to the "Incoming return received" summary row) so a future stale/hand-edited row can't reproduce the same double-render — the invariant is now enforced in code, not just assumed true by construction.
  - `context/schema/roles-and-permissions`, `src/lib/domain.ts` inline comments, and this entry cover the capability/schema changes; `PERMISSION_GRID` was not touched since `processReturns` was never listed there.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓. New field/ACL verified live via curl.

- **"Received & verified at the warehouse · {time}" + "Re-open & re-weigh" added to the direct-return Customer Return card, ported from a prototype screenshot the user supplied directly (2026-08-26):** matches `Dev-OrderDetail.jsx:1181-1183` exactly — same `['Warehouse','Owner']` gate (this port's `canReceiveReturn`), same confirm-dialog copy. Previously this port had the equivalent row for the *parallel* Incoming Return path only (`handleUndoInbound`); the direct (isReturned-stage) path had no way to correct a mis-weighed line once every line was confirmed.
  - **New live field**: `orders.return_received_at` (nullable timestamp, curl + admin token, same pattern as `cod_received_at`/`payment_confirmed_at`) — there was no accurate "when was this received" timestamp to show; using `orders.updated_at` as a stand-in (briefly considered) would have shown the time of *any* later edit to the order, not specifically the receive event. Set/cleared alongside every existing `return_received` write (`handleConfirmReturnLine`, `handleConfirmInboundLine`, `handleUndoInbound`, `handleConfirmSettle`'s replacement branch) so it never goes stale. ACL: read extended to Warehouse/Production (the two roles with a restricted `orders.read` field list — Admin/Finance/Courier already have `fields:["*"]`), write extended to Warehouse's `orders.update`.
  - **New `handleReopenReceive()`**, mirroring the prototype's `reopenReceive` (`Dev-OrderDetail.jsx:551-562`): resets `return_verified: false` on every returned line (bringing back each line's interactive weigh-in box) while deliberately *keeping* `returned`/photo values as a pre-fill — re-opening never deletes data, matching the prototype's own comment on this — and resets the order's `return_received`/`return_received_at`/`return_settle`/`return_doc` (whatever Admin already decided may have been based on a since-corrected weight). Also added the identical timestamp to the parallel path's existing "Incoming return received" row for consistency, now that a real field exists for it.
  - Live demo: order **#260821021** set to a fully-received, fully-verified state so the new row/button/timestamp render immediately.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓. New field/ACL verified live via curl.

- **"Received & verified" moved from an order-level summary row into each `ReturnLineBox`, and `renderReturnLineBox` extracted into a real component (2026-08-26):** user asked (1) for the confirmed-line summary + reopen to read per-line data instead of one order-wide row, and (2) for `renderReturnLineBox` to become a proper component, plus my opinion on whether other render-helper functions in this file should be too.
  - **New per-line `order_lines.return_verified_at`** (curl + admin token, same ACL extension as `return_verified`) — each confirmed line now shows its own "Received & verified at the warehouse · {its own time}" plus a **per-line** "Re-open & re-weigh" (only that line resets — a sibling line stays confirmed, matching the same independent-per-line spirit the whole rebuild is built on). The old order-level version (`handleReopenReceive`, added last session) is gone, replaced by `handleReopenReturnLine`/`handleReopenInboundLine` — one per card, since each needs to reset a different pending-field (`returned` vs `inbound_return`) and different order-level flags.
  - **`renderReturnLineBox` extracted to `src/components/ReturnLineBox/ReturnLineBox.tsx`**, a real module-level component, not a function called inline. Its dependencies (state, handlers) are now explicit props instead of closure captures — `OrderDetail.tsx`'s two call sites became plain `<ReturnLineBox ... />` JSX. Gave it its own `ReturnLineBox.module.css` rather than importing `OrderDetail.module.css` (per explicit follow-up request) — the classes unique to the box moved out of `OrderDetail.module.css` entirely, the handful of shared small primitives it still needs (thumbnails, undo-row, plain input) were duplicated into the new module. Pulled `formatClock()` out to a new `src/lib/format.ts` in the same pass, since both `OrderDetail.tsx` and the new component need it and it had no dependencies on `OrderDetail`'s own state to begin with.
  - **On "is this good practice," told the user directly, not just done silently**: yes for `ReturnLineBox` specifically — it has a clean, self-contained prop surface and no hooks of its own. But flagged the real reason `renderPanel()` (this file's other big JSX-returning helper, ~1000+ lines, dozens of closure dependencies) exists as a plain function in the first place, per its own comment: defining a component *nested inside* another component's body and rendering it as `<Panel/>` gives it a new identity every render, forcing React to unmount/remount the whole subtree — visible flicker, lost focus, lost local state. The fix was never "call it as a function instead," it's "move the definition to module scope" (exactly what `ReturnLineBox` now does) — a nested-function workaround and a real top-level extraction solve the same problem differently, and only the latter gets the standard React benefits (memoization, testability, smaller `OrderDetail.tsx`). Recommended doing further extractions (starting with `renderPanel()`) incrementally, one at a time with a build/test in between, rather than as one mass refactor — `OrderDetail.tsx` is ~6600 lines of business-critical order-processing logic, and a sweeping extraction pass is a lot of surface area to get wrong at once. Did not do more extractions without confirming scope first.
  - Live demo: order **#260821021**'s line now has a `return_verified_at` timestamp so the new per-line row renders with real data.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓ (396 modules, up from 393 — the new component + its own module.css + the new `format.ts`). New field/ACL verified live via curl.

- **`ReturnLineBox` gained a kept-by-customer / returned pill split, matching `customer_return_admin_04_1.png`, and got its `.module.css` re-split after a round of live experimentation (2026-08-27):** the box previously showed only one line ("{qty} {unit} {name} · returned"); the design shows two separate outlined pills — an accent-colored "{kept qty} kept by customer" (only when > 0) and a red "{returned qty} returned" (green/accent + checkmark once confirmed) — mirroring the prototype's own `keptOf()`/returned split (`Dev-OrderDetail.jsx:1092,1121-1126`, `keptOf = qty − returned` for weight units / `delivered` for counted ones). This port computes it as `kept = max(0, delivered − returnedQty)`, using `delivered` (the original hand-off count) as the baseline.
  - New `.returnPill`/`.returnPillKept`/`.returnPillReturned`/`.returnPillConfirmed`/`.returnPillQty`/`.returnPillLabel` classes in `ReturnLineBox.module.css`.
  - The component's CSS import had drifted back to `OrderDetail.module.css` during live in-IDE experimentation (and `ReturnLineBox.module.css` had been deleted) — re-split cleanly: `.returnLineBox`/`.returnLineBoxConfirmed`/`.returnLineName` moved back out of `OrderDetail.module.css` (confirmed zero remaining references there first), the shared small primitives (`.muted`/`.detailValue`/`.cardActions`/thumbnails/`.undoRow`/`.editInput`) duplicated in, matching the same split rationale as the first pass. Also fixed a real, silent CSS bug found in the drifted version: `background-color: var(bg-surface-hover-dark)` — missing the `--` prefix, so the browser was dropping the whole declaration.
  - Live demo: order **#260821023**'s Tasmania Salmon Portion line now has `delivered: 4, inbound_return: 2` (Lamb Leg stays a full return, no kept pill) so both pill states are visible on one order, matching the design screenshot's exact 2-kept/2-returned split.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓ (396 modules). No schema change needed — `delivered` already existed.

- **New `PhotoUploadButton` component; `OrderRow`'s expanded line items gained unit price + subtotal + a Total row, matching `context/designs/customer-return/OrderRow.png` (2026-08-27):**
  - **`src/components/PhotoUploadButton/`** — a control styled identically to `Button` (imports `Button.module.css`'s own classes directly: variant/size/`.active`) that also holds inline photo thumbnails, replacing the pattern of a separate `Button` (camera trigger) sitting next to its own `.thumbnailsContainer`. A native `<label>` with exactly one labelable child (the hidden file input) — not a nested `<Button>`, the two-labelable-elements pattern this app moved away from earlier this session. Applied to `ReturnLineBox`'s pending-line camera upload, replacing its own inline Button+thumbnails pair.
  - **`OrderRow`'s expanded line-item rows** previously showed only `{qty} {unit} — {name}` with a single bare price number (in a `.lineAmount` class that was referenced but never actually defined in the CSS — silently unstyled). Rebuilt as a 5-column grid (name / qty / unit / `@ unit price` / subtotal) plus a bottom `Total` row summing every line's `price × qty` — both only rendered when `seePrices`. Also fixed `OrderRow.tsx`'s own `currency` formatter, which was missing `style: "currency", currency: "IDR"` (present on every other currency formatter in this codebase, e.g. `OrderDetail.tsx`'s) — prices here rendered as a bare number with no "Rp" prefix until now.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓ (398 modules).

- **Return-note photo converted from single-file to multi-photo (2026-08-27):** `PhotoUploadButton` itself always supported rendering multiple thumbnails (`photos` is a plain array, fully controlled by the caller) — the actual gap was `OrderDetail.tsx`'s return-note usage, which only ever staged one `noteFileId: string | null`. Converted to `noteFileIds: string[]`, appended to on each upload (not replaced), removable per-photo (`handleRemoveReturnNotePhoto`), and `handleConfirmSettle`'s return-note branch now loops the array to create one `return_documents` row (`kind: "note"`) per staged photo instead of at most one. `ReturnLineBox`'s own receive-weigh-photo usage already accumulated correctly (backed by `receivePhotosMap`, a real per-line array) and needed no change.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓ (398 modules).

- **Admin Settle form re-verified against the prototype line-by-line and 3 real gaps fixed, plus a "Warehouse — receive & verify" heading added above the return-line boxes (2026-08-27):**
  - **Confirm button wasn't actually gated on receipt for the return-note path** — re-read `Dev-OrderDetail.jsx:1253` directly: `disabled={!doc || (doc.key==='return-note' && !retPrinted) || (doc && !doc.replacement && doc.key!=='revise-return' && !order.returnReceived)}`. This port's button was missing that third clause entirely — Confirm & close was clickable even before the warehouse received the goods. Added it (`selectedDoc && !selectedDoc.replacement && selectedDoc.key !== "revise-return" && !order.return_received`). Only applies to the return-note (no-replacement) path — the replacement and revise-return paths were already correctly active regardless of receipt (matches their "Send replacement…"/"Send revised DO/SI…" button labels, which imply action now, goods catch up later — the whole point of the parallel Incoming Return card).
  - **Admin's "Goods not back yet…" status line was showing a wrong replacement text once goods arrived** — re-read `Dev-OrderDetail.jsx:1233`: the whole line is wrapped in `{!order.returnReceived && (...)}` — it simply disappears once received, no substitute text. This port had a ternary showing "Received — waiting for an admin to update the Accurate documents and decide." in its place — wrong audience: that string belongs to the *Warehouse*-facing fallback (`Dev-OrderDetail.jsx:1265`, shown when `!canDecide`), not to Admin's own view. Fixed both: Admin's line now hides entirely once received; the Warehouse-facing fallback (this port's `!canDecideReturn` branch, previously a single static string with no received/not-received distinction) now correctly shows the two prototype variants.
  - **New "Warehouse — receive & verify" heading + "Weigh or count what actually came back, then confirm." sub-line**, added above the return-line boxes, shown to `canReceiveReturn` while `!order.return_received` — this port had the "Admin — update Accurate, then process" heading but nothing analogous for Warehouse, ported from `Dev-OrderDetail.jsx:1133-1134`. Same visual treatment as the Admin heading (`.row`/`.fieldLabel`/`.separator`).
  - Also fixed two small regressions found while re-reading this block: the Admin heading had been live-edited into `t("Admin —")` (not a real key) + hardcoded English "update Accurate, then process" — restored to the one correct `t("Admin — update Accurate, then process")` key; and the "order closes only after the warehouse receives the goods" hint had similarly lost its `t()` wrapper (hardcoded English) — restored.
  - `npx tsc -b` ✓, `npx eslint src` ✓ (only the pre-existing unrelated `AddItemModal.tsx` error), `npm run build` ✓ (398 modules).

## In Progress

- None

## Next Up

- i18n coverage is now app-wide but not 100% of every string: `` `Failed to X: ${apiError}` `` runtime error messages stay English-only everywhere (deliberate scope boundary, see the 2026-08-07 i18n entry), and `order_history.what`/`STAGE_FLOW`/pipeline-stage-label free-form entries (e.g. "Stage advanced: X → Y", "Note: …") only translate when they exactly match a dict key — dynamic ones fall through to English. Acceptable for now; revisit if Bahasa-speaking users report it as confusing rather than just an English aside in a mostly-Indonesian screen.
- Apply the `Toggle` `size` variants to any other hand-rolled toggle UI found elsewhere in the app (only `Products.tsx`/`ProductEdit.tsx`'s OOS controls were converted so far).
- Decide the "Create/edit products" vs "toggle OOS" split: the Settings-Owner.png grid shows Warehouse unchecked for "Create/edit products," but Warehouse still needs *some* product-touching capability for the Products page OOS toggle built earlier this session. Currently both stay on the shared `manage_products` capability (Warehouse: true) to avoid regressing that feature — a cleaner fix would split OOS-toggling into its own capability, gated separately from full product create/edit.
- `overrideCreditLimit` (Clear an order over the credit limit) is still matrix-only — no real UI exists yet (`confirmDocsReturned` was wired for real 2026-08-10). Same for `resetData`/`editAfterLock`/`backupRestore`'s actual backup-restore implementation and CSV export's actual file generation (buttons are gated correctly now but still `window.alert` stubs).
- `manage_customers`/`manage_products` were left out of the `Capability` union's "grid-only, no live default" style additions since they already existed — but they're now also rendered in the new Roles & Permissions grid for the first time; double check their coded defaults still make sense as the app grows past this session.
- Live role-based QA pass against `prototype-audit.md` §9's surviving (non-struck) checklist rows — everything there is currently "verified by reading the code," not by actually logging in as each role. No live Directus test accounts for the 6 roles have been exercised this session.

## Open Questions

- Dashboard role scope: Admin-only default view vs per-role default view (from `project-overview.md`) — the auth + role context now makes per-role scoping possible; decision still open.
- Whether to add Zustand for cross-component state or rely on React Context (from `code-standards.md`) — currently using React Context for auth/role; no need for Zustand yet.
- Order number generation: client-side sequential (`getNextOrderNo`) works but has a race window on concurrent creates. A Directus flow / server-side sequence is a later hardening.
- Static-token fallback in `directus.ts`: remove once all read paths migrate to the authenticated client.
- `useAttentionItems` (2026-08-06 update): every signed-in role still sees all 6 buckets (deliberately, matching the prototype's "one dashboard for everyone" design — see `prototype-audit.md` §3.5), but buckets are now tagged with the `Capability` that owns them and role-owned items sort first. Full per-role *filtering* (e.g. Finance only ever seeing the finance-approval bucket) remains an open call tied to the "Dashboard role scope" question above — sorting was chosen over hiding since the prototype itself only filters 3 of its 10 attention entries.

## Architecture Decisions

- **Design tokens live in `src/styles/tokens.css`** — single CSS file with all custom properties, sourced from `context/ui-context.md`. Dark theme via `[data-theme="dark"]` selector on `:root`. No Tailwind, no CSS-in-JS.
- **`context/ui-registry.md`** — the imprint skill's consistency registry. Baseline established 2026-07-07 before any component was built; moved from the workspace root into `context/` the same day so all context sources live in one folder. Derived from the dashboard design spec.
- **Dashboard is UI-first with mock data** — built the full Admin Dashboard shell against `Dashboard.png` using in-memory mock view-models (`src/data/mockDashboard.ts`), deferring Directus wiring to a schema-first unit per `ai-workflow-rules.md`. No localStorage/IndexedDB — mock data is transient and not a source of truth.
- **Open decision resolved for now: Dashboard defaults to the Admin view.** The role-scoped dashboard question remains open for other roles.
- **`context/ui-tokens.md`** — AI-facing quick reference, not the source of truth. `ui-context.md` wins on conflicts.
- **`localStorage` is allowed for local-only UI state that isn't business data** (e.g. the sidebar collapsed flag, the notifications "last read" cursor — see `useNotificationReadState`). Architecture.md invariant #2 bans `localStorage`/`IndexedDB` as the *source of truth for business data* (Directus/Postgres wins on conflict); it doesn't ban client-side caches of viewer-specific UI state that has no server-side equivalent to conflict with. If a feature needs real per-user server-tracked state (e.g. actual per-message read receipts shared across devices), that requires a Directus schema change and is a separate schema-first unit — don't reach for `localStorage` to fake that.
- **Returns are modeled as parallel buckets, not a linear sub-pipeline** (`returnBucketsForOrder()` in `pipeline.ts`). `order.stage` stays `'returned'` for the whole workflow; `awaiting_return`/`admin_action`/`awaiting_signed_doc`/`replacement_transit` are computed from `return_received`/`return_settle`/`return_doc`/`return_inbound`/`is_replacement` and can be simultaneously true — mirrors how Finance already runs alongside Cold Storage rather than blocking it. Any future code that touches return state should call `returnBucketsForOrder()` rather than comparing `stage` against a return-bucket key directly, since that comparison is never true.
- **The Owner Settings "Roles & Permissions" grid is gated by a hardcoded `role === 'Owner'` check, not a capability.** Every other gate in the app resolves through `can()`, but granting the *ability to edit permissions* is itself the highest-privilege action in the system — a capability-based gate would let an Owner accidentally (or a compromised Admin account) grant itself more access. Mirrors the prototype's own accepted hardcoded gate for this exact screen. `role_permissions` writes take effect immediately for the editing session via `AuthState.refreshPermissions()`, but other signed-in sessions only pick up changes on their next login/rehydrate — no realtime push exists.
- **Active language is a team-wide Directus setting (`settings.lang`), not a personal `localStorage` preference** — unlike theme. `LanguageProvider` fetches it once on mount as the source of truth; `localStorage` is only a fast-load cache to avoid an English flash before the fetch resolves. This was a deliberate deviation from the theme precedent, driven by the Settings page's own existing copy ("Sets the default UI language for the whole team") rather than an assumption — check a feature's own stated intent before defaulting to the theme pattern for every new per-user-ish setting.

## Session Notes

- The default Vite scaffold (`App.tsx`, `App.css`, `index.css`) still uses hardcoded purple (`#aa3bff`) and will be replaced when the first real component is built. Do not patch it.
- `tokens.css` is imported before `index.css` in `main.tsx` so tokens are available globally.
- **Correction (2026-08-07): the `.env` `VITE_DIRECTUS_TOKEN` is valid, not stale.** The 2026-08-06/07 note above claiming `INVALID_CREDENTIALS` on `/roles` no longer holds — confirmed directly via `GET /users/me` (returns `kudamonofellas@gmail.com`, role "Owner/Developer/Administrator") and `GET /roles`, `/permissions`, `/users` (all 200) while applying the Directus ACL corrections the same day. It's a genuine admin-capable token, usable for live smoke-testing and read/write API verification — just never echo its value into any output.
